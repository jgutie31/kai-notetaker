---
task: "Build KCG's Tauri/Rust AI-Notetaker, hard gates first"
slug: 20260805-083600_kai-notetaker
project: kai-notetaker
effort: deep
effort_source: classifier
phase: build
progress: 260/284
mode: interactive
started: 2026-08-05T13:36:00Z
updated: 2026-08-07T18:20:00Z
---

## Problem

Kairos Compliance Group needs an internal AI meeting-notetaker to replace an ad-hoc tool built inside Kai (screenshot-based, no real recording pipeline), and to reduce reliance on Fathom/Fireflies — both of which are cloud-first SaaS that KCG cannot fully vet against its own PCI/HIPAA obligations without either a BAA it doesn't have or trusting a vendor's SOC 2 report on faith. Every KCG meeting with Smithville, Nave Security, or any future healthcare/PCI client potentially contains cardholder-environment details or PHI — the exact category of data KCG exists to protect. A 3-round multi-agent Council debate (2026-08-05) converged on a concrete architecture; this ISA is the system of record for building it.

## Vision

Jeremiah opens the app before a client scoping call, hits record, and never thinks about where the audio goes — because it never leaves his machine unless he explicitly asks for a one-off frontier-model polish pass. For meetings already on his connected Microsoft calendar, he doesn't even have to remember to hit record — the app notices the meeting is starting, opens the join link, and is already capturing audio by the time he's in the call, restoring the convenience the old (now-disabled) auto-join cron used to provide, without its screenshot-based architecture. After the call, a searchable, diarized, summarized meeting library exists locally, encrypted, with a tamper-evident record of every access. Two years from now, when KCG pitches this internally-proven tool as a differentiator, the claim "we built this the way we'd want our own auditors to find it" is backed by architecture Jeremiah can actually walk Brian Burke through line by line — not a vendor's marketing page.

## Out of Scope

- **No cloud-hosted transcription or diarization in v1.** Every ASR/diarization pass runs on-device. Cloud sync is a v2+ opt-in behind a hard BAA gate, not a v1 feature.
- **No commercial release, billing, multi-tenant auth, or public sign-up in this ISA's scope.** This is a 2-year internal KCG test bed for Jeremiah, Paula, and QSA partner Brian Burke. Commercialization is a separate future initiative gated on SOC 2 Type II or a Brian-reviewed risk assessment.
- **No native SwiftUI build in v1.** Deferred to an optional v2 polish shell over the same Rust engine via FFI, per Council consensus — explicitly not blocking v1.
- **No mobile (iOS/Android) targets**, despite `create-tauri-app` scaffolding mobile capability by default — desktop only (macOS + Windows).
- **No support for meeting platforms' own bot/join APIs (Zoom bot, Teams bot) in v1 — still true after AutoJoinRecording (2026-08-06).** AutoJoinRecording opens the meeting's join link in the user's default browser/Teams client and starts *local* system/microphone audio capture — it does not use Zoom's or Microsoft's bot APIs to have a bot join on Jeremiah's behalf, and it does not bypass any lobby/waiting-room prompt. Calendar-based auto-join was previously deferred as "a future feature" — as of 2026-08-06 (Jeremiah's explicit next-priority pick after CalendarOAuth) it is now in scope, scoped to Microsoft/Outlook meetings with a Graph `onlineMeeting.joinUrl` only. Meetings whose join link only exists as text in the event body/location (a bare Zoom/Meet URL, not a Graph-native online meeting) are NOT auto-detected in v1 — regex-based link extraction from free text is a real, separate capability, deliberately deferred.
- **No production-grade key-management service (AWS KMS, HashiCorp Vault) in v1.** SQLCipher key lives in the OS keychain (Keychain on macOS, Credential Manager on Windows) — a documented, reasonable v1 choice, not a placeholder for "we'll get to real security later."
- **No frontier-model default path.** Claude/GPT-tier calls are opt-in, per-meeting-capped, explicit-request-only — never a default step in the pipeline.
- **RecordingDurability (2026-08-06) does not guarantee zero audio loss on an ungraceful death — it bounds the loss.** A crash within the first `FLUSH_INTERVAL_SECS` of a brand-new recording, before any checkpoint has happened, can still lose that initial window entirely — data that has not yet been captured cannot be made durable, and data captured but not yet flushed at the exact instant of death is a real, physically-grounded limit, not a bug to chase to zero. The goal is "lose at most a few seconds," not "never lose anything."
- **TeamsPresenceAdhocRecording (2026-08-07) is Microsoft/Teams-only.** No equivalent ad-hoc/unscheduled-meeting detection is built for Zoom or Google Meet in this pass — Jeremiah's own explicit scope cut, pending his separate research into whatever equivalent capability (if any) those platforms expose. An unscheduled Zoom or Meet call still requires a manual Start Recording click.

## Principles

- **Local-first is not a feature flag, it's the architecture.** If a code path can silently start sending audio or transcript text to a network endpoint without an explicit, logged, user-initiated action, that code path is wrong regardless of what problem it solves.
- **Compliance and competitive differentiation are the same decision, not a tradeoff.** Every choice that reduces data egress reduces both audit risk and cost simultaneously — the Council's converged insight from 2026-08-05.
- **Encrypted-but-mutable is not compliant.** Storage security and audit-trail integrity are separate properties; SQLCipher solves the first, a hash-chained log solves the second, and neither substitutes for the other.
- **Retention is code, not configuration.** A setting a user (even Jeremiah) can toggle off is not retention enforcement — it's a suggestion.
- **Architecture earns claims, marketing doesn't.** "Zero third-party data egress" can be said the day the code proves it. "HIPAA/PCI-safe by architecture" cannot be said until a qualified third party (Brian Burke at minimum, SOC 2 Type II eventually) has reviewed it.
- **Ship the gates before the UI.** A beautiful app with no tamper-evident audit trail is a liability with a nice coat of paint.

## Constraints

- Stack is Tauri 2.x, Rust core, React + TypeScript frontend, `bun` as package manager — no npm/npx, per standing operational rule.
- Audio/ASR/diarization/LLM inference must run fully on-device with zero network calls, except the explicitly-gated frontier-model polish path and the explicitly-gated (BAA-blocked-by-default) cloud sync path.
- Audit log MUST be a separate storage primitive from the encrypted SQLCipher database — hash-chained (each entry embeds the previous entry's hash), append-only, independently verifiable for tamper detection without needing the DB decryption key.
- Retention/deletion enforcement MUST live in a scheduled background job inside the data layer with no UI-exposed override, toggle, or disable path.
- Any future cloud-sync code path MUST check a BAA-on-file flag programmatically before any network call to a sync target is possible — the check exists even before any real sync target is implemented, defaulting to blocked.
- Cross-platform target: macOS (Metal-accelerated) and Windows (CUDA/Vulkan-accelerated) from one Rust codebase — no OS-specific fork of the core engine.
- No hardcoded absolute paths in source; use Tauri's app-data-dir resolution and env vars per standing PAI convention (never hardcode `/Users/...`).

## Goal

Ship a Tauri desktop app whose Rust core has a working, unit-tested hash-chained audit log, a code-enforced retention/deletion job, and a BAA-gate stub blocking all cloud paths by default — verified by `cargo test` passing 100% with tamper-detection and override-attempt tests explicitly failing as designed — before any transcription, diarization, LLM, or UI feature code is written on top of it.

## Criteria

### Build & Scaffold

- [x] ISC-1: `~/Projects/kai-notetaker` exists as a git repository with an initial commit (probe: `git -C ~/Projects/kai-notetaker log --oneline` returns ≥1 line).
- [x] ISC-2: Project was scaffolded via `create-tauri-app` with `--manager bun --template react-ts --tauri-version 2` (probe: `package.json` + `src-tauri/Cargo.toml` both present).
- [x] ISC-3: Rust toolchain (`rustc`, `cargo`) resolves on PATH without manual export in a fresh non-interactive shell (probe: `which rustc cargo` in a clean Bash tool call).
- [x] ISC-4: `cargo check` in `src-tauri/` exits 0 on the unmodified scaffold (probe: `cargo check` output ends `Finished`).
- [x] ISC-5: `bun run tauri dev` launches the scaffold app window without a crash (probe: process stays alive ≥10s, no panic in stderr).
- [ ] ISC-6: `bun run tauri build` produces a signed-or-unsigned platform bundle on macOS (probe: `.app`/`.dmg` artifact exists under `src-tauri/target/release/bundle/`).
- [ ] ISC-7: CI-equivalent local build succeeds on a clean checkout (probe: `git clone` to temp dir, `bun install && cargo check`, exit 0).

### Hard Gate 1 — Audit Log (hash-chained, append-only, tamper-evident)

- [x] ISC-8: An `AuditLog` Rust module exists at `src-tauri/src/audit_log.rs` (probe: file exists, `mod audit_log;` referenced in `main.rs` or `lib.rs`).
- [x] ISC-9: `AuditLog::append(entry)` writes a JSONL record containing at minimum: `timestamp`, `event_type`, `actor`, `payload_hash`, `prev_hash`, `entry_hash` (probe: unit test asserts all six fields present in the written line).
- [x] ISC-10: `entry_hash` is computed as `blake3(prev_hash || canonical_payload_bytes)` (probe: unit test recomputes the hash independently and asserts equality).
- [x] ISC-11: The first entry in a fresh log uses a fixed genesis `prev_hash` (all-zero or documented constant) (probe: unit test on empty log's first append).
- [x] ISC-12: `AuditLog::verify_chain()` returns `Ok(())` on an untampered log of ≥5 entries (probe: unit test appends 5 entries, calls verify, asserts `Ok`).
- [x] ISC-13: `AuditLog::verify_chain()` returns `Err` when any single byte in a middle entry's payload is mutated after writing (probe: unit test mutates entry 3 of 5, asserts `verify_chain()` is `Err` and identifies the tampered index).
- [x] ISC-14: `AuditLog::verify_chain()` returns `Err` when an entry is deleted from the middle of the log file (probe: unit test removes line 3 of 5, asserts `Err`).
- [x] ISC-15: `AuditLog::verify_chain()` returns `Err` when entries are reordered (probe: unit test swaps lines 2 and 3, asserts `Err`).
- [x] ISC-16: Appending to the log is the ONLY write operation exposed — there is no `update`, `delete`, or `truncate` function on `AuditLog`'s public API (probe: `grep -c "pub fn" src-tauri/src/audit_log.rs` enumerated and manually confirmed only `append`, `verify_chain`, `read_all`/`iter` are public).
- [x] ISC-17: Every database write (meeting created, transcript saved, export, deletion) triggers exactly one `AuditLog::append` call before the operation is considered complete (probe: integration test counts audit entries before/after each DB-mutating call).
- [x] ISC-18: The audit log file lives outside the SQLCipher-encrypted database file, at a distinct path (probe: `AuditLog::path()` != `Database::path()`, asserted in unit test).
- [x] ISC-19: Anti: the audit log is NEVER writable by any code path outside the `AuditLog` module's public API (probe: `grep -r "audit_log.jsonl\|AUDIT_LOG_PATH" src-tauri/src/ --include=*.rs -l` returns only files that import `audit_log` module, not raw file I/O elsewhere).
- [x] ISC-20: `cargo test audit_log::` passes 100% with zero ignored tests (probe: `cargo test` output, `test result: ok`, `0 ignored`).

### Hard Gate 2 — Retention & Deletion Enforcement

- [x] ISC-21: A `RetentionPolicy` struct exists with a configurable `max_age_days: u32` field, defaulting to a documented value (probe: struct definition + default impl in `src-tauri/src/retention.rs`).
- [x] ISC-22: A `retention_sweep()` function identifies all meeting records older than `max_age_days` (probe: unit test seeds records with varying ages, asserts correct subset identified).
- [x] ISC-23: `retention_sweep()` hard-deletes (not soft-delete/flag) the audio, transcript, and derived-data rows for expired records from the SQLCipher database (probe: unit test asserts row count decreases and `SELECT` for the deleted ID returns no row).
- [x] ISC-24: Every deletion performed by `retention_sweep()` triggers an `AuditLog::append` with `event_type: "retention_delete"` before/as part of the deletion (probe: integration test asserts audit entry count increases by exactly the number of deleted records).
- [x] ISC-25: `retention_sweep()` is registered as a scheduled `tokio` background task that runs on app startup and on an interval, not only on manual trigger (probe: `grep` for `tokio::spawn` or `tokio::time::interval` wrapping the sweep call in the app's setup code).
- [x] ISC-26: There is no public function, Tauri command, or IPC handler that allows the frontend to disable, skip, or delay `retention_sweep()` (probe: `grep -r "#\[tauri::command\]" src-tauri/src/ | grep -i retention` returns zero results, OR any matching command is read-only (e.g., `get_retention_policy`) and contains no disable/skip verb).
- [x] ISC-27: Anti: no code path sets `max_age_days` to a value ≥ 36500 (100 years) or a sentinel "never" value that would functionally disable retention (probe: unit test asserts `RetentionPolicy::new()` rejects `max_age_days` above a hard ceiling, e.g. 3650/10 years).
- [x] ISC-28: `cargo test retention::` passes 100% with zero ignored tests.

### Hard Gate 3 — Cloud Sync BAA Gate

- [x] ISC-29: A `CloudSyncGate` module exists exposing `is_sync_allowed(target: &SyncTarget) -> bool` (probe: file + function signature exist).
- [x] ISC-30: `is_sync_allowed()` returns `false` for every `SyncTarget` when no BAA record exists (probe: unit test with empty BAA store, asserts `false` for a fabricated target).
- [x] ISC-31: A `BaaRecord` type requires at minimum `vendor_name`, `signed_date`, `expiration_date`, and `document_reference` fields — none optional (probe: struct definition, compile fails if any field is `Option` — this is intentionally a hard-fail-to-compile probe reviewed manually, not runtime).
- [x] ISC-32: `is_sync_allowed()` returns `false` if the matching `BaaRecord`'s `expiration_date` is in the past, even if a record exists (probe: unit test with an expired BAA record, asserts `false`).
- [x] ISC-33: `is_sync_allowed()` returns `true` only when a non-expired `BaaRecord` exists for that exact `SyncTarget` (probe: unit test with valid, current BAA record, asserts `true`).
- [x] ISC-34: No network-calling function in the codebase (searched by `grep -r "reqwest::\|hyper::\|TcpStream::connect" src-tauri/src/`) exists yet outside of test/scaffold code — v1 has zero actual sync targets implemented, only the gate (probe: grep returns no matches in non-test source, confirming the gate has nothing to gate around yet — this IS the intended v1 state).
- [x] ISC-35: Anti: no default/wildcard `SyncTarget` bypasses the gate (probe: unit test asserts a target not explicitly matched by any `BaaRecord` returns `false`, not `true`).
- [x] ISC-36: `cargo test cloud_sync_gate::` passes 100% with zero ignored tests.

### Storage Layer (SQLCipher)

- [x] ISC-37: `rusqlite` with the `sqlcipher` feature (or equivalent bundled SQLCipher binding) is added to `Cargo.toml` and compiles (probe: `cargo check` succeeds after dependency addition).
- [x] ISC-38: A database file created by the app is opened with `PRAGMA key` set from a value read from the OS keychain, never a hardcoded or config-file string (probe: `grep -r "PRAGMA key" src-tauri/src/` shows the key value is sourced from a keychain-reading function, not a literal).
- [x] ISC-39: Opening the database file with the wrong key fails (probe: unit test opens with an incorrect key, asserts error).
- [x] ISC-40: [refined — see Decisions 2026-08-05 22:xx] Schema captures meetings, transcript segments (with per-segment speaker attribution), action items, and embeddings — not a literal separate `speakers` table (probe: `SELECT name FROM sqlite_master` shows `meetings`, `transcript_segments`, `meeting_summaries`, `action_items`, `meeting_embeddings`).
- [ ] ISC-41: A `meetings` row references its audio file by a content hash, not a mutable path alone (probe: schema column `audio_sha256` or equivalent exists and is NOT NULL). Real gap, not yet built — `audio_path` is a plain mutable string today.
- [ ] ISC-42: Database migrations run idempotently — running the migration twice on the same file does not error or duplicate schema (probe: integration test runs migration function twice, asserts no error). Real gap — `reset_if_schema_outdated` is an explicit dev-only drop-and-recreate stopgap, not a real migration system.

### Audio Capture

- [x] ISC-43: App can enumerate available audio input devices on macOS (probe: `cpal` or equivalent device-list call returns ≥1 device on the dev machine).
- [ ] ISC-44: App can enumerate available audio input devices on Windows (probe: same call succeeds on a Windows build/CI runner — DEFERRED-VERIFY, no Windows machine available this session, follow-up task required).
- [x] ISC-45: Recording writes a raw audio buffer to a temp file before any processing begins (probe: integration test starts a 2-second recording, asserts temp file exists and has non-zero size).
- [x] ISC-46: Recorded audio is deleted from any temp/scratch location once ASR transcription has completed successfully, unless the user has explicitly opted to retain raw audio (probe: integration test asserts temp file no longer exists post-transcription in default config).
- [x] ISC-47: Anti: recorded audio is never written to a location outside the app's own data directory (probe: `grep -r "tempfile::\|std::env::temp_dir" src-tauri/src/audio*.rs` confirms any temp usage resolves under the app data dir, not system `/tmp` unscoped).
- [ ] ISC-48: A visible recording indicator is shown in the UI whenever the microphone is active (probe: UI component test / manual screenshot — DEFERRED to UI phase).

### ASR (whisper-rs)

- [x] ISC-49: `whisper-rs` is added as a dependency and a minimal binding compiles (probe: `cargo check` succeeds with the crate in `Cargo.toml`).
- [x] ISC-50: A GGUF/GGML Whisper model file loads successfully from the app's model-storage directory (probe: integration test loads a small model (e.g. `tiny` or `base`) and asserts no load error).
- [x] ISC-51: Transcription of a known short test WAV file produces non-empty text output (probe: integration test with a fixture audio file, asserts output string length > 0).
- [x] ISC-52: Transcription runs with Metal acceleration enabled on macOS when available (probe: build feature flag `metal` compiles; runtime log confirms GPU backend selected, not CPU fallback, on the dev Mac).
- [x] ISC-53: Transcription output includes per-segment timestamps (probe: unit test asserts each transcript segment has `start_ms`/`end_ms` fields).
- [x] ISC-54: Anti: the ASR pipeline makes zero network calls (probe: integration test runs transcription with network access blocked/mocked-to-fail, asserts transcription still succeeds).
- [ ] ISC-55: ASR processing of a 10-minute meeting completes in under 5 minutes wall-clock on the dev Mac (probe: timed integration test — DEFERRED, no model downloaded yet this session).

### Diarization (pyannote-ONNX via `ort`)

- [x] ISC-56: `ort` (ONNX Runtime Rust bindings) is added as a dependency and compiles (probe: `cargo check` succeeds).
- [x] ISC-57: A pyannote-derived ONNX diarization model loads successfully (probe: integration test — DEFERRED, model not yet sourced/converted this session).
- [x] ISC-58: Diarization output assigns a consistent speaker label to the same voice across non-contiguous segments in a multi-speaker test fixture (probe: integration test with a 2-speaker fixture, asserts speaker-label consistency).
- [x] ISC-59: Diarization output merges with ASR segment timestamps to produce speaker-labeled transcript lines (probe: unit test on the merge function with fixture ASR + diarization output).
- [x] ISC-60: Anti: diarization pipeline makes zero network calls (same probe pattern as ISC-54).

### Local LLM Pipeline (Summarization / Action Items / Embeddings / Q&A)

- [x] ISC-61: A local inference runtime (llama.cpp bindings or equivalent) is added and compiles (probe: `cargo check` succeeds).
- [x] ISC-62: A quantized local model (Qwen2.5-14B-Instruct or Llama-3.1-8B fallback) loads successfully (probe: integration test — DEFERRED, model not yet downloaded this session).
- [x] ISC-63: Long transcripts are chunked into ~2K-token windows with 200-token overlap before summarization (probe: unit test on the chunking function with a fixture transcript, asserts window size and overlap match spec within tolerance).
- [x] ISC-64: Chunk summaries are hierarchically merged into a single meeting summary (map-reduce), not concatenated raw (probe: unit test asserts final summary length is bounded, not O(n) with chunk count).
- [x] ISC-65: Action-item extraction returns structured JSON (not freeform text) matching a defined schema (probe: unit test validates output against JSON schema, asserts parse success).
- [x] ISC-66: `bge-large` (or equivalent) embeddings are generated locally for each transcript chunk for search/Q&A retrieval (probe: unit test asserts embedding vector of expected dimensionality returned).
- [x] ISC-67: Search/Q&A retrieval over the local embedding index returns relevant chunks for a fixture query (probe: integration test with known fixture transcript + query, asserts top result matches expected chunk).
- [x] ISC-68: Anti: the default summarization/action-item/embedding pipeline makes zero calls to any frontier-model API (Claude/GPT) (probe: integration test runs the full local pipeline with frontier-API network calls mocked to fail loudly, asserts pipeline still succeeds).
- [x] ISC-69: A frontier-model "polish" call is available only via an explicit, separate user-triggered function/command — never invoked automatically by the summarization pipeline (probe: `grep` confirms the frontier-call function is not referenced from within the default pipeline's call graph).
- [x] ISC-70: A frontier-model polish call is capped at exactly one invocation per meeting record — a second attempt on the same meeting is rejected or requires explicit override with a logged reason (probe: unit test calls the polish function twice for the same meeting ID, asserts the second call is rejected).
- [x] ISC-71: The frontier-model polish call is fed the local-generated summary text, never the raw transcript (probe: unit test/mock asserts the outbound payload size is bounded to summary-length, not transcript-length, and asserts raw transcript text is absent from the payload).
- [x] ISC-72: Every frontier-model polish call triggers an `AuditLog::append` with `event_type: "frontier_call"` including which meeting and which vendor (probe: integration test asserts audit entry created).

### Cross-Platform

- [ ] ISC-73: The Rust core compiles on macOS aarch64 (probe: `cargo check` — already passing, ISC-4 baseline).
- [ ] ISC-74: The Rust core compiles for a Windows target via cross-compilation or CI runner (probe: `cargo check --target x86_64-pc-windows-msvc` — DEFERRED-VERIFY, no Windows toolchain/runner set up this session, follow-up task required).
- [ ] ISC-75: `whisper-rs`'s GPU backend selection is conditional per-OS (Metal on macOS, CUDA/Vulkan on Windows) via Cargo feature flags, not a single hardcoded backend (probe: `Cargo.toml` feature declarations reviewed for per-target conditionality).
- [x] ISC-76: No `#[cfg(target_os = "macos")]`-only code path exists for any of the three hard-gate modules (audit log, retention, BAA-gate) — those modules are fully OS-agnostic (probe: `grep -r "cfg(target_os" src-tauri/src/audit_log.rs src-tauri/src/retention.rs src-tauri/src/cloud_sync_gate.rs` returns zero matches).

### Live Device Switching (added 2026-08-05, beyond the original Council plan — Jeremiah's own request after hands-on testing)

- [x] ISC-94: Every capture stream, regardless of source device, is resampled in real time to one fixed `CANONICAL_SAMPLE_RATE` (48kHz) before reaching the shared recording buffer (probe: `stopped_recording_always_declares_canonical_sample_rate` — WAV header always declares 48000Hz regardless of device).
- [x] ISC-95: `RecordingSession::switch_device` preserves all already-captured audio and the buffer continues growing after the switch (probe: `switch_device_preserves_already_captured_audio_and_keeps_growing`).
- [x] ISC-96: Switching to a nonexistent device name returns a clean typed error and does NOT disrupt the currently-active stream (probe: `switch_device_to_nonexistent_name_errors_without_disturbing_current_stream` — asserts the original stream is still capturing after the failed switch).
- [x] ISC-97: The real-time streaming resampler (rubato `Async::new_sinc`) produces an output frame count within a small, sinc-filter-explained tolerance of the mathematically expected input/output ratio (probe: `streaming_resampler_upsamples_to_expected_frame_count`, 44100→48000 over 2s of real input).
- [ ] ISC-98: Anti/DEFERRED-VERIFY: true cross-hardware resampling correctness (two physically different real devices with genuinely different native sample rates, audibly verified) is NOT confirmed — this dev machine has exactly one real input device. Follow-up task: verify with a second physical mic/interface attached, or a CI runner with virtual audio devices at different rates.
- [x] ISC-99: The device selector remains interactive during an active recording (not just pre-recording), with a visible "Switching…" state and rollback to the previous selection on a failed switch (probe: manual + code review of `handleDeviceChange` in `RecordingControl.tsx`; no automated frontend test written this pass).

### Meeting Processing Pipeline + Library/Detail (added 2026-08-05, Jeremiah's explicit next-priority)

- [x] ISC-100: When a recording stops, the full pipeline (resample to 16kHz → ASR → diarization → merge → map-reduce summarization → action-item extraction → embeddings) runs automatically and persists a real, structured meeting record — not a mock (probe: `full_pipeline_end_to_end_on_real_48k_fixture`, real 48kHz fixture through all 4 real engines, real content assertions on the resulting transcript).
- [x] ISC-101: Any pipeline step failure marks the meeting `failed` with a real error message rather than leaving it stuck at `processing` indefinitely (probe: `failed_step_marks_meeting_failed_not_stuck_processing`).
- [x] ISC-102: The four heavy models load exactly once, in a background OS thread at app startup, not blocking window appearance and not reloaded per-recording (probe: live log line `all pipeline engines loaded and ready` confirmed this session; `stop_recording`'s background thread waits on the shared `Arc<Mutex<Option<Arc<PipelineEngines>>>>` rather than loading its own copies).
- [x] ISC-103: Meeting Library screen lists real persisted meetings (title, date, duration, status) and polls every 4s so a `processing` row flips to `ready` without a manual refresh (probe: code review of `MeetingLibrary.tsx`; no automated frontend test written this pass).
- [x] ISC-104: Meeting Detail screen displays real transcript (speaker-labeled, timestamped), summary, and action items for a selected meeting, polling until processing completes (probe: code review of `MeetingDetail.tsx`; no automated frontend test written this pass).

### InstallerFirstRun

- [x] ISC-105: A function exists that reports which of the 5 local models are missing from disk (probe: `cargo test model_provisioning::missing_models`).
- [x] ISC-106: Every model download URL is byte-verified against the real file already on disk before being trusted, not assumed from filename alone (probe: `curl -sIL` content-length matched `stat -f%z` for all 5 during BUILD — recorded in Changelog).
- [x] ISC-107: A download writes to a temp path and only lands at the final destination on full success — a crash/interrupt never leaves a partial file that looks present (probe: `download_model`'s `.download-tmp` + rename-on-success code path).
- [x] ISC-108: Engine loading at app startup resolves models from a runtime-real directory (`$APPDATA/models`), not a build-time-only path that wouldn't exist on an installed end-user machine (probe: `resolve_models_dir`; `grep -n "CARGO_MANIFEST_DIR" src/lib.rs` shows zero remaining runtime uses outside the dev-fallback branch).
- [x] ISC-109: The frontend shows a real per-model progress bar (bytes downloaded / total) during first-run download, not just a spinner (probe: `FirstRunSetup.tsx`, `model-download-progress` event payload includes `downloaded`/`total`).
- [ ] ISC-110: Anti: the installer never bundles the ~5.5GB of models into the app installer itself (probe: `du -sh` on the built `.dmg`/`.app` bundle stays well under 1GB — DEFERRED-VERIFY, no release build produced this session, follow-up task: verify at first real `tauri build`).
- [x] ISC-111: A real end-to-end first-run download (not just unit tests) succeeds on a machine with zero models present in `$APPDATA/models` (probe: `cargo run --example verify_model_download` — real network download of all 5 models, 4/5 exact byte-size match, the 5th (archive-extracted) confirmed via SHA256 checksum identical to the known-good dev-tree copy; a false "MISMATCH" on the first run was traced to the test comparing an extracted file's size against the compressed archive's download size — a test-script bug, not a real download/extraction defect — fixed before re-confirming clean). `$APPDATA/models` is now genuinely fully populated for real, so `resolve_models_dir` will use it going forward instead of the dev-tree fallback.
- [x] ISC-126: `README.md` documents real, verified setup steps (prerequisites, first-time clone/build/run, model download behavior, encryption/Keychain note) instead of the generic create-tauri-app boilerplate it shipped with (probe: `git show HEAD:README.md` before this fix showed only "Recommended IDE Setup"; real prerequisites — cmake, Xcode CLT — confirmed via `grep 'name = "cmake"' Cargo.lock` + `which cmake`, not guessed).
- [x] ISC-127: Secure secret storage (SQLCipher key today, OAuth refresh tokens next) is cross-platform via the `keyring` crate (macOS Keychain / Windows Credential Manager / Linux Secret Service behind one API), not macOS-only `security-framework` code that would need rewriting for Windows/Linux later (probe: `security_framework` fully removed from `Cargo.toml`/source — `grep -rn "security_framework" src/` zero matches; real production database with Jeremiah's migrated meetings confirmed still decryptable after the swap via `cargo run --example import_legacy_recordings -- inspect 3` — same title, same summary, same 338 segments as before the migration, proving `keyring`'s macOS backend found the exact same Keychain item rather than silently generating a new key).

### MeetingImport (old cron migration)

- [x] ISC-112: The WAV reader feeding the pipeline accepts any real mono sample rate, not only the live recorder's own 48kHz (probe: `read_wav_mono_resampled_accepts_a_real_16k_fixture`).
- [x] ISC-113: All real recordings from the old cron's `recordings/` directory (audio only, never the sibling `screenshots/` directory) are imported and reach `ready` status in the new app (probe: `import_legacy_recordings` example's own summary line — `=== import complete: 6 succeeded, 0 failed ===`).
- [x] ISC-114: The old `meeting-watcher` PULSE.toml cron job is disabled only after ISC-113 passes, never before (probe: `enabled = false` in PULSE.toml's `meeting-watcher` job, added only after the 6-succeeded-0-failed confirmation above).
- [x] ISC-115: Anti: the import never runs concurrently with the live app against the same database/audit-log files (probe: this session's Decisions log — dev app process stopped before the import ran, restarted only after).
- [ ] ISC-116: Installer downloads are verified by cryptographic checksum, not byte-size alone (probe: `grep -n "sha256\|Sha256" src-tauri/src/model_provisioning.rs` — currently zero matches; DEFERRED, real gap surfaced by advisor call, not this session's scope).
- [ ] ISC-117: The SQLCipher Keychain key has a documented recovery/escrow path if the Keychain item ever becomes inaccessible (probe: a RUNBOOK section naming where the key lives and what to do if access is denied — DEFERRED, not yet written).
- [ ] ISC-118: Anti: a local-LLM inference crash (any `GGML_ASSERT`/abort, not only the n_batch class fixed this session) cannot kill a concurrently-recording live meeting (probe: architecture review or integration test proving live capture survives a forced LLM-thread abort — DEFERRED, real architectural risk surfaced by advisor call; today's fix closed the one specific crash class actually hit, not this whole risk category).

### Meeting Management + Real-Audio Quality Fixes (2026-08-05/06, Jeremiah's hands-on testing of the migrated real meetings)

- [x] ISC-119: A meeting's title can be renamed after the fact, persisted to the database (probe: `rename_meeting_updates_title`; frontend click-to-edit in `MeetingDetail.tsx`, gated to `status === "ready"`).
- [x] ISC-120: A meeting can be deleted (soft — hidden from the list, not destroyed) from either the library list or the detail screen, with an ~8s "Undo" toast shared across both entry points (probe: `deleted_meeting_disappears_from_list_but_undelete_restores_it`; shared `pendingUndo` state lifted to `App.tsx` so either delete path gets the same grace window).
- [x] ISC-121: The diarization clustering threshold matches sherpa-onnx's own official example for the unknown-speaker-count case (0.90), not the crate's bare struct default (0.5) — confirmed via a real reprocessing run of a genuine 43-minute 3-person meeting: distinct speaker labels dropped from 82 down to 11-12 (probe: `cargo run --example import_legacy_recordings -- inspect 3`, before/after `distinct speaker labels` count).
- [x] ISC-122: ASR transcription of a long real meeting does not degenerate into a single hallucinated sentence repeated across dozens of separate ~30-second windows — confirmed via the same real reprocessing run: segment count dropped from 1579 (94% identical repeated text) to 338 (zero repeated-phrase pattern; most-repeated line is a natural "Mmm." at 12 occurrences), and the resulting summary is genuinely coherent, on-topic PCI-DSS/Smithville/QSA content instead of a dismissive "appears to be a test recording" (probe: `sort | uniq -c` on the real transcript segments, before/after).
- [x] ISC-123: Anti: `no_context: true` alone does NOT fix cross-window ASR repetition — a real, negative result logged rather than silently discarded. The actual fix (`logprob_thold: -0.5`, tightening the `is_no_speech` suppression gate) is a different, independently-necessary mechanism (probe: Decisions/Changelog entry showing byte-identical transcript output before/after the `no_context`-only change).
- [x] ISC-124: Meeting Detail's top section (summary/action items) has a real, resize-responsive constrained height, not unconstrained natural sizing — content that doesn't fit scrolls internally at any window size, and a manually-dragged split stays valid (re-clamped via `ResizeObserver`) if the window is resized afterward (probe: Jeremiah's real report — Action Items weren't visible until going fullscreen; fixed via always maintaining a real clamped `topHeight` instead of a `null` "let CSS decide" escape hatch).
- [x] ISC-125: Generated meeting summaries never open with an LLM meta-commentary preamble ("Here is a combined meeting summary of N sentences:") — both prompts (chunk map-step and meeting reduce-step) explicitly forbid it, and a real-model regression test asserts the output doesn't start with "here is"/"here's" (probe: `short_thin_transcript_gets_a_direct_summary_not_meta_commentary`). The 4 already-generated real summaries with this preamble (meetings 1, 2, 3, 5) were cleaned in place via a one-off `clean-summaries` mode rather than re-run through the full pipeline, since only the preamble text was wrong, not the underlying content.

### Real Speaker Identification (2026-08-06, Jeremiah's explicit next priority — "the best it possibly can be, considering that will be what makes the AI summaries even better")

- [x] ISC-128: `known_speakers` / `known_speaker_embeddings` / `meeting_speaker_labels` schema exists and round-trips real data — enrolling the same name twice reuses one row (idempotent by name), embedding samples persist and reload as `Vec<f32>`, and per-meeting labels upsert via `ON CONFLICT(meeting_id, speaker_index)` rather than duplicating (probe: `storage::` speaker tests below).
- [x] ISC-129: `SpeakerIdEngine` wraps sherpa-onnx's `SpeakerEmbeddingExtractor` + `SpeakerEmbeddingManager` for real voice-fingerprint extraction and nearest-match search, with the match threshold (0.6) verified against sherpa-onnx's own official example (`speaker_embedding_manager.rs`, `let threshold = 0.6;`) rather than guessed (probe: `speaker_id::` tests below).
- [x] ISC-130: During normal meeting processing, each distinct diarized speaker's concatenated audio (skipping any speaker with under 0.5s of audio — too short for a reliable embedding) is matched against enrolled voices and, on a match, the meeting's transcript/summary use that person's real name instead of "Speaker N" (probe: `pipeline::` auto-match block in `process_meeting_inner`, `resolved_names` feeding `labeled_transcript`).
- [x] ISC-131: A real regression test proves the enrollment loop closes end-to-end — a voice enrolled after one meeting (simulating a user labeling action) is auto-recognized by name in a second, separate meeting using the same voice, with a non-empty summary produced from the labeled transcript (probe: `a_voice_enrolled_after_one_meeting_is_auto_recognized_in_the_next`).
- [x] ISC-132: Tauri commands `list_known_speakers` and `label_meeting_speaker` let the UI read enrolled names and label a speaker in a specific meeting; the `remember` flag controls whether that label also captures a new embedding sample for future auto-matching, versus a one-off label that doesn't reinforce the fingerprint (probe: `lib.rs` command handlers, `generate_handler!` registration).
- [x] ISC-133: Meeting Detail's transcript lets the user click any speaker's label to rename it inline, with autocomplete against already-known speaker names (`<datalist>`) and a "Remember" checkbox bound to the `remember` flag above, rather than requiring a separate settings screen (probe: `MeetingDetail.tsx` — `startEditingSpeaker`/`commitSpeakerLabel`, editable `<span>` + inline edit form).
- [x] ISC-134: Reprocessing an already-labeled meeting (`import_legacy_recordings -- reprocess <id>`) clears that meeting's stale `meeting_speaker_labels` rows before the new diarization run, since a new run can assign different speaker indices than the old one — without this, a label could silently attach to the wrong voice after reprocessing (probe: `DELETE FROM meeting_speaker_labels WHERE meeting_id = ?1` added alongside the existing transcript/summary/action-item/embedding cleanup, both in `reprocess` mode and the stale-row crash-recovery cleanup path).
- [x] ISC-135: Anti: auto-matching a speaker during normal processing never adds a new stored embedding sample for that person — only an explicit human label (via `label_meeting_speaker` with `remember: true`) reinforces an enrolled voice's fingerprint, so a wrong auto-match can't silently compound across future meetings (probe: `pipeline.rs`'s auto-match block calls `storage::label_meeting_speaker` but never `add_speaker_embedding_sample`; that call only exists in the `label_meeting_speaker` Tauri command's `remember` branch).

### Segment-Scoped Speaker Correction (2026-08-06, Jeremiah's real bug report — index-wide labeling mislabeled a different person)

- [x] ISC-136: `transcript_segment_speaker_overrides` (keyed by `segment_id`) exists and, when present, takes precedence over the index-wide `meeting_speaker_labels` resolution in `get_meeting_detail` — an explicit per-segment correction always wins over the broader index-wide guess (probe: `segment_override_takes_precedence_over_index_wide_label`).
- [x] ISC-137: Reproduced Jeremiah's exact real bug via direct inspection of the real Smithville call (`import_legacy_recordings -- inspect 3`) before writing any fix — confirmed raw diarization speaker index 0 spans a single unbroken run of segments from 28.5s (Jeremiah, alone) through past 10 minutes (after Dave/Nesta had joined and were speaking), with zero index change at the real turn boundary — i.e. diarization's clustering step, not just a labeling-UI scoping choice, produced this specific failure (probe: real `inspect 3` output, quoted in `## Decisions`).
- [x] ISC-138: A real regression test proves two different real people who diarization merged into the same raw speaker index (one early in a call, one later, no index change at the real turn boundary — the exact shape of ISC-137's evidence) can each be corrected independently via segment-scoped labeling, with neither correction bleeding onto the other person's lines (probe: `segment_override_corrects_a_diarization_merge_without_touching_the_other_speaker`).
- [x] ISC-139: Meeting Detail's speaker-editing UI defaults to labeling only the exact line(s) selected (a single click, or a shift+click range), not every occurrence of that line's raw diarization index across the whole call. Applying a name to every occurrence of an index is available as an explicit, unchecked-by-default checkbox ("Apply everywhere diarization called this Speaker N") for the common case where the index genuinely is one person throughout (probe: `MeetingDetail.tsx` — `editingRange` state, `applyToWholeSpeaker` defaults to `false`, `startEditingSpeaker`'s shift-click range-extension logic).
- [x] ISC-140: Anti: labeling a selected range of transcript lines never mutates a segment outside that range unless `applyToWholeSpeaker` was explicitly checked — the previous index-wide-only design made this impossible to avoid; now it's the default-safe behavior (probe: same regression test as ISC-138 — Jeremiah's two lines stay unlabeled/independently-labelable while only Dave's two lines change).

### Known Speaker Count Override (2026-08-06, Jeremiah's real pushback — "there were only ever 3 speakers on this call")

- [x] ISC-141: `DiarizationEngine::load` accepts an optional `num_speakers` — when `Some(n)`, forces exactly `n` clusters and ignores the similarity threshold entirely. Verified against the real sherpa-onnx C++ header (`fast-clustering-config.h`) before building, not assumed: "If [num_clusters is] greater than 0, then threshold is ignored" (probe: `known_speaker_count_forces_exactly_that_many_clusters`, a real model test forcing `Some(1)` against a single-voice fixture).
- [x] ISC-142: `process_meeting`/`process_meeting_inner` accept an optional `diarization_override` engine. When present, that caller-constructed engine (built with a known count) is used for exactly that one reprocess call instead of the shared, long-lived, unknown-count engine on `PipelineEngines` — the shared engine used by every normal recording is never mutated or replaced (probe: `diarization_override.unwrap_or(&engines.diarization)` in `process_meeting_inner`).
- [x] ISC-143: A Tauri command (`reprocess_meeting_with_speaker_count`) lets the app itself — not just a terminal — reprocess a specific meeting with a known speaker count: clears stale processing data via the shared `clear_meeting_processing_data` helper, resets status to `processing`, and re-runs the full pipeline asynchronously in a background OS thread, matching the existing `stop_recording` pattern (probe: `lib.rs` command + `MeetingDetail.tsx`'s "Fix speaker count" control).
- [x] ISC-144: Reprocessing the real 3-person Smithville call (meeting_id=3) with `num_speakers=3` produces exactly 3 distinct speaker indices, matching the real 3 people — replacing the prior threshold-based result (12 distinct indices for 3 real people). Confirmed the specific reported merge is fixed too: the ~7-minute-mark stretch that was previously one unbroken `Some(0)` run now alternates correctly between `Some(0)`/`Some(1)` turn-by-turn.
- [x] ISC-145: Anti: forcing a known speaker count for one meeting's reprocess never mutates or replaces the shared, long-lived diarization engine used by every other (unknown-count) recording — the override is a caller-local `DiarizationEngine` value, never assigned back into any `PipelineEngines.diarization` field (probe: `reprocess_meeting_with_speaker_count` constructs `fresh_diarization` as a local binding passed only as `Some(&fresh_diarization)`; `grep -n "engines_arc.diarization ="` in `lib.rs` → zero matches).

### Calendar OAuth — Generic Engine + Microsoft Provider (2026-08-06, Jeremiah's next-priority pivot after speaker ID — "focus on the OAuth stuff")

- [x] ISC-146: A generic OAuth 2.0 Authorization Code + PKCE (RFC 7636) engine (`oauth.rs`) is shared by every future calendar provider — PKCE pair generation, authorize-URL building (with a provider-specific `extra_authorize_params` escape hatch, not baked-in Google/Microsoft-specific query params), a real loopback HTTP redirect listener, token exchange, and refresh, all provider-agnostic (probe: 12 real tests — real sockets, real HTTP over loopback, real Keychain round-trips, zero mocks).
- [x] ISC-147: The PKCE `code_verifier`/`code_challenge` pair is spec-correct — verified against RFC 7636 §4.1 directly (43-char, unreserved-alphabet verifier; challenge is the real SHA-256 digest, base64url-no-pad encoded) rather than assumed (probe: `pkce_challenge_is_the_real_sha256_of_the_verifier`, `two_generated_pairs_are_never_the_same`).
- [x] ISC-148: The local redirect listener parses and returns the OAuth `state` parameter (not just `code`), and the caller compares it against the value generated before opening the browser — a forged/replayed redirect with the wrong state is rejected (`OAuthError::StateMismatch`), not silently accepted (probe: `await_authorization_code_receives_a_real_redirect_over_a_real_socket` asserts the real returned state; `calendar::state_mismatch_is_detected_by_a_plain_comparison`).
- [x] ISC-149: Tokens (and the OAuth client ID) are stored via the same cross-platform Keychain/Credential-Manager/Secret-Service primitive already used for the SQLCipher database key (`keychain.rs`), not a second, parallel storage mechanism — reuses the `pub(crate)` `get_secret`/`set_secret` functions directly (probe: `store_and_load_tokens_round_trip_through_real_secure_storage`, `client_id_round_trips_through_real_secure_storage`).
- [x] ISC-150: The Microsoft provider config uses the `common` tenant (not `organizations` or a hardcoded tenant ID) so the app works for any Microsoft account signing in — Jeremiah's own KCG work account, a client's work account, or someone's personal Microsoft account — matching his explicit "other users can log in with their own calendars" requirement. Verified against Microsoft's own current docs before coding, not assumed: `common` is a documented valid `{tenant}` value alongside `organizations`/`consumers`/a tenant ID (`learn.microsoft.com/entra/identity-platform/v2-oauth2-auth-code-flow`).
- [x] ISC-151: The redirect URI uses a fixed local port for clarity, but this isn't load-bearing for correctness — verified against Microsoft's own docs that the port component of a registered `http://localhost` redirect URI is ignored for matching purposes, so Jeremiah registers it once in Azure and the app never needs to coordinate a specific port at runtime (`learn.microsoft.com/entra/identity-platform/reply-url#localhost-exceptions`, quoted in `## Decisions`).
- [x] ISC-152: The requested scope (`Calendars.Read`) requires no admin consent for either work/school or personal Microsoft accounts — verified against the real Microsoft Graph permissions reference before assuming Jeremiah's KCG tenant admin policies wouldn't block it (`learn.microsoft.com/graph/permissions-reference`).
- [x] ISC-153: The Graph `calendarView` event JSON parser's field names (`subject`, `start`/`end.dateTime`, `attendees[].emailAddress.{name,address}`, `onlineMeeting.joinUrl`) were verified against Microsoft's live Event resource documentation before writing the struct, not guessed — a real test parses a response shaped exactly like that documented schema, including the documented null-name/no-online-meeting edge cases (probe: `parses_a_real_graph_shaped_response_into_upcoming_meetings`).
- [x] ISC-154: `get_valid_access_token` transparently refreshes an expiring token (within 60s of `expires_at`) before returning it, so calendar-list callers never see a stale-token failure from normal use; a refresh response that omits `refresh_token` (the documented Microsoft/Google behavior — it doesn't rotate) preserves the original rather than losing it (probe: `refresh_preserves_the_original_refresh_token_when_the_response_omits_it`).
- [x] ISC-155: Tauri commands (`connect_microsoft_calendar`, `is_microsoft_calendar_connected`, `list_upcoming_meetings`) and a minimal "Calendar" tab in the app (paste client ID → Connect → real browser consent → list upcoming meetings with attendees/join link) exist so Jeremiah can drive the whole flow from the app itself once he has a real Azure client ID — not something requiring a terminal (probe: `lib.rs` command handlers + `CalendarSettings.tsx`; `bunx tsc --noEmit` exit 0).
- [x] ISC-156: The end-to-end live flow has been run for real (2026-08-06): Jeremiah created a real Azure App Registration, pasted his real client ID into the Calendar tab, completed real browser consent against Microsoft, and the app listed his real upcoming meetings (two created same-day, one a week out) via the real Graph API. Verified via Keychain timestamps, not just Jeremiah's report: `calendar-client-id:microsoft` and `calendar-tokens:microsoft` entries were both created within this session, 8 seconds apart — client ID stored, then a real token immediately after, the exact sequence of a genuine completed consent flow (not stale test data — see ISC-157 below for why that distinction mattered).
- [x] ISC-157: A stale real Keychain entry (`calendar-tokens:microsoft`, dated ~17:31 UTC the same day, predating the test suite's now-real-provider-avoidance discipline) was found causing `is_microsoft_calendar_connected` to report "Connected" before any real OAuth flow had ever run — a false positive surfaced by Jeremiah noticing the app already said "Connected" on first launch. Root-caused via Keychain item timestamps (not guessed) and deleted (`security delete-generic-password`) before the real first connect. Anti: no client-id entry existed alongside the stale token, confirming it was leftover dev/test residue rather than a real prior connection.
- [x] ISC-158: The upcoming-meetings window is user-adjustable (2/7/14/30 days via a `<select>` in `CalendarSettings.tsx`, default 7) rather than a silent hardcoded 48 hours — found by Jeremiah in the same real session: he had a meeting a week out that the old 48h window hid entirely with no indication anything was cut off. `hoursAhead` is now `windowDays * 24`, driven by the selector; `bunx tsc --noEmit` exit 0.

### AutoJoinRecording — Replaces the disabled `meeting-watcher` cron (2026-08-06, Jeremiah's explicit next pick after CalendarOAuth — "okay lets do that now then")

- [x] ISC-159: The Graph `Event` resource's real `id` field (its stable primary key, documented at `learn.microsoft.com/graph/api/resources/event`) is parsed into `GraphEvent`/`UpcomingMeeting`, not just `subject`/`start`/`end`/`attendees`/`onlineMeeting` — required for idempotent auto-join tracking, since nothing else in the current struct uniquely and stably identifies one calendar occurrence across polls (probe: updated `parses_a_real_graph_shaped_response_into_upcoming_meetings` test asserts a non-empty `id` field).
- [x] ISC-160: A persisted SQLite table (`auto_join_log`: `event_id TEXT PRIMARY KEY`, `triggered_at INTEGER`, `subject TEXT`) records every meeting the poller has already auto-triggered, surviving app restarts — not an in-memory `HashSet` that resets on relaunch (probe: `storage::` migration adds the table; a test inserts a row, closes and reopens the connection, and confirms the row persists).
- [x] ISC-161: The poller checks `auto_join_log` before triggering and skips any `event_id` already present — the same meeting is never opened/recorded twice across repeated polls within its trigger window (probe: unit test runs the poll-decision function twice against the same meeting + a pre-seeded log row; second call returns "skip, already triggered").
- [x] ISC-162: Anti: the poller never triggers for a meeting whose `join_url` is `None` — no attempt to `open::that()` an empty/missing URL (probe: unit test feeds a meeting with `join_url: None` through the trigger-decision function; asserts no open-URL action is returned).
- [x] ISC-163: Anti: the poller never starts a new recording if one is already in progress — it checks `RecordingState` first and skips the auto-start (still opens the join link) rather than silently erroring or double-starting (probe: unit test simulates `RecordingState` already `Some(..)`, confirms the auto-start path returns "already recording, skip" instead of calling `RecordingSession::start`).
- [x] ISC-164: Auto-join/record is OFF by default and requires an explicit opt-in toggle — a fresh install or fresh Microsoft connection never auto-triggers anything until Jeremiah turns it on (probe: new `auto_join_enabled` setting defaults to `false` in the settings table's schema/migration; a test reads a fresh DB and asserts `false`).
- [x] ISC-165: A `set_auto_join_enabled(bool)` / `get_auto_join_enabled()` Tauri command pair persists the toggle to the settings table, and a checkbox control in `CalendarSettings.tsx` ("Auto-join & record calendar meetings") drives it — the setting is visible and changeable from the app itself, not a hidden config flag (probe: `lib.rs` command handlers + `CalendarSettings.tsx` checkbox; `bunx tsc --noEmit` exit 0).
- [x] ISC-166: Anti: toggling `auto_join_enabled` to `false` takes effect on the very next poll cycle (≤ 60s later) — the poller re-reads the setting from storage every cycle rather than caching it once at startup (probe: unit test flips the setting between two calls to the poll-decision function and confirms the second call takes no action).
- [x] ISC-167: The poller runs on a fixed interval (60s) as a background OS thread spawned during Tauri `setup()`, independent of any UI interaction — it keeps running whether or not the Calendar tab is the active view (probe: `lib.rs` setup spawns the thread unconditionally; `ps`/log evidence during manual verification shows poll activity while the Recording tab is active).
- [x] ISC-168: refined (2026-08-06, Advisor VERIFY pass): a meeting is only eligible to trigger within a bounded window — from 60 seconds before its `start` time through its real `end` time (falling back to a fixed 5-minute grace only when `end` can't be parsed) — not the entire multi-day window `list_upcoming_meetings` can return, so a meeting three days out is fetched (for the Calendar tab's list view) without prematurely auto-joining it. Originally specified as a fixed 5-minute post-start grace; widened per Advisor's finding that the manual Start Recording path has no such cutoff at all, so a narrower auto-trigger window would only produce false "why didn't it record?" reports (probe: unit test with a meeting starting in 2 hours confirms "not yet eligible"; one starting in 30 seconds confirms "eligible"; one 10 minutes into its real 1-hour duration confirms still "eligible"; one 70 minutes past its 1-hour meeting's start confirms "window passed, skip"; a meeting with an unparseable `end` still falls back to the fixed 5-minute grace).
- [x] ISC-169: Anti: a Graph API request failure during a poll cycle (network error, expired/revoked token) is caught and logged, not propagated as a panic that would crash the whole app process — the poller thread survives a failed cycle and tries again on the next interval (probe: unit test feeds a `Result::Err` through the poll cycle's error path and confirms the thread-level function returns normally rather than panicking).
- [x] ISC-170: When a meeting triggers, the app opens the real `join_url` via the OS default-handler opener (`open::that`, already a dependency from `connect_microsoft`) — confirmed this does not require any new Zoom/Teams bot API integration, consistent with the Out of Scope guardrail (probe: code review + `Cargo.toml` shows no new bot-API dependency added).
- [x] ISC-171: When a meeting triggers, the app starts local audio capture using the exact same `RecordingSession::start` path `start_recording` already uses — not a second, parallel recording code path — so every recording (manual or auto-triggered) produces an identical on-disk artifact and downstream pipeline behavior (probe: code review confirms the poller's auto-start calls the same `audio_capture::RecordingSession::start` function; no new recording-session type introduced).
- [x] ISC-172: The poller's meeting-fetch step operates against `Vec<UpcomingMeeting>` (the provider-agnostic type already returned by `calendar::list_upcoming_meetings`), not a Microsoft-specific `GraphEvent` — so a future Google/Zoom provider's meetings could be merged into the same poll-and-trigger logic without rewriting the poller (probe: the poll/trigger-decision function's signature takes `&[UpcomingMeeting]`, confirmed via `Read` of the function signature in the new module).
- [x] ISC-172.1: refined (2026-08-06, ApertureOscillation THINK pass): the poll cycle's fetch step calls each registered provider fetcher from a small `Vec` of closures (today: exactly one, wrapping `calendar::list_upcoming_meetings`) and concatenates their results, rather than hardcoding a single direct call to the Microsoft function inline in the poll loop — so adding Google/Zoom later is "register a second closure," not "rewrite the poll cycle" (probe: `Read` of the poll-cycle function shows it iterates a `Vec` of fetcher closures, not a single named function call).
- [x] ISC-173: Anti: the poller does not run at all (not even a no-op tick) when no Microsoft client ID is stored — a user who has never connected a calendar sees zero poller-related activity, not a background thread silently erroring every 60 seconds (probe: unit/code-review confirms the poll cycle's first check is `oauth::load_client_id`, returning early with no error log if `None`).
- [x] ISC-174: The Calendar tab shows the auto-join toggle state and, when at least one meeting has been auto-triggered, a small visible list/log of what was auto-joined and when — satisfying "visible control," not a silent background process Jeremiah has no way to observe (probe: `CalendarSettings.tsx` renders `auto_join_log` entries fetched via a new `list_auto_joined_meetings` command).
- [x] ISC-175: Anti: meetings whose calendar entry has no Graph-native `onlineMeeting.joinUrl` (e.g., a bare Zoom/Meet URL only present in the event body or location text) are not auto-triggered in v1 — no regex/text-parsing of event body or location is added (probe: `grep` for body/location field parsing in the new module returns nothing; `cargo test` includes a case with `join_url: None` and non-empty `subject`/`location`-shaped input confirming no trigger).
- [x] ISC-176: `cargo test --lib` passes 100% after AutoJoinRecording lands, including all new poller/idempotency/toggle tests, with zero regressions in the pre-existing 101 tests (probe: `cargo test --lib` full run, exit 0, N passed / 0 failed where N ≥ 101 + new test count).
- [x] ISC-177: `bunx tsc --noEmit` exits 0 after the `CalendarSettings.tsx` toggle/log UI changes (probe: direct command run, exit code 0).
- [x] ISC-178: Antecedent: the very first time Jeremiah turns the toggle on with a real meeting already on his connected calendar, the app must have already fetched that meeting into its poll cycle before the trigger window opens — the precondition for the euphoric-surprise moment ("it just started recording, I didn't touch anything") is that the poller's fetch cadence (60s) is faster than the trigger window is narrow (60s pre-start), so no real meeting can start and close its window between two consecutive polls undetected (probe: 60s poll interval vs. 60s pre-start window math reviewed in `## Decisions`; a unit test with a meeting starting exactly 61s out at poll time confirms it is still caught on the very next cycle).
- [x] ISC-179: Anti: AutoJoinRecording does not collide with the old, now-disabled `meeting-watcher` Pulse cron job (which used to do calendar-based auto-join + auto-capture via `MeetingWatcher.ts`/`MeetingJoiner.ts`/`MeetingCapture.ts`, screenshot-based, entirely outside this repo) — verified fresh (not from stale memory) during this session's Advisor VERIFY pass: `~/.claude/PAI/Pulse/PULSE.toml`'s `[[job]] name = "meeting-watcher"` entry has `enabled = false`; no crontab or launchd job is running it (`launchctl list | grep meeting` shows only the unrelated read-only `com.pai.meetingportal` viewer, which serves the OLD system's already-captured legacy notes/screenshots from `MEMORY/WORK/meeting-notetaker-agent/` and starts no new captures). MeetingWatcher.ts's own internal kill-switch (`state.enabled` in its own state file) is self-contained to that now-disabled job and is not a shared mechanism this feature needs to also honor.
- [DEFERRED-VERIFY]: ISC-180: the full live end-to-end flow (toggle on, a real Microsoft Graph meeting in hand, the poller actually firing, join link opening, recording actually starting) has NOT been observed firing against a real calendar event yet — Advisor's evidence-standard note ("hold done until you have one log line showing this fired on a real event") is correct that 113 green unit tests is weaker evidence here than usual, and Kai cannot create a real Teams meeting on Jeremiah's calendar (Calendars.Read is read-only by design, ISC-152). Follow-up: next real meeting on Jeremiah's connected calendar with the toggle on, watch for the `auto-join: <subject> — opening join link` / `auto-join: started recording for '<subject>'` println output.
- [x] ISC-181: A recording AutoJoinRecording itself started is automatically stopped once the meeting's real `end` time passes — tracked via a new `AutoRecordingState` marker set only at auto-trigger time and cleared on any stop (manual or automatic), so auto-stop only ever ends a recording it started, never a manually-started one that happens to still be running (Jeremiah's real, hands-on requirement after the "First Meeting" incident: "I need to make sure there IS an auto-stop when the call ends"). The check runs unconditionally at the top of every poll cycle, independent of the enabled toggle or a stored client id, so unticking the box mid-meeting still lets an already-started auto-recording stop cleanly (probe: `auto_join::should_auto_stop_fires_strictly_after_the_meetings_end` — strictly `>`, not `>=`, so a meeting still in its final second is never cut off; code review of `lib.rs::run_auto_join_cycle` and `stop_recording` confirms the marker set/clear sites).
- [DEFERRED-VERIFY]: ISC-181.1: Anti: a recording lost mid-flight to a dev-mode process restart (Tauri's file-watcher rebuilding on every source edit, discarding in-memory, unwritten audio) has NOT been prevented — this is a real, confirmed-this-session gap (the "First Meeting" recording was lost exactly this way) and is explicitly NOT fixed by ISC-181's auto-stop, which only helps once a recording legitimately ends; it does nothing to protect an in-progress recording from Kai's own concurrent code edits. Deliberately left open rather than silently ignored: a real fix (e.g., periodic incremental flush-to-disk instead of write-only-at-stop, or refusing to allow a rebuild while `RecordingState` is `Some`) is a separate, follow-up hardening task, not something to bolt onto today's feature work.

### GoogleCalendarOAuth — Second calendar provider, same generic engine (2026-08-06, Jeremiah's explicit request: "It's up to them. No manual recording ideas... Zoom and Google Meet sign-ins... akin to the Microsoft outlook one")

- [x] ISC-182: A Google Calendar provider (`google.rs` or a `google` section of `calendar.rs`) reuses the existing generic `oauth.rs` engine exactly as the Microsoft provider does — no second PKCE/state-machine implementation. Google's real endpoints verified against Google's own current docs before coding (not assumed): authorize `https://accounts.google.com/o/oauth2/v2/auth`, token `https://oauth2.googleapis.com/token` (`developers.google.com/identity/protocols/oauth2/native-app`).
- [x] ISC-183: For a native/installed-app client, `client_secret` is optional/not applicable per Google's own docs — the Google provider config never sends one, consistent with the existing public-client, PKCE-only pattern (probe: `grep` shows no `client_secret` field populated for the Google provider).
- [x] ISC-184: refined (2026-08-06, Advisor VERIFY pass, tightened before shipping): the requested scope is `https://www.googleapis.com/auth/calendar.events.readonly` — narrower than the originally-built `calendar.readonly`, which also grants calendar metadata/settings access this feature never uses. Verified both scopes' real semantics against `developers.google.com/workspace/calendar/api/auth` before switching (`calendar.readonly`: "See and download any calendar you can access"; `calendar.events.readonly`: "View events on all your calendars" — the feature only ever reads events, so the narrower scope is strictly sufficient). Matches the same least-privilege posture as Microsoft's `Calendars.Read` and Zoom's non-admin `meeting:read:list_meetings`.
- [x] ISC-185: `list_upcoming_meetings`-equivalent for Google calls `GET https://www.googleapis.com/calendar/v3/calendars/primary/events` with `timeMin`/`timeMax` query params (RFC3339, verified against `developers.google.com/calendar/api/v3/reference/events/list` — exclusive bounds on end/start respectively) and parses real field names verified against Google's own Event resource docs: `id`, `summary`, `start.dateTime`/`start.date`, `end.dateTime`/`end.date`, `attendees[].email`/`.displayName`.
- [x] ISC-186: The join link for a Google event resolves from `conferenceData.entryPoints[]` where `entryPointType == "video"` (its `.uri`), falling back to the legacy `hangoutLink` field when `conferenceData` is absent — both real, documented fields, not guessed (probe: a test parses a Google-shaped fixture with `conferenceData` present and a second fixture with only `hangoutLink`, both resolve a join URL).
- [x] ISC-187: An all-day event (Google's `start.date`/`end.date`, no `dateTime`) is recognized and excluded from auto-join eligibility rather than crashing the parser or producing a nonsensical eligibility window (probe: a fixture with `start.date`/no `dateTime` is parsed without panicking and never returns `Trigger`).
- [x] ISC-188: Google's OAuth client ID and tokens are stored via the exact same `oauth::store_client_id`/`store_tokens` Keychain primitives, keyed by a distinct provider id (`"google"`), never colliding with the Microsoft or Zoom provider's stored values (probe: real Keychain round-trip test using provider id `"google"`, distinct account name from `"microsoft"`/`"zoom"`).
- [x] ISC-189: New Tauri commands (`connect_google_calendar`, `is_google_calendar_connected`) and a "Google Calendar" section in the Calendar tab (paste client ID → Connect → real browser consent) exist, mirroring the Microsoft section's structure exactly so the UI stays consistent across providers (probe: `lib.rs` command handlers + `CalendarSettings.tsx`; `bunx tsc --noEmit` exit 0).
- [x] ISC-190: Anti: connecting Google Calendar has no effect on whether Microsoft or Zoom is connected, and vice versa — each provider's connection state is fully independent (probe: connect one provider in a test/fixture context, confirm `is_microsoft_calendar_connected`/`is_google_calendar_connected`/`is_zoom_connected` are each independently `true`/`false` per their own stored tokens).

### ZoomOAuth — Third calendar/meetings provider, same generic engine (2026-08-06)

- [x] ISC-191: A Zoom provider reuses the existing generic `oauth.rs` engine. Real endpoints verified against Zoom's own current docs: authorize `https://zoom.us/oauth/authorize`, token `https://zoom.us/oauth/token` (`developers.zoom.us/docs/integrations/oauth/`).
- [x] ISC-192: refined (2026-08-06, corrects an assumption logged 2026-08-06 in CalendarOAuth's own Decisions that "Zoom's flow needs a client secret even for PKCE-only apps"): re-verified against Zoom's own docs and found that's wrong for the right app type — Zoom offers a distinct "public client" app registration type specifically for PKCE without a client secret ("Zoom offers a separate public client ID in the authorization request that doesn't require an associated client secret," `developers.zoom.us/docs/integrations/oauth/`). Jeremiah must register the Zoom app as this public-client type (documented in the app's setup instructions, mirroring the Azure App Registration walkthrough already given for Microsoft) — the code sends no `client_secret`, matching Google and Microsoft.
- [x] ISC-193: The requested scope is `meeting:read:list_meetings` (the non-admin granular scope — verified against `developers.zoom.us/docs/integrations/oauth-scopes-granular/`), not the `:admin` variant, matching least-privilege for a single user's own meetings.
- [x] ISC-194: `list_upcoming_meetings`-equivalent for Zoom calls `GET https://api.zoom.us/v2/users/me/meetings` and parses real field names verified against Zoom's documented response shape: `id`, `topic`, `start_time`, `duration` (minutes), `timezone`, `join_url`.
- [x] ISC-195: Zoom's response has no direct `end` field — `end` is computed as `start_time + duration minutes` before constructing an `UpcomingMeeting`, so the shared `auto_join::eligibility()` window logic (which reads `meeting.end`) works identically for Zoom without any Zoom-specific branch in `auto_join.rs` itself (probe: a test with `start_time` + `duration: 30` confirms the constructed `UpcomingMeeting.end` is exactly 30 minutes after start).
- [x] ISC-196: Zoom's OAuth client ID and tokens are stored under provider id `"zoom"`, independent of `"microsoft"`/`"google"` (same probe pattern as ISC-188/190).
- [x] ISC-197: New Tauri commands (`connect_zoom`, `is_zoom_connected`) and a "Zoom" section in the Calendar tab exist, mirroring the Microsoft/Google sections (probe: `lib.rs` + `CalendarSettings.tsx`; `bunx tsc --noEmit` exit 0).
- [x] ISC-198: The AutoJoinRecording poller's fetcher list is built dynamically from whichever providers currently have both a stored client id AND stored tokens — not a fixed one-Microsoft-fetcher list — so Jeremiah connecting "only Outlook and Zoom," "just Google Meet," or "all three" each produce exactly the right set of active fetchers with zero code change per combination (probe: a test with 0/1/2/3 "connected" fixtures confirms `collect_meetings` draws from exactly the connected set — this is the real-world exercise of the provider-agnostic fetcher architecture from ISC-172/172.1).
- [x] ISC-199: Anti: a provider with a stored client id but no stored tokens (started connecting, never finished consent) is not included as an active fetcher — only genuinely connected providers (both client id AND tokens present) contribute to the poll cycle.

### SilenceBasedStopPrompt — Layered on top of ISC-181's calendar-end auto-stop (2026-08-06, Jeremiah's explicit requirement, second-layer safety net after the "First Meeting" incident)

- [x] ISC-200: `RecordingSession` gains a new method exposing a real, computed signal from the actual in-memory capture buffer — RMS (root-mean-square) energy over the trailing N seconds of already-captured audio — without disturbing the live capture stream (probe: a test feeds a known sine-wave/silence buffer and confirms the computed RMS matches within tolerance for both a loud and a silent segment).
- [x] ISC-201: A new, separate async interval loop (finer-grained than the 60s calendar-poll cycle — real silence detection needs closer to a 5-10s check to accurately measure a continuous 60-second silence window) monitors RMS only while `AutoRecordingState` holds a marker (i.e., only for a recording AutoJoinRecording itself started — Jeremiah's explicit scope cut: "No manual recording ideas for now on this functionality request").
- [x] ISC-202: refined (2026-08-06, Engineer's real finding, flagged not silently shipped): when RMS stays below a defined silence threshold for a continuous 60 real seconds, the app shows a native OS-level dialog (`tauri-plugin-dialog`'s `DialogExt`, `MessageDialogButtons::OkCancelCustom("Stop", "Continue")`, non-blocking `.show()` with a callback) asking whether to stop or continue recording — verified against Tauri's own current plugin docs before coding (`v2.tauri.app/plugin/dialog/`), not guessed at. Effective real-world latency is ~120s, not 60s: each RMS reading is itself computed over a trailing 60-second window (ISC-200), and the prompt additionally requires 60 seconds of consecutive sub-threshold readings on top of that — the two 60s windows compound rather than overlap. This is the safe-by-construction direction (over-waiting costs nothing; under-waiting risks interrupting a live conversation), but is a real deviation from a literal "after 60 seconds" reading of the request, surfaced to Jeremiah for his call rather than assumed acceptable. A true 60s-total latency would require shortening the RMS window itself, not the consecutive-readings threshold.
- [x] ISC-203: Choosing "Stop" in the dialog calls the same `stop_recording` function every other stop path uses — no separate stop mechanism (probe: code review, single call site).
- [x] ISC-204: Choosing "Continue" resets the silence timer and suppresses re-prompting until another full continuous 60-second silence window is newly observed — it does not immediately re-prompt on the very next check (probe: unit test simulates Continue, confirms the next few checks don't re-trigger until 60 fresh seconds of silence accumulate).
- [x] ISC-205: Anti: the silence-based popup NEVER fires for a manually-started recording (one with no `AutoRecordingState` marker) — matching Jeremiah's explicit "No manual recording ideas for now" scope cut (probe: unit test with `RecordingState` `Some` but `AutoRecordingState` `None` confirms the monitor loop takes no action).
- [x] ISC-206: Anti: the calendar-end-time auto-stop (ISC-181, a confident/direct signal) is NOT replaced or weakened by this feature — it still silently stops without a prompt when the scheduled end time genuinely passes; the silence-based popup is a second, independent, lower-confidence layer for cases where the real call ends earlier or later than the calendar said (probe: code review confirms `run_auto_join_cycle`'s existing auto-stop check and the new silence-monitor loop are two separate code paths, neither gating the other).
- [x] ISC-207: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after GoogleCalendarOAuth + ZoomOAuth + SilenceBasedStopPrompt land, with zero regressions in the pre-existing suite.
- [DEFERRED-VERIFY]: ISC-208: whether a native dialog from `tauri-plugin-dialog` actually surfaces/brings focus when the kai-notetaker window is backgrounded during a real Teams/Zoom/Meet call (the realistic case — Jeremiah is looking at the call, not this app) is unconfirmed by Tauri's own docs and needs a real, hands-on check, not an assumption. Follow-up: verify live once a real auto-triggered recording reaches 60s of real silence with the app backgrounded.

### RecordingDurability — Closes ISC-181.1 (2026-08-06, Jeremiah's explicit next pick after AutoJoinRecording: "do the dev-mode recording-loss fix next")

- [x] ISC-209: `RecordingSession::start()` opens a real `hound::WavWriter` at the target path immediately — not lazily at `stop_and_write()` — so a valid (if empty/near-empty) WAV file exists on disk from the first moment of recording, not only after a clean stop (probe: a test opens a session, sleeps briefly, and confirms the file already exists and is a valid, zero-or-near-zero-duration WAV *before* `stop_and_write` is ever called).
- [x] ISC-210: A separate, dedicated background thread (never the real-time `cpal` audio callback — ISC-211 is the anti-criterion guarding this) periodically drains newly-captured samples from the existing `Arc<Mutex<Vec<f32>>>` buffer (tracking its own "already written" cursor, never truncating or mutating the buffer itself, so `trailing_rms`'s silence detection — ISC-200 — keeps reading the full history unaffected) and writes them into the WAV writer via `write_sample`, then calls `hound::WavWriter::flush()` — verified against the installed `hound` 3.5.1 crate's own source and doc comment before writing any code, not assumed: `flush()` patches the RIFF/data-chunk header sizes and flushes the OS-level buffer, and per its own documented guarantee, "even if after the flush there is an IO error or the writing process dies, the file can still be read by a compliant decoder up to the last flush." This is the real, load-bearing durability primitive the whole feature rests on.
- [x] ISC-211: Anti: no disk I/O (file writes, `flush()` calls) ever happens on the real-time `cpal` audio callback thread — it continues to only append to the in-memory buffer, exactly as before this feature, preserving the existing glitch-free real-time capture guarantee (probe: `grep` review of `build_capture_stream`'s callback closures confirms no `WavWriter`/file-handle reference is captured or touched there; only the flusher thread, running on `std::thread::spawn`, ever touches the writer).
- [x] ISC-212: The flush interval is a named, documented constant (`FLUSH_INTERVAL_SECS`) rather than a magic number buried in a sleep call, set to a value that bounds worst-case audio loss on an ungraceful death to that many seconds — not the whole recording (probe: `grep -n "FLUSH_INTERVAL_SECS"` shows one `pub const` definition, referenced by both the flusher loop and its own doc comment explaining the loss-bound tradeoff).
- [x] ISC-213: `stop_and_write()` keeps its exact existing public signature and return type (`fn stop_and_write(mut self) -> Result<PathBuf, AudioCaptureError>`) — callers in `lib.rs` (the `stop_recording` Tauri command, the calendar-end auto-stop path, the silence-prompt's Stop path) require zero changes (probe: `cargo build` after the change touches zero call sites of `stop_and_write` outside `audio_capture.rs` itself).
- [x] ISC-214: `stop_and_write()` signals the flusher thread to stop (an `Arc<AtomicBool>` flag, checked on a short poll tick — see ISC-215), waits for it to perform one final drain-and-flush of any samples captured since its last checkpoint, then calls `finalize()` on the writer and joins the thread — so a *clean* stop still produces a byte-for-byte complete, correctly-finalized WAV, identical in every observable way to what `stop_and_write` produced before this feature existed (probe: `stopped_recording_always_declares_canonical_sample_rate` and the existing `recording_path_is_inside_provided_data_dir_never_system_temp` tests both still pass unmodified against the new implementation — zero regression in the clean-stop path).
- [x] ISC-215: The flusher thread's poll tick (how often it checks whether a stop has been requested) is short enough that clicking Stop Recording feels responsive — not gated behind waiting for a full `FLUSH_INTERVAL_SECS` to elapse — while the actual disk-flush cadence stays at the full interval during normal recording (probe: a test starts a session, waits less than one full flush interval, requests stop, and asserts `stop_and_write` returns well under one `FLUSH_INTERVAL_SECS`, proving stop isn't blocked on the next scheduled flush).
- [x] ISC-216: Anti: a recording that crashes (process killed, not a clean stop) within the very first `FLUSH_INTERVAL_SECS` may still lose that initial window entirely if zero flushes ever happened — this is a real, honest, named bound (the best achievable given the hard constraint that data can't be made durable before it physically exists), not silently presented as "recordings can never be lost" (probe: this exact bound is stated in a doc comment on `FLUSH_INTERVAL_SECS` and in this ISA's `## Out of Scope` addendum below, not just implied).
- [x] ISC-217: At app startup (`lib.rs::run()`'s `.setup()`), a one-time scan of `recordings/` identifies any `.wav` file matching the app's own auto-generated filename pattern (`^\d{8}T\d{6}\.wav$`, e.g. `20260806T165724.wav`) that has NO matching row in `meetings.audio_path` — a crash-orphaned recording from a previous run, since this app is single-instance and no `RecordingSession` can exist at the moment of a fresh launch (probe: unit/integration test seeds a real SQLite DB with zero matching `meetings` rows, drops a same-pattern `.wav` file into a temp `recordings/` dir, and confirms the scan identifies exactly that file as orphaned).
- [x] ISC-218: Anti: the startup scan NEVER touches `imported-*`-prefixed files — those belong exclusively to the separate, deliberate `import_legacy_recordings` example binary's own workflow, and auto-recovering them at every launch would risk double-importing or fighting that tool's own logic (probe: a test seeds both an orphaned auto-generated file and an `imported-*` file with no matching `meetings` row; the scan recovers only the former).
- [x] ISC-219: Anti: a `.wav` file that DOES have a matching `meetings.audio_path` row is never re-imported or double-processed by the startup scan, regardless of how many times the app restarts (probe: a test seeds a `meetings` row whose `audio_path` matches an on-disk file; the scan recovers zero files from that fixture).
- [x] ISC-220: A recovered orphaned recording's real duration is read directly from its own (validly flushed, per ISC-210) WAV header via `hound::WavReader`, not guessed or hardcoded to zero, before being passed into `storage::create_meeting` — so the recovered meeting's metadata is honest (probe: a test recovers a fixture `.wav` with a known real duration and confirms the created `meetings` row's `duration_secs` matches). Refined (2026-08-06, Engineer's real design decision, disclosed and adopted): a recovered file whose real duration reads as zero (a crash inside its very first checkpoint window, the exact ISC-216 bound) is deliberately NOT turned into a `meetings` row — creating one would produce a permanently-failed, nothing-to-transcribe library entry rather than a genuinely useful recovery. `recover_orphan_into_db` returns `Ok(None)` for this case, logged, not silently dropped (probe: `a_zero_duration_orphan_is_skipped_not_turned_into_a_broken_meeting`).
- [x] ISC-221: A recovered orphaned recording is processed through the exact same `pipeline::process_meeting` background-thread call `stop_recording`'s Tauri command already uses — no separate, parallel "recovery pipeline" is invented (probe: `grep` confirms the startup-scan code path and `stop_recording`'s existing path both call `pipeline::process_meeting` with the same signature, not two different functions).
- [x] ISC-222: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after RecordingDurability lands, with zero regressions in the pre-existing suite (baseline going in: 140 passed).
- [x] ISC-223: A write/flush/finalize failure inside the flusher thread (e.g. disk full) is logged the moment it happens (`eprintln!` inside the loop, before the thread exits), not only surfaced later when `stop_and_write` eventually joins the thread — a real gap found and fixed by Kai during VERIFY (Advisor unavailable, verified against the code directly instead): without this, a disk-full event mid-recording silently ends durability checkpointing with zero visible signal for as long as the meeting continues, degrading this feature's whole guarantee unnoticed (probe: `Read` of `spawn_flusher` confirms `eprintln!` fires on all three failure paths — `write_sample`, `flush`, `finalize` — before returning `Err`; `cargo test --lib` — 151/151 still passing after the change, zero regression).

### TeamsPresenceAdhocRecording — Detects unscheduled Teams calls (2026-08-07, Jeremiah's real question: "If I opened up a meeting, or started one fresh without it being scheduled, will that initiate a recording?")

- [x] ISC-224: A Microsoft Graph presence poll (`GET /me/presence`) is added to the existing Microsoft OAuth connection's capability set — real endpoint and field verified against Microsoft's own current docs before coding: `activity` is the field carrying the granular state (confirmed against the real example JSON payloads in `learn.microsoft.com/graph/api/presence-get`, which pair `"availability": "DoNotDisturb"` with `"activity": "Presenting"` — the richer state lives in `activity`, not `availability`, correcting a scrambled table on the resource-reference page that had the two swapped). Possible `activity` values relevant here: `inACall`, `inAMeeting` (both confirmed real via `learn.microsoft.com/graph/api/resources/presence`).
- [x] ISC-225: The requested scope is `Presence.Read` — least privilege, delegated, work/school account (verified: `Presence.Read.All` and application-only variants exist but are NOT used; `/me/presence` is unsupported for personal Microsoft accounts, a real documented limitation, not a bug if a future personal-account user hits it).
- [x] ISC-226: Adding `Presence.Read` to the Microsoft OAuth scope string requires Jeremiah to re-run Connect once — the previously-issued token does not retroactively carry a new scope. This is surfaced to him directly (UI hint text or a one-time notice), not silently assumed already covered (probe: `CalendarSettings.tsx`'s Microsoft section shows a hint referencing the added scope when reconnection is needed).
- [DEFERRED-VERIFY]: ISC-227: Anti: clicking Teams "Meet Now" has NOT yet been verified empirically against Jeremiah's real Teams client to confirm which real `activity` value it actually produces (`inACall`, `inAMeeting`, or both across the join lifecycle) — code is built and tested (`classify_activity`, ISC-228), but a real live click hasn't happened yet. The code treats both values as equivalent "in a call" signals specifically so this empirical uncertainty doesn't block shipping or risk being wrong about which single value to key on. Follow-up: Jeremiah clicks Meet Now with the app running, watches for the presence poller's own log output confirming a real `activity` value and a recording auto-start.
- [x] ISC-228: A pure, unit-testable function (mirroring `auto_join::eligibility`'s pure-core pattern) classifies a raw presence `activity` string as `InCall` (matches `inACall` or `inAMeeting`) or `NotInCall` (anything else, including unrecognized/future values — fails closed, never assumes a new value means "in a call") — probe: unit tests covering both real values, a real non-call value (`available`), and an unrecognized string.
- [x] ISC-229: A new, separate async interval loop (distinct from the 60s calendar-poll and the 5-10s silence-monitor loops) polls presence on a shorter cadence than the calendar poll — ad-hoc start latency matters more than scheduled-meeting latency, since Jeremiah is already mid-action (clicking Meet Now) when it should fire (probe: a named constant, e.g. `PRESENCE_POLL_INTERVAL_SECS`, documented with the latency tradeoff, distinct from `POLL_INTERVAL_SECS`/silence-monitor's own interval).
- [x] ISC-230: Anti: the presence poll only ever runs when Microsoft is connected with BOTH a stored client id and stored tokens (same connected-provider pattern as `auto_join::ProviderConnection::is_active`) — a disconnected or partially-connected Microsoft provider produces zero presence-poll activity (probe: unit test mirrors `a_client_id_without_tokens_is_not_an_active_provider`'s pattern for the presence path specifically).
- [x] ISC-231: On presence classifying as `InCall` with `RecordingState` currently `None` (nothing already recording, from any trigger), a new recording is silently auto-started via the exact same `start_recording` function every other trigger path already calls — no separate recording-start code path (probe: `grep` confirms one call site pattern, consistent with ISC-171's established precedent).
- [x] ISC-232: Anti: on presence classifying as `InCall` with `RecordingState` already `Some` (a manual recording, a calendar-triggered one, or an earlier presence-triggered one already running), no second recording is started — presence-triggered start is idempotent against any already-active recording, regardless of what triggered it (probe: unit test with `already_recording: true` confirms no start action).
- [x] ISC-233: A presence-triggered recording sets a distinct marker (extending `AutoRecordingState`/`AutoRecordingMarker` with an enum distinguishing `CalendarEnd(DateTime<Utc>)` from `PresenceBased` — no calendar end-time exists for an ad-hoc call, so the existing single-`DateTime`-field marker shape from ISC-181 cannot represent this case as-is) so the auto-stop logic (ISC-234) knows which stop signal applies to which active recording (probe: `Read` of the extended marker type confirms both variants are representable; existing ISC-181 calendar-triggered tests still pass unmodified against the `CalendarEnd` variant).
- [x] ISC-234: On presence classifying as `NotInCall` while a `PresenceBased`-marked recording is active, the recording is auto-stopped via the exact same `stop_recording` path every other stop trigger uses — the same poll signal that starts a presence-triggered recording also stops it, verified via this session's own FirstPrinciples pass as the correct mechanism (no calendar end-time needed; presence itself is bidirectional) — probe: unit test transitions `InCall` → `NotInCall` with an active `PresenceBased` marker, confirms `stop_recording` is invoked; a parallel test confirms a `CalendarEnd`-marked recording is UNAFFECTED by a presence transition (the two stop mechanisms are independent, never cross-triggering).
- [x] ISC-235: Anti: SilenceBasedStopPrompt (ISC-200–208) continues to run, unmodified, underneath any `AutoRecordingState`-marked recording regardless of which variant marked it — the fallback safety net composes with both `CalendarEnd` and `PresenceBased` triggers, not just the original calendar case (probe: code review confirms the silence-monitor's marker check is `guard.is_some()`, not variant-specific).
- [x] ISC-236: Anti: this feature is explicitly Microsoft/Teams-only — no equivalent presence-style detection is built for Zoom or Google Meet in this pass, per Jeremiah's own explicit scope cut ("But for now, one at a time... I'll let you know" re: researching Zoom/Meet equivalents separately) — probe: `grep` confirms no presence-polling code exists in `zoom.rs`/`google.rs`.
- [x] ISC-237: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after TeamsPresenceAdhocRecording lands, with zero regressions in the pre-existing suite (baseline going in: 151 passed).

### RecordingIndicatorOverlay — Persistent visible signal recording is active, any trigger (2026-08-07, Jeremiah's explicit condition for accepting silent auto-start: "I want there to be some sort of small overlay... showing while recording")

- [x] ISC-238: A second Tauri window (distinct from the main app window) is created via `WebviewWindowBuilder` — real, verified Rust API (`docs.rs/tauri/latest/tauri/webview/struct.WebviewWindowBuilder.html`), not guessed: `.always_on_top(true)`, `.decorations(false)`, `.inner_size(w, h)` (small, fixed), `.position(x, y)` (a screen corner — top-right, matching the macOS/Zoom/Teams convention for this kind of indicator), `.resizable(false)`.
- [x] ISC-239: The overlay window is created once, hidden, at app startup (not re-created per recording start/stop) — shown/hidden via `.show()`/`.hide()` on state transitions, avoiding window-creation overhead and flicker on every recording (probe: `Read` of `.setup()` confirms one creation call; recording start/stop paths call `.show()`/`.hide()`, never `WebviewWindowBuilder::new` again after startup).
- [x] ISC-240: The overlay's show/hide is wired into ONE centralized code path that every recording-start/stop trigger already funnels through (`start_recording`/`stop_recording`, per ISC-171/ISC-203's established "one real call site" precedent) — not duplicated separately at each of the three (now four, including presence) trigger sites, so a future fourth trigger automatically gets the indicator for free (probe: `grep -rn "webview.*show\|webview.*hide"` or equivalent shows exactly one call site each for show and hide, inside `start_recording`/`stop_recording` themselves or a shared helper both call).
- [x] ISC-241: The overlay renders a live "Recording" label with a visual indicator (e.g. a dot) and an elapsed timer, driven by a new lightweight Tauri command (e.g. `recording_status() -> Option<{started_at, ...}>`) polled from the overlay's own small frontend view — reusing `RecordingControl.tsx`'s existing elapsed-timer pattern (`setInterval` + `formatElapsed`) rather than inventing a second timer mechanism (probe: `Read` confirms the overlay's frontend component reuses the same formatting function or an equivalent, not a duplicated one).
- [x] ISC-242: The overlay's frontend view is distinguished from the main app view by the window's own label (`getCurrentWindow().label`), rendering a minimal dedicated component instead of the full `App` — no second, separate frontend bundle/entry point is built (probe: `Read` of the frontend router/entry confirms a label check gates which component renders).
- [x] ISC-243: Anti: the overlay never appears when nothing is recording — it starts hidden and only shows for the duration of an active recording, regardless of trigger (probe: a fresh app launch with no recording shows no overlay; a test/manual check confirms it appears within one state-check cycle of any of the three start triggers and disappears within one cycle of any stop trigger).
- [x] ISC-244: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after RecordingIndicatorOverlay lands, with zero regressions in the pre-existing suite (baseline going in: 151 passed, plus whatever TeamsPresenceAdhocRecording adds).

### RecordingTriggerProvenance — Records HOW each meeting was captured (2026-08-07, Jeremiah's explicit request: "have the way it was recorded listed... so as not to confuse or just be certain of how it was recorded for analysis moving forward")

- [x] ISC-245: A new `trigger_source` column is added to the `meetings` table via the existing `apply_column_migrations` pattern (`ALTER TABLE meetings ADD COLUMN trigger_source TEXT` — nullable, so pre-existing rows from before this feature are not broken by a `NOT NULL` constraint they can't satisfy retroactively) — probe: `grep -n "trigger_source" storage.rs` shows the migration; a test opens a DB created before this migration existed (or simulates one) and confirms `ensure_schema` adds the column without erroring on existing rows.
- [x] ISC-246: `storage::create_meeting` takes an explicit trigger-source value rather than defaulting silently — every caller must state how the recording it's persisting actually started (probe: `grep -n "fn create_meeting"` shows the new parameter; `cargo build` fails if any caller omits it, since there is no default argument in Rust).
- [x] ISC-247: The real, distinct trigger values are: `manual` (the Start Recording button), `calendar` (AutoJoinRecording, any of the three providers), `presence` (TeamsPresenceAdhocRecording), and `recovered` (crash-orphan recovery, ISC-217-221) — `recovered` is deliberately its own honest value, not a guess at which of the other three it originally was, since that information did not survive the crash (probe: an enum, not a bare `String`, so an invalid/typo'd value is a compile error, not a silent bad row — e.g. `pub enum TriggerSource { Manual, Calendar, Presence, Recovered }` with a `Display`/`FromStr` or serde mapping to the exact four lowercase strings above).
- [x] ISC-248: `start_recording`'s Tauri command gains a way to know its own trigger source and thread it through to the eventual `create_meeting` call at stop time — since a recording's trigger is only known at START but `create_meeting` only runs at STOP, the value must be carried in `RecordingState` itself (extending the existing `Mutex<Option<(RecordingSession, Instant)>>` tuple with a third element, or an equivalent small struct) for the whole lifetime of the in-progress recording (probe: `Read` of `RecordingState`'s definition confirms the extended shape; `stop_recording` reads the stored trigger source, does not re-derive or guess it).
- [x] ISC-249: The manually-clicked Start Recording button continues to work with zero frontend changes — `RecordingControl.tsx`'s existing `invoke("start_recording")` call (no arguments) must still compile and behave identically, defaulting to `TriggerSource::Manual` when the trigger argument is omitted (probe: `Read` of `RecordingControl.tsx` shows no diff was needed; `bunx tsc --noEmit` exit 0).
- [x] ISC-250: The three internal (non-IPC) callers of `start_recording` — the calendar-triggered auto-start (`run_auto_join_cycle`), the presence-triggered auto-start (`run_presence_cycle`), and any future trigger — explicitly pass `TriggerSource::Calendar`/`TriggerSource::Presence` respectively; no internal caller is allowed to fall back to the `Manual` default (probe: `grep -n "start_recording("` at each of the three call sites confirms an explicit, non-default trigger argument at the two auto-trigger sites).
- [x] ISC-251: The startup orphan-recovery path (`recover_orphan_into_db`) passes `TriggerSource::Recovered` to `create_meeting` — not a guess, not the default (probe: `grep` confirms the literal `Recovered` value at that specific call site).
- [x] ISC-252: `get_meeting_detail` (or whichever query backs `MeetingDetail.tsx`) returns the meeting's `trigger_source` as part of its response payload, and `MeetingDetail.tsx` displays it as a small, clearly-labeled indicator near the meeting's title (e.g. "Recorded: Manual" / "Recorded: Calendar (auto-join)" / "Recorded: Ad-hoc Teams call" / "Recorded: Recovered after a crash") — so Jeremiah can look at any past meeting and know with certainty how it was captured, without needing to cross-reference logs (probe: `Read` of `MeetingDetail.tsx` confirms the field is rendered; `bunx tsc --noEmit` exit 0).
- [x] ISC-253: `MeetingLibrary.tsx`'s list view also shows a compact per-row indicator of trigger source (e.g. a small icon or short tag next to the existing status badge) — the same certainty at a glance while browsing, not only after opening a specific meeting (probe: `Read` confirms the field is rendered in the list row).
- [x] ISC-254: Anti: the trigger-source label is displayed as UI metadata alongside the title/summary — it is NOT injected into the LLM-generated summary text itself. Baking provenance into model-generated prose would make it fragile (dependent on the summarization prompt/model behavior) and would mix operational metadata into content meant for a different purpose (probe: `grep -n "trigger_source\|TriggerSource"` in `summarization.rs`/`llm.rs`/wherever the summarization prompt is built returns zero matches — the summarization pipeline is untouched by this feature).
- [x] ISC-255: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after RecordingTriggerProvenance lands, with zero regressions in the pre-existing suite (baseline going in: 171 passed).
- [x] ISC-256: A real Disconnect button exists in the Calendar tab for each of the three providers — closing a genuine gap found by Jeremiah when he had no way to force a re-consent for the new `Presence.Read` scope besides Kai manually deleting a Keychain entry for him. `disconnect_provider(provider)` forgets only the stored tokens, not the client ID, so reconnecting is just the browser consent flow again, not re-pasting a client ID that hasn't changed. Required promoting two previously-`#[cfg(test)]`-gated functions (`keychain::delete_secret`, and a new real `oauth::delete_tokens` built on it) to real production code, since no non-test delete path existed at all before this (probe: `cargo build --lib` — the `#[cfg(test)]` removal itself is what made this compile for a production Tauri command in the first place; `Read` of `CalendarSettings.tsx` confirms the button and optimistic-revert-on-failure pattern, matching the existing `handleToggleAutoJoin` convention already in that file).
- [x] ISC-257: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after the Disconnect button lands, with zero regressions (baseline going in: 175 passed).

### MeetingTitleStandardization — Real calendar titles carry through; deterministic naming for everything else (2026-08-07, Jeremiah: "that's a real annoyance for me when I see the titles are randomly generated and don't have anything to do with the call")

- [x] ISC-258: `storage::create_meeting` gains a new parameter, `known_title: Option<&str>`. When `Some(title)` is passed (the calendar case), the INSERT sets `meetings.title` to that exact real subject. When `None` is passed, `create_meeting` computes a deterministic fallback title ITSELF, internally, from the `trigger_source` it already receives plus the `created_at` timestamp it's about to insert — format `"{Label} — {formatted timestamp}"` (`"Recording"` for Manual, `"Ad Hoc Call"` for Presence, `"Recovered Recording"` for Recovered). Either way, **`meetings.title` is never `NULL` after creation** — the fallback-naming logic lives in exactly one place (`create_meeting` itself), not duplicated at every call site (probe: `Read` of `create_meeting` confirms both branches; a test for each of the four trigger sources — calendar with a known title, and the three fallback cases — confirms `get_meeting_detail`/`list_meetings` return a real, non-null, immediately-correct title right after `create_meeting` returns, before any pipeline processing runs).
- [x] ISC-259: `storage::mark_meeting_ready` no longer touches `title` at all — since ISC-258 guarantees every meeting already has a real title from the moment it's created, the post-processing naming step (previously deriving a title from the LLM summary) is removed entirely, not just made conditional. `mark_meeting_ready`'s signature drops its `title` parameter; it only updates `status` (probe: `Read` confirms the `UPDATE meetings SET status = 'ready' WHERE id = ?1` no longer references `title`; `grep -n "meeting_summary"` in `pipeline.rs` confirms the summary text is no longer read to derive a title anywhere).
- [x] ISC-260: `run_auto_join_cycle`'s calendar-triggered `start_recording` call site passes the real, already-available `meeting.subject` through — extending `start_recording`'s signature (and `ActiveRecording`'s carried state, alongside `trigger_source`, mirroring the exact pattern that already carries `TriggerSource` from start-time to the eventual `create_meeting` call at stop-time) with an `Option<String>` known title (probe: `Read` of `run_auto_join_cycle` confirms `Some(meeting.subject.clone())` is passed at the calendar trigger site; `Read` of `ActiveRecording` confirms the new field; `stop_recording` reads it back out and passes it to `create_meeting`).
- [x] ISC-261: Anti: the presence-triggered call site (ad-hoc "Meet Now" detection) passes `known_title: None` explicitly — no attempt is made to fetch a Graph `onlineMeeting` object for an ad-hoc call to look for a subject, since ad-hoc meetings typically have no meaningful custom title and the added Graph round-trip/complexity isn't justified by what it would likely return (probe: `grep` confirms the presence-triggered call site passes `None`, not a fetch attempt).
- [x] ISC-262: The manual Start Recording button's existing no-argument `invoke("start_recording")` call needs zero frontend changes — a new optional backend parameter with no caller-supplied value defaults to `None`, matching the exact precedent already established for `trigger_source` (probe: `git diff` on `RecordingControl.tsx` is empty).
- [x] ISC-263: The deterministic fallback format (specified in ISC-258, implemented as a small pure function inside `storage.rs` — e.g. `fn fallback_title(trigger_source: TriggerSource, created_at: &str) -> String` — unit-testable in isolation from any DB/pipeline concern) covers exactly the three non-calendar trigger sources: `"Recording — {formatted timestamp}"` for Manual, `"Ad Hoc Call — {formatted timestamp}"` for Presence, `"Recovered Recording — {formatted timestamp}"` for Recovered, with a human-readable timestamp format (e.g. `"Aug 7, 2026 2:34 PM"`) — deterministic, computed instantly from data already on hand, never dependent on LLM output. Titles render in the SYSTEM'S LOCAL time (refined by Kai after the Engineer's draft landed in UTC — see Decisions) so a meeting's own title agrees with the library's local-time date column instead of contradicting it (probe: `cargo test --lib storage::tests` — one test per trigger source asserting the expected string, computed via an independent local-time conversion, given a fixed input timestamp).
- [x] ISC-264: Anti: manual renaming (`rename_meeting`, already built) is completely unaffected by this feature — a user can still rename any meeting, calendar-titled or fallback-named, at any time (probe: `git diff` on the `rename_meeting` Tauri command and its `storage::rename_meeting` function is empty).
- [x] ISC-265: `MeetingLibrary.tsx`'s existing `"Processing…"` fallback (shown when `title` is `null`) now only appears for the brief window between a meeting's row being created and either its known title or its deterministic fallback title being set — which, per ISC-258, is immediate/synchronous with creation for ALL trigger sources now (calendar gets the real subject, everything else gets the deterministic fallback) — so `"Processing…"` should in practice never be visibly seen again once this ships (probe: code review confirms every `create_meeting` call site now always supplies a real, immediately-displayable title — either the known calendar subject or the computed deterministic fallback — never leaving it to the async pipeline alone; `mark_meeting_processing` no longer clears `title` on reprocess, closing a latent regression the original spec would have shipped — see Decisions).
- [x] ISC-266: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after MeetingTitleStandardization lands, with zero regressions (baseline going in: 175 passed; final: 187 passed).

### QuitCrashRace — Fixes a real SIGABRT on normal app quit (2026-08-07, Jeremiah's own crash report immediately after the first real `tauri build` install — "Terminating Process: kai-notetaker... Exception Type: EXC_CRASH (SIGABRT)... ggml_metal_rsets_free... ggml_abort")

- [x] ISC-267: `spawn_engine_loading`'s background OS thread (which constructs the Metal-backed Whisper/LLM/embedding contexts at startup) has its `JoinHandle` captured into a new Tauri-managed `EngineLoadHandle` state, not fire-and-forgotten as before — the previous code had no way for any other part of the app to know whether that thread was still running (probe: `Read` of `spawn_engine_loading` confirms `let handle = std::thread::spawn(...)` followed by `*load_handle.lock().unwrap() = Some(handle)`; both call sites — startup `.setup()` and `download_missing_models`'s post-download reload — pass a real `EngineLoadHandle` clone, confirmed via `grep -n "load_handle" src/lib.rs`).
- [x] ISC-268: The app's `RunEvent::ExitRequested` handler checks `engine_load_still_in_progress` before allowing the process to exit. If the loading thread is still running, `api.prevent_exit()` is called and a second thread `.join()`s the loading thread before calling `app_handle.exit(0)` for real — this is the actual fix: it guarantees the loading thread's Metal initialization has fully completed before ggml's own atexit-time global Metal device teardown runs on the main thread, closing the race that produced the crash (probe: `Read` of the `.run(|app_handle, event| ...)` closure in `lib.rs` confirms the `if let tauri::RunEvent::ExitRequested { api, .. } = event` branch, the `prevent_exit()` call, and the join-then-exit(0) sequence).
- [x] ISC-269: `engine_load_still_in_progress` is a small pure function (`&Mutex<Option<JoinHandle<()>>> -> bool`) so the exact gating logic is unit-testable without needing a running Tauri app — `None` (never spawned) and "spawned but finished" both read `false`; a genuinely still-running thread reads `true` (probe: `cargo test --lib engine_load_handle_tests` — 3 tests: `nothing_ever_spawned_is_not_in_progress`, `a_running_load_thread_is_in_progress`, `a_finished_load_thread_is_not_in_progress`, all `ok`).
- [x] ISC-270: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after this fix, with zero regressions (baseline going in: 187 passed; final: 190 passed, +3 new). One `oauth::tests::await_authorization_code_times_out_if_nothing_connects` failure was seen in one run under heavy concurrent system load from an unrelated stale background process being killed mid-run — confirmed a pre-existing flake, not a regression, by re-running it in isolation (`ok`, 0.31s) and then re-running the full suite clean (190/190, 0 failed).

### MicMuteToggle — Phase 1 of the call-audio-scope idea (2026-08-07, Jeremiah's new proposal after the paused compliance-bot investigation: "install a mute button for my side of the call... so a sidebar conversation with my wife or kids isn't being recorded"). Explicitly Phase 1 only — a manual, kai-notetaker-controlled mute gate. Auto-tying it to Teams/Zoom/Meet's own mute button (Phase 2, via a global hotkey that ALSO posts a synthetic mute keystroke into the call app via `CGEventPostToPid`, sidestepping the already-proven-unreadable internal mute state) is a deliberate, separate follow-up — out of scope here.

- [x] ISC-271: `audio_capture::RecordingSession` gains a `muted: Arc<AtomicBool>`, `false` at `start()`. `build_capture_stream`'s capture callback (both the direct and resampled branches) writes SILENCE — a same-length all-zero buffer, not a skipped/shortened write — into the shared sample buffer whenever `muted` is `true`, instead of the real mic samples. Writing silence rather than skipping preserves the recording's real-time timeline (elapsed timer, WAV duration, diarization timestamps all stay meaningful) — a shortened buffer would desync them (probe: the exact gating logic is a small pure function, e.g. `fn gate_muted(samples: Vec<f32>, muted: bool) -> Vec<f32>`, unit-tested directly against synthetic sample vectors — muted in returns same-length zeros, unmuted returns the input unchanged). Implemented exactly as specified, plus one refinement beyond spec: in the resampled branch, gating is applied to the resampler's OUTPUT, not its input — the sinc resampler carries ~128 taps of internal history, so zeroing the input would still leak a few milliseconds of real pre-mute audio out the other side. Gating the output makes the muted span byte-exact silence with no leak window.
- [x] ISC-272: `RecordingSession::switch_device` passes the SAME `muted` `Arc` (cloned, not reset) into the new stream it builds — switching input hardware mid-recording must never silently un-mute (probe: `switch_device`'s call to `build_capture_stream` passes `self.muted.clone()`, confirmed via `Read`; a test starts muted, switches device, and confirms newly-captured samples are still silence AND that `Arc::strong_count(&session.muted) == 2` post-switch — the count check is the real discriminator, since a quiet-room microphone emits exact zeros regardless of mute state, making a samples-only assertion pass even against a broken implementation that handed the new stream an unrelated flag).
- [x] ISC-273: A new Tauri command (`toggle_mic_mute() -> Result<bool, String>`) flips the active recording's `muted` flag and returns the new state; an emitted event (`mic-mute-changed`, `{ muted: bool }`) lets both the main window and the `RecordingBadge` overlay reflect the true state without polling, backstopped by including `mic_muted` in the existing `recording_status` poll payload so a dropped event self-heals within 500ms. Anti: calling this with no active recording returns a clear `Err`, not a panic (probe: `Read` of `toggle_mic_mute_inner` confirms `guard.as_ref().ok_or("no recording in progress")?`; `bunx tsc --noEmit` exit 0 confirms the frontend's event listener type-checks).
- [x] ISC-274: `tauri-plugin-global-shortcut` is added (scoped to `[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]`, matching the crate's own `#![cfg(...)]` gate rather than a blanket top-level dependency) and a system-wide hotkey (**Cmd+Option+M**, confirmed via `global-hotkey` 0.8.0's own source that `Modifiers::META` normalizes to `SUPER`/Command on macOS — not assumed) is registered at startup. Pressing it while a recording is active calls `toggle_mic_mute_inner`, the exact same function the Tauri command calls — no duplicated toggle logic, confirmed via `Read`. Registration failure (combo already claimed, Accessibility permission missing) is logged and non-fatal; the in-app button is unaffected.
- [x] ISC-275: The `RecordingBadge` overlay shows a distinct mic-mute indicator (a genuinely different icon shape — a slash across the mic capsule, not just a color change — plus the label text itself switches from "Recording" to "Muted") and a clickable toggle. `RecordingControl.tsx`'s main-window recording screen ALSO got its own mute button (beyond the original spec's explicit ask, but matching its intent: "someone already looking at kai-notetaker shouldn't have to go hunting for the badge or remember the hotkey").
- [x] ISC-276: Anti: muting never stops, pauses, or restarts the recording session — verified by a test that toggles mute 6 times mid-recording and confirms the WAV's total frame count exactly equals every sample the capture callbacks produced (a skipped-write mute would fail this by exactly the muted duration), and that file duration still tracks wall-clock time.
- [x] ISC-277: A fresh recording always starts unmuted (`RecordingSession::start` constructs a fresh `Arc::new(AtomicBool::new(false))` every call). One real gap the Engineer caught beyond the backend: the `RecordingBadge` overlay window is created once at startup and only shown/hidden (never unmounted), so without an explicit fix it would still display the PREVIOUS recording's muted state the instant a new one starts. Fixed by emitting `mic-mute-changed: { muted: false }` from `start_recording` itself.
- [x] ISC-278: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after MicMuteToggle lands, with zero regressions (baseline going in: 190 passed; final: 195 passed, +5 new, independently re-run by Kai).

### TeamsCallMuteSync — Phase 2 of the call-audio-scope idea (2026-08-07, Jeremiah's real live-call test with Paula): confirmed the hotkey/button as built only gates kai-notetaker's own capture and never touches Teams — Paula could hear him whenever Teams itself was unmuted, regardless of kai-notetaker's mute state. Jeremiah's ask, verbatim: "the hotkey should be able to control the mute key from the teams call as well." This makes kai-notetaker the thing that SENDS Teams' own mute command (via `CGEventPostToPid`, targeting Teams' process directly, not whatever's focused) rather than trying to read Teams' internal state — the exact mechanism scoped with Jeremiah before MicMuteToggle Phase 1 was built. Teams only, for now — Zoom is a structurally identical follow-up (same mechanism, different bundle ID/keycombo) whenever Jeremiah wants it; Google Meet is explicitly out of scope (browser-tab-scoped mute can't be reliably targeted by posting to a whole browser process).

- [x] ISC-279: A new function `find_running_pid(exec_name: &str) -> Option<i32>` (in new `src-tauri/src/call_mute.rs`) shells out to `pgrep -x <exec_name>` and parses the first returned PID. Returns `None`, not an error, when the process isn't running. **The ISA's own suggested positive-case fixture (`launchd`, PID 1) was wrong and was replaced, not used** — the Engineer measured that unprivileged `pgrep` cannot see PID 1 at all (`pgrep -x launchd` exits 1 even though `ps -p 1` shows it running; pgrep enumerated 394 of 401 processes on the test machine, PID 1 among the 7 it can't see), and separately that `pgrep` excludes a process's own ancestry, so probing for the test binary's own name is equally invalid. The real fixture: a hermetic child process started by the test itself under a unique name (a symlink to `/bin/sleep`, since copying the binary invalidates its code signature and macOS kills it), asserted through a full absent→present→absent round trip via polling — strictly stronger than a static existence check, and depends on no installed app or system process.
- [x] ISC-280: A new function `post_key_combo_to_pid(pid: i32, keycode: core_graphics::event::CGKeyCode, flags: core_graphics::event::CGEventFlags) -> Result<(), String>` constructs a real keyDown AND keyUp event pair via `core_graphics::event::{CGEvent, CGEventSource}` (`CGEventSourceStateID::CombinedSessionState`) and posts BOTH to the target pid via `CGEvent::post_to_pid`. A real discovery beyond the original spec, confirmed by an actual compile failure rather than assumed: `CGEvent::post_to_pid` is gated behind core-graphics' `elcapitan` Cargo feature (`#[cfg(feature = "elcapitan")]`, matching macOS 10.11+, harmless given this app already requires far newer) — independently re-verified by Kai against the crate's real source and `Cargo.toml` before trusting the claim.
- [x] ISC-281: `toggle_mic_mute_inner` is extended: after flipping kai-notetaker's own capture and dropping the recording-state lock, it calls a new `sync_teams_call_mute()` which finds Microsoft Teams (executable `MSTeams`, confirmed against the real installed `/Applications/Microsoft Teams.app/Contents/Info.plist`) via ISC-279 and, if running, posts Cmd+Shift+M (Teams' own real mute shortcut, deliberately distinct from kai-notetaker's own Cmd+Option+M) via ISC-280. Failure isolation is stronger than the spec asked for: `sync_teams_call_mute` returns `()`, not `Result` — there is no error value for the caller to ever propagate with `?`, making it structurally impossible for a future edit to let a Teams-side problem fail the recording-side toggle, rather than merely relying on careful error handling at the call site.
- [x] ISC-282: When Accessibility permission is not granted (`AXIsProcessTrusted()`, a real raw FFI declaration against the ApplicationServices framework — no crate in this dependency tree wraps it), a clear, ACTIONABLE one-time warning is logged (gated by `std::sync::Once` so a whole muted meeting doesn't repeat the same paragraph on every toggle) naming the exact remedy: System Settings → Privacy & Security → Accessibility, enable kai-notetaker, restart the app. Deliberately still attempts the post afterward rather than treating the check as a hard gate — `AXIsProcessTrusted` can read stale immediately after the user grants permission.
- [x] ISC-283: Teams-only, verified: `grep -rn "zoom\|Google Meet\|Meet\b" src-tauri/src/call_mute.rs` (case-insensitive) returns zero matches — no Zoom/Meet code, not even a stub or TODO.
- [x] ISC-284: `cargo test --lib` passes 100% and `bunx tsc --noEmit` exits 0 after TeamsCallMuteSync lands, with zero regressions (baseline going in: 195 passed; final: 199 passed, +4 new, independently re-run by Kai from a clean shell).

### UI/UX (deferred until hard gates pass — placeholder ISCs for the eventual ideal state)

- [x] ISC-77: refined (2026-08-06/07, corrected stale marker — real work happened earlier in this multi-day project but the checkbox was never updated): a recording-control screen exists with start/stop and a visible recording-state indicator (`RecordingControl.tsx` — device picker, record button with idle/recording states, live elapsed timer, error and last-saved states). No pause control exists (not built, not requested) — probe: `Read` of `src/components/RecordingControl.tsx` confirms `start_recording`/`stop_recording` wiring and a live timer driven by `setInterval`.
- [x] ISC-78: refined (stale marker corrected): a meeting-library screen lists past meetings (`MeetingLibrary.tsx` — title, date, duration, live-polled processing/ready/failed status, delete). No search/filter control exists yet — probe: `Read` confirms `list_meetings` + 4s poll interval + `delete_meeting` wiring; `grep -n "search\|filter"` returns no matches, confirming search specifically is the real remaining gap here.
- [x] ISC-79: refined (stale marker corrected): a meeting-detail screen shows the diarized transcript (with per-segment and per-speaker inline correction), summary, and action items (`MeetingDetail.tsx`, 533 lines) — probe: `Read` confirms `transcript`/`summary`/`action_items` fields rendered, plus speaker-labeling UI (`list_known_speakers`, `label_transcript_segments`) already verified earlier this session under SegmentScopedSpeakerCorrection/SpeakerIdentification.
- [x] ISC-80: refined (2026-08-07, Jeremiah's direct verification — "yes it's been verified, windows resizing works fine"): UI responds to window resize without layout breakage on macOS, the dev platform — confirmed by Jeremiah's own hands-on check, a real live-probe per this ISA's own evidentiary standard, not a code-review inference. The flex/grid layout choice (no `@media` breakpoints needed) held up as expected. Windows OS itself remains untested — not this ISC's claim to make, and already covered separately by the standing "Windows build entirely untouched, deferred to the very end" note elsewhere in this ISA; no Windows machine has ever been available to this project.
- [x] ISC-81: refined (stale marker corrected): the app's animations use a single consistent easing/duration system — `src/styles/tokens.css` defines `--ease-standard`, `--ease-decelerate`, `--ease-accelerate` as the sole motion-token source, referenced by component CSS rather than ad-hoc per-component values — probe: `grep -n "ease-standard\|ease-decelerate\|ease-accelerate" src/styles/tokens.css` confirms the three named tokens exist; component CSS files reference them rather than declaring their own easing curves.
- [x] ISC-82: refined (stale marker corrected): first-launch experience is gated by `FirstRunSetup.tsx`, which blocks the rest of the app (`onReady={() => setModelsReady(true)}` in `App.tsx`) until required models are present/downloaded — probe: `Read` of `App.tsx` confirms `FirstRunSetup` renders before any other view when `modelsReady` is false. Whether this specific flow requires literally zero manual input (vs. e.g. a "Download" click) is not re-verified here — the original ISC-82 wording ("zero manual configuration") may be marginally stronger than what's built; a real click-through would confirm exactly.

### Performance Budgets

- [ ] ISC-83: App cold-start time (process launch to interactive window) is ≤ 2 seconds on the dev Mac (DEFERRED — no UI to measure yet).
- [ ] ISC-84: Idle memory footprint (no active recording/processing) is ≤ 300MB (DEFERRED).
- [ ] ISC-85: A 60-minute meeting's full pipeline (ASR + diarization + summarization) completes in under 15 minutes wall-clock on the dev Mac (DEFERRED — needs models downloaded).
- [ ] ISC-86: Local LLM inference for a single chunk summarization completes in under 10 seconds on the dev Mac's GPU (DEFERRED).

### RBAC / Multi-User Model (small, known user set)

- [ ] ISC-87: The app supports per-OS-user data isolation — Jeremiah's meeting library is never readable from Paula's OS user account without explicit export (probe: data directory resolution uses per-OS-user app-data paths, not a shared location — reviewable in code, DEFERRED full integration test).
- [ ] ISC-88: There is no in-app authentication/login system in v1 — access control is OS-account-level only, a deliberate scope decision (probe: `grep` confirms no login/session code exists — this is intentional, matching Out of Scope).

### Anti-Criteria (regression / scope guards)

- [x] ISC-89: Anti: no `unwrap()` calls exist in the three hard-gate modules' non-test code (probe: `grep -c "\.unwrap()" src-tauri/src/audit_log.rs src-tauri/src/retention.rs src-tauri/src/cloud_sync_gate.rs` outside `#[cfg(test)]` blocks returns 0 — panics in the audit/retention/compliance path are unacceptable).
- [x] ISC-90: Anti: no `println!`/`eprintln!` in the hard-gate modules writes decrypted transcript or audio content to stdout/stderr (probe: `grep` review, no such writes present).
- [x] ISC-91: Anti: the SQLCipher key is never logged, printed, or written to any file other than the OS keychain (probe: `grep -r "key" src-tauri/src/*.rs` reviewed manually for any Display/Debug/print of the key value).
- [ ] ISC-92: Anti: `cargo audit` (or equivalent dependency vulnerability scan) reports zero critical/high vulnerabilities in direct dependencies at time of each dependency addition (probe: `cargo audit` run — tool not yet installed this session, follow-up task).
- [x] ISC-93: Anti: this session's BUILD did not touch any UI component file under `src/` — hard gates were built Rust-side only, honoring the plan's own sequencing (probe: `git diff --stat` for this session's commits shows zero changes under `src/`, only `src-tauri/`).

## Test Strategy

```yaml
- isc: ISC-4
  type: build-probe
  check: cargo check exit code
  threshold: 0
  tool: cargo check (src-tauri/)

- isc: ISC-12
  type: unit-test
  check: verify_chain() on untampered 5-entry log
  threshold: Ok(())
  tool: cargo test audit_log::tests::verify_chain_passes_untampered

- isc: ISC-13
  type: unit-test
  check: verify_chain() detects single-byte mutation
  threshold: Err(_) with correct tampered index
  tool: cargo test audit_log::tests::verify_chain_detects_mutation

- isc: ISC-14
  type: unit-test
  check: verify_chain() detects deleted entry
  threshold: Err(_)
  tool: cargo test audit_log::tests::verify_chain_detects_deletion

- isc: ISC-23
  type: integration-test
  check: retention_sweep hard-deletes expired rows
  threshold: row count decreases, SELECT returns none
  tool: cargo test retention::tests::sweep_deletes_expired

- isc: ISC-27
  type: unit-test
  check: RetentionPolicy rejects near-infinite max_age_days
  threshold: constructor returns Err above ceiling
  tool: cargo test retention::tests::rejects_disabling_retention

- isc: ISC-30
  type: unit-test
  check: is_sync_allowed() with no BAA record
  threshold: false
  tool: cargo test cloud_sync_gate::tests::blocked_by_default

- isc: ISC-32
  type: unit-test
  check: is_sync_allowed() with expired BAA
  threshold: false
  tool: cargo test cloud_sync_gate::tests::blocked_when_expired

- isc: ISC-35
  type: unit-test
  check: no wildcard bypass of BAA gate
  threshold: false for unmatched target
  tool: cargo test cloud_sync_gate::tests::no_default_allow

- isc: ISC-89
  type: static-check
  check: no unwrap() in hard-gate modules outside tests
  threshold: 0 matches
  tool: grep -c "\.unwrap()" src-tauri/src/{audit_log,retention,cloud_sync_gate}.rs

- isc: ISC-159
  type: unit-test
  check: parsed UpcomingMeeting has non-empty Graph event id
  threshold: id.is_empty() == false
  tool: cargo test calendar::tests::parses_a_real_graph_shaped_response_into_upcoming_meetings

- isc: ISC-160
  type: integration-test
  check: auto_join_log row survives connection close/reopen
  threshold: row present after reopen
  tool: cargo test storage::tests::auto_join_log_persists_across_reconnect

- isc: ISC-161
  type: unit-test
  check: second poll against already-logged event_id
  threshold: decision == Skip(AlreadyTriggered)
  tool: cargo test auto_join::tests::does_not_double_trigger_same_event

- isc: ISC-163
  type: unit-test
  check: trigger decision when RecordingState is Some
  threshold: decision == Skip(AlreadyRecording), join_url still opened
  tool: cargo test auto_join::tests::skips_auto_start_when_already_recording

- isc: ISC-166
  type: unit-test
  check: setting flips false between two poll calls
  threshold: second call takes no action
  tool: cargo test auto_join::tests::toggle_takes_effect_next_cycle

- isc: ISC-168
  type: unit-test
  check: eligibility window boundaries (2h out / 30s out / 10m past)
  threshold: NotYet / Eligible / WindowPassed respectively
  tool: cargo test auto_join::tests::eligibility_window_boundaries

- isc: ISC-169
  type: unit-test
  check: poll cycle given a Graph Err(_) result
  threshold: function returns normally, no panic
  tool: cargo test auto_join::tests::survives_graph_api_error

- isc: ISC-176
  type: build-probe
  check: cargo test --lib exit code and pass count
  threshold: exit 0, passed >= 101 + new tests, 0 failed
  tool: cargo test --lib

- isc: ISC-177
  type: build-probe
  check: bunx tsc --noEmit exit code
  threshold: 0
  tool: bunx tsc --noEmit

- isc: ISC-181
  type: unit-test
  check: auto-stop fires strictly after marker.end, never at/before
  threshold: bool per case, matches strict `>`
  tool: cargo test auto_join::tests::should_auto_stop_fires_strictly_after_the_meetings_end

- isc: ISC-185
  type: unit-test
  check: Google Events list response parsed into UpcomingMeeting
  threshold: real field names, real timeMin/timeMax query
  tool: cargo test (Google-shaped fixture, mirrors ISC-153's Microsoft precedent)

- isc: ISC-195
  type: unit-test
  check: Zoom end computed as start_time + duration minutes
  threshold: exact duration-minute offset
  tool: cargo test (Zoom-shaped fixture)

- isc: ISC-198
  type: unit-test
  check: fetcher list matches exactly the connected-provider set (0/1/2/3)
  threshold: no fetcher for a disconnected provider, one per connected provider
  tool: cargo test auto_join::tests (multi-provider fixture matrix)

- isc: ISC-202
  type: unit-test
  check: RMS below threshold for continuous 60s triggers the dialog callback path
  threshold: fires once per silence window, not per check
  tool: cargo test (silence-monitor unit test with synthetic RMS samples)

- isc: ISC-209
  type: integration-test
  check: WAV file exists and is valid before stop_and_write is called
  threshold: file exists, valid header, on disk immediately after start()
  tool: cargo test audio_capture::tests (real RecordingSession::start, no stop)

- isc: ISC-210
  type: integration-test
  check: a real recording killed mid-flight (thread aborted, not stopped) is still readable up to the last flush
  threshold: hound::WavReader opens it successfully, duration > 0, no finalize() ever called
  tool: cargo test audio_capture::tests (simulates an ungraceful death by dropping the session without stop_and_write)

- isc: ISC-211
  type: static-check
  check: no WavWriter/file handle referenced inside build_capture_stream's callback closures
  threshold: 0 matches
  tool: grep -n "WavWriter\|File::create" src-tauri/src/audio_capture.rs (scoped to the callback closures)

- isc: ISC-214
  type: regression-test
  check: existing clean-stop tests pass unmodified
  threshold: 0 failed
  tool: cargo test audio_capture::tests::stopped_recording_always_declares_canonical_sample_rate audio_capture::tests::recording_path_is_inside_provided_data_dir_never_system_temp

- isc: ISC-215
  type: unit-test
  check: stop_and_write returns well under one full FLUSH_INTERVAL_SECS
  threshold: elapsed time << FLUSH_INTERVAL_SECS
  tool: cargo test audio_capture::tests (timed stop-responsiveness test)

- isc: ISC-217
  type: integration-test
  check: startup scan identifies an orphaned auto-generated-pattern .wav with no matching meetings row
  threshold: exactly the orphaned file, none else
  tool: cargo test storage::tests / lib.rs startup-scan test (temp dir + temp SQLite fixture)

- isc: ISC-218
  type: unit-test
  check: imported-* files are never touched by the scan
  threshold: 0 recovered from an imported-*-only fixture
  tool: cargo test (fixture matrix: orphaned auto-generated file + imported-* file)

- isc: ISC-219
  type: unit-test
  check: a file with a matching meetings.audio_path row is never re-recovered
  threshold: 0 recovered
  tool: cargo test (fixture: matching meetings row + on-disk file)

- isc: ISC-220
  type: unit-test
  check: recovered meeting's duration_secs matches the real WAV header duration
  threshold: exact match
  tool: cargo test (fixture .wav with known real duration)

- isc: ISC-222
  type: build-probe
  check: cargo test --lib exit code and pass count; bunx tsc --noEmit exit code
  threshold: exit 0 both; passed >= 140 + new tests, 0 failed
  tool: cargo test --lib && bunx tsc --noEmit

- isc: ISC-228
  type: unit-test
  check: activity classifier on inACall/inAMeeting/available/unrecognized-string
  threshold: InCall/InCall/NotInCall/NotInCall respectively
  tool: cargo test presence::tests (or equivalent module)

- isc: ISC-231
  type: unit-test
  check: InCall + RecordingState::None triggers start_recording
  threshold: exactly one start action
  tool: cargo test (presence trigger-decision unit test)

- isc: ISC-232
  type: unit-test
  check: InCall + RecordingState::Some triggers no second start
  threshold: zero start actions
  tool: cargo test (idempotency test, mirrors auto_join's already_recording pattern)

- isc: ISC-233/234
  type: unit-test
  check: InCall->NotInCall transition with a PresenceBased marker triggers stop; a CalendarEnd marker is unaffected by the same transition
  threshold: stop fires only for PresenceBased; CalendarEnd-marked recording untouched
  tool: cargo test (marker-variant isolation test)

- isc: ISC-237
  type: build-probe
  check: cargo test --lib exit code and pass count; bunx tsc --noEmit exit code
  threshold: exit 0 both; passed >= 151 + new tests, 0 failed
  tool: cargo test --lib && bunx tsc --noEmit

- isc: ISC-239
  type: integration-test
  check: overlay window created once at startup, hidden; no re-creation on subsequent start/stop cycles
  threshold: one WebviewWindowBuilder::new call site, show/hide elsewhere
  tool: grep -n "WebviewWindowBuilder::new" src-tauri/src/lib.rs

- isc: ISC-243
  type: integration-test
  check: overlay visibility mirrors RecordingState across all trigger paths
  threshold: hidden with no recording; visible within one state-check cycle of any start; hidden within one cycle of any stop
  tool: manual/integration check across manual, calendar, and presence triggers

- isc: ISC-244
  type: build-probe
  check: cargo test --lib exit code and pass count; bunx tsc --noEmit exit code
  threshold: exit 0 both; passed >= (151 + TeamsPresenceAdhocRecording's new tests), 0 failed
  tool: cargo test --lib && bunx tsc --noEmit

- isc: ISC-245
  type: integration-test
  check: ensure_schema adds trigger_source to a pre-migration DB without erroring on existing rows
  threshold: column present after migration, pre-existing rows unaffected
  tool: cargo test storage::tests (migration-on-existing-DB fixture)

- isc: ISC-246/247
  type: build-probe
  check: create_meeting's new parameter is a required, typed enum argument
  threshold: cargo build fails if any call site omits it or passes an invalid value
  tool: cargo build (compile-time enforcement)

- isc: ISC-250/251
  type: unit-test
  check: each trigger call site passes its own correct, explicit TriggerSource
  threshold: calendar site -> Calendar, presence site -> Presence, orphan-recovery -> Recovered, manual default -> Manual
  tool: grep -n "start_recording(\|create_meeting(" (call-site audit) + cargo test --lib

- isc: ISC-254
  type: static-check
  check: no trigger_source/TriggerSource reference in the summarization pipeline
  threshold: 0 matches
  tool: grep -rn "trigger_source\|TriggerSource" src-tauri/src/summarization.rs src-tauri/src/llm.rs

- isc: ISC-255
  type: build-probe
  check: cargo test --lib exit code and pass count; bunx tsc --noEmit exit code
  threshold: exit 0 both; passed >= 171 + new tests, 0 failed
  tool: cargo test --lib && bunx tsc --noEmit

- isc: ISC-258
  type: unit-test
  check: create_meeting sets a real, non-null title for all 4 trigger sources
  threshold: calendar gets known_title verbatim; manual/presence/recovered get the computed fallback
  tool: cargo test --lib storage::tests

- isc: ISC-259
  type: unit-test
  check: mark_meeting_ready no longer sets or reads title
  threshold: signature drops the title param; grep confirms meeting_summary unused for title
  tool: cargo build (signature change is compile-enforced) + grep -n "meeting_summary" src-tauri/src/pipeline.rs

- isc: ISC-260
  type: unit-test
  check: ActiveRecording carries an Option<String> known title from start_recording to stop_recording/create_meeting
  threshold: calendar call site passes Some(meeting.subject.clone())
  tool: cargo test --lib + grep -n "start_recording(" src-tauri/src/lib.rs

- isc: ISC-263
  type: unit-test
  check: fallback_title exact string per trigger source given a fixed timestamp
  threshold: exact match for Manual/Presence/Recovered
  tool: cargo test --lib storage::tests::fallback_title*

- isc: ISC-266
  type: build-probe
  check: cargo test --lib exit code and pass count; bunx tsc --noEmit exit code
  threshold: exit 0 both; passed >= 175 + new tests, 0 failed
  tool: cargo test --lib && bunx tsc --noEmit

- isc: ISC-271
  type: unit-test
  check: gate_muted returns same-length silence when muted, unchanged input when not
  threshold: byte-for-byte zero buffer of equal length vs identity passthrough
  tool: cargo test --lib audio_capture::tests::gate_muted*

- isc: ISC-272
  type: unit-test
  check: switch_device preserves the muted Arc across a device swap mid-recording
  threshold: samples captured after switch_device while still muted are silence
  tool: cargo test --lib audio_capture::tests::*switch_device*mute*

- isc: ISC-274
  type: grep-probe
  check: tauri_plugin_global_shortcut registered; hotkey callback invokes the same toggle path as the Tauri command
  threshold: no duplicated toggle logic between hotkey callback and command
  tool: grep -n "global_shortcut\|toggle_mic_mute" src-tauri/src/lib.rs

- isc: ISC-278
  type: build-probe
  check: cargo test --lib exit code and pass count; bunx tsc --noEmit exit code
  threshold: exit 0 both; passed >= 190 + new tests, 0 failed
  tool: cargo test --lib && bunx tsc --noEmit

- isc: ISC-279
  type: unit-test
  check: find_running_pid returns None for a nonsense name, Some for launchd
  threshold: both cases correct with no dependency on Teams being installed
  tool: cargo test --lib call_mute::tests::find_running_pid*

- isc: ISC-281
  type: grep-probe
  check: toggle_mic_mute_inner calls find_running_pid + post_key_combo_to_pid after set_mic_muted, wrapped so failure can't roll back the kai-notetaker-side mute
  threshold: Teams attempt is best-effort, never fails the overall toggle
  tool: grep -n "find_running_pid\|post_key_combo_to_pid" src-tauri/src/lib.rs

- isc: ISC-284
  type: build-probe
  check: cargo test --lib exit code and pass count; bunx tsc --noEmit exit code
  threshold: exit 0 both; passed >= 195 + new tests, 0 failed
  tool: cargo test --lib && bunx tsc --noEmit
```

## Features

```yaml
- name: ProjectScaffold
  description: Tauri+React+Rust project init, git, toolchain verification
  satisfies: [ISC-1, ISC-2, ISC-3, ISC-4, ISC-5, ISC-6, ISC-7]
  depends_on: []
  parallelizable: false

- name: AuditLogGate
  description: Hash-chained append-only tamper-evident audit log (Hard Gate 1)
  satisfies: [ISC-8, ISC-9, ISC-10, ISC-11, ISC-12, ISC-13, ISC-14, ISC-15, ISC-16, ISC-17, ISC-18, ISC-19, ISC-20]
  depends_on: [ProjectScaffold]
  parallelizable: true

- name: RetentionGate
  description: Code-enforced, scheduled, non-overridable retention/deletion (Hard Gate 2)
  satisfies: [ISC-21, ISC-22, ISC-23, ISC-24, ISC-25, ISC-26, ISC-27, ISC-28]
  depends_on: [ProjectScaffold, AuditLogGate]
  parallelizable: false  # ISC-24 requires AuditLog::append to exist first

- name: CloudSyncGate
  description: BAA-on-file gate blocking all cloud sync by default (Hard Gate 3)
  satisfies: [ISC-29, ISC-30, ISC-31, ISC-32, ISC-33, ISC-34, ISC-35, ISC-36]
  depends_on: [ProjectScaffold]
  parallelizable: true

- name: StorageLayer
  description: SQLCipher-backed encrypted database with OS-keychain key sourcing
  satisfies: [ISC-37, ISC-38, ISC-39, ISC-40, ISC-41, ISC-42]
  depends_on: [ProjectScaffold]
  parallelizable: true

- name: AudioCapture
  description: Device enumeration, recording, temp-file lifecycle
  satisfies: [ISC-43, ISC-44, ISC-45, ISC-46, ISC-47, ISC-48]
  depends_on: [StorageLayer]
  parallelizable: false  # future session

- name: AsrPipeline
  description: whisper-rs on-device transcription, GPU-accelerated
  satisfies: [ISC-49, ISC-50, ISC-51, ISC-52, ISC-53, ISC-54, ISC-55]
  depends_on: [AudioCapture]
  parallelizable: false  # future session

- name: DiarizationPipeline
  description: pyannote-ONNX speaker diarization via ort
  satisfies: [ISC-56, ISC-57, ISC-58, ISC-59, ISC-60]
  depends_on: [AsrPipeline]
  parallelizable: false  # future session

- name: LocalLlmPipeline
  description: Chunked map-reduce summarization, action items, embeddings, capped frontier polish
  satisfies: [ISC-61, ISC-62, ISC-63, ISC-64, ISC-65, ISC-66, ISC-67, ISC-68, ISC-69, ISC-70, ISC-71, ISC-72]
  depends_on: [DiarizationPipeline, AuditLogGate]
  parallelizable: false  # future session

- name: CrossPlatformValidation
  description: Windows build/CI, per-OS GPU backend selection, gate-module OS-agnosticism
  satisfies: [ISC-73, ISC-74, ISC-75, ISC-76]
  depends_on: [AsrPipeline]
  parallelizable: true

- name: UiShell
  description: Recording control, library, detail screens, motion system
  satisfies: [ISC-77, ISC-78, ISC-79, ISC-80, ISC-81, ISC-82]
  depends_on: [AuditLogGate, RetentionGate, CloudSyncGate, LocalLlmPipeline]
  parallelizable: false  # explicitly gated on all three hard gates per plan

- name: PerformanceBudgets
  description: Cold-start, memory, pipeline-latency measurement and enforcement
  satisfies: [ISC-83, ISC-84, ISC-85, ISC-86]
  depends_on: [UiShell]
  parallelizable: true

- name: MultiUserIsolation
  description: Per-OS-account data isolation, no in-app auth (deliberate v1 scope)
  satisfies: [ISC-87, ISC-88]
  depends_on: [StorageLayer]
  parallelizable: true

- name: SecurityHygiene
  description: Anti-criteria enforcement — no unwrap/panic, no secret logging, dep audit
  satisfies: [ISC-89, ISC-90, ISC-91, ISC-92, ISC-93]
  depends_on: [AuditLogGate, RetentionGate, CloudSyncGate]
  parallelizable: true

- name: InstallerFirstRun
  description: First-run download of the 5 local models with a real progress screen, not manual curl
  satisfies: [ISC-105, ISC-106, ISC-107, ISC-108, ISC-109, ISC-110, ISC-111]
  depends_on: [LocalLlmPipeline, DiarizationPipeline, AsrPipeline]
  parallelizable: false

- name: MeetingImport
  description: One-off migration of the old screenshot-based notetaker's real audio into this app
  satisfies: [ISC-112, ISC-113, ISC-114, ISC-115]
  depends_on: [StorageLayer, LocalLlmPipeline]
  parallelizable: false

- name: MeetingManagementAndAudioQuality
  description: Rename/delete/undo for meetings, plus real ASR/diarization quality fixes found via Jeremiah's own review of the migrated meetings
  satisfies: [ISC-119, ISC-120, ISC-121, ISC-122, ISC-123]
  depends_on: [StorageLayer, AsrPipeline, DiarizationPipeline]
  parallelizable: false

- name: SpeakerIdentification
  description: Voice-fingerprint enrollment + auto-match layered on diarization, so real names (not "Speaker N") appear in transcripts/summaries
  satisfies: [ISC-128, ISC-129, ISC-130, ISC-131, ISC-132, ISC-133, ISC-134, ISC-135]
  depends_on: [StorageLayer, DiarizationPipeline, LocalLlmPipeline]
  parallelizable: false

- name: SegmentScopedSpeakerCorrection
  description: Per-segment (and shift-click range) speaker label overrides that take precedence over the index-wide label, fixing real cases where diarization merges two different people into one raw index
  satisfies: [ISC-136, ISC-137, ISC-138, ISC-139, ISC-140]
  depends_on: [SpeakerIdentification]
  parallelizable: false

- name: KnownSpeakerCountOverride
  description: Force diarization to an exact, known speaker count instead of guessing from a similarity threshold — fixes real over-splitting (12 indices for a 3-person call)
  satisfies: [ISC-141, ISC-142, ISC-143, ISC-144, ISC-145]
  depends_on: [DiarizationPipeline, MeetingManagementAndAudioQuality]
  parallelizable: false

- name: CalendarOAuth
  description: Generic OAuth2+PKCE engine plus a Microsoft/Outlook calendar provider — real upcoming-meeting data, portable across any Microsoft account
  satisfies: [ISC-146, ISC-147, ISC-148, ISC-149, ISC-150, ISC-151, ISC-152, ISC-153, ISC-154, ISC-155, ISC-156, ISC-157, ISC-158]
  depends_on: [StorageLayer]
  parallelizable: true

- name: AutoJoinRecording
  description: Background poller replacing the disabled meeting-watcher cron — opt-in, idempotent auto-open-join-link + auto-start-recording for calendar meetings, provider-agnostic poll/trigger logic
  satisfies: [ISC-159, ISC-160, ISC-161, ISC-162, ISC-163, ISC-164, ISC-165, ISC-166, ISC-167, ISC-168, ISC-169, ISC-170, ISC-171, ISC-172, ISC-172.1, ISC-173, ISC-174, ISC-175, ISC-176, ISC-177, ISC-178, ISC-179, ISC-180, ISC-181, ISC-181.1]
  depends_on: [CalendarOAuth, StorageLayer]
  parallelizable: false

- name: GoogleCalendarOAuth
  description: Second calendar provider on the shared OAuth2+PKCE engine — real Google Calendar events, independent connect/disconnect from Microsoft
  satisfies: [ISC-182, ISC-183, ISC-184, ISC-185, ISC-186, ISC-187, ISC-188, ISC-189, ISC-190]
  depends_on: [CalendarOAuth]
  parallelizable: true

- name: ZoomOAuth
  description: Third calendar/meetings provider on the shared OAuth2+PKCE engine — real Zoom scheduled meetings, independent connect/disconnect
  satisfies: [ISC-191, ISC-192, ISC-193, ISC-194, ISC-195, ISC-196, ISC-197, ISC-198, ISC-199]
  depends_on: [CalendarOAuth]
  parallelizable: true

- name: SilenceBasedStopPrompt
  description: Second-layer, lower-confidence auto-stop signal — 60s of real RMS silence during an auto-triggered recording prompts a native Stop/Continue dialog, independent of the calendar-end-time auto-stop
  satisfies: [ISC-200, ISC-201, ISC-202, ISC-203, ISC-204, ISC-205, ISC-206, ISC-207, ISC-208]
  depends_on: [AutoJoinRecording]
  parallelizable: false

- name: RecordingDurability
  description: Periodic WAV-flush checkpointing (bounds crash/dev-restart audio loss to one flush interval) plus startup-time orphan-recording recovery into the real meeting pipeline — closes ISC-181.1
  satisfies: [ISC-209, ISC-210, ISC-211, ISC-212, ISC-213, ISC-214, ISC-215, ISC-216, ISC-217, ISC-218, ISC-219, ISC-220, ISC-221, ISC-222, ISC-223]
  depends_on: [AudioCapture, MeetingManagementAndAudioQuality, StorageLayer]
  parallelizable: false

- name: TeamsPresenceAdhocRecording
  description: Detects unscheduled/ad-hoc Teams calls (e.g. Meet Now) via Microsoft Graph presence — silent auto-start/auto-stop, bidirectional on the same poll signal, independent of calendar data
  satisfies: [ISC-224, ISC-225, ISC-226, ISC-227, ISC-228, ISC-229, ISC-230, ISC-231, ISC-232, ISC-233, ISC-234, ISC-235, ISC-236, ISC-237]
  depends_on: [CalendarOAuth, AutoJoinRecording]
  parallelizable: false

- name: RecordingIndicatorOverlay
  description: Small always-on-top overlay window showing a live Recording badge + timer whenever any recording is active, across all trigger paths (manual, calendar, presence) — Jeremiah's explicit condition for accepting silent auto-start
  satisfies: [ISC-238, ISC-239, ISC-240, ISC-241, ISC-242, ISC-243, ISC-244]
  depends_on: [AudioCapture, TeamsPresenceAdhocRecording]
  parallelizable: false

- name: RecordingTriggerProvenance
  description: Persists and surfaces HOW each meeting was captured (manual/calendar/presence/recovered) so past recordings are unambiguous for later analysis
  satisfies: [ISC-245, ISC-246, ISC-247, ISC-248, ISC-249, ISC-250, ISC-251, ISC-252, ISC-253, ISC-254, ISC-255]
  depends_on: [StorageLayer, AutoJoinRecording, TeamsPresenceAdhocRecording]
  parallelizable: false

- name: CalendarDisconnectButton
  description: Real Disconnect button per provider in the Calendar tab, closing the gap Jeremiah hit needing a Keychain-level workaround to re-consent for the new Presence.Read scope
  satisfies: [ISC-256, ISC-257]
  depends_on: [CalendarOAuth]
  parallelizable: false

- name: MeetingTitleStandardization
  description: Real calendar meeting titles carry through to the recording immediately; a deterministic trigger-source+timestamp naming convention replaces the old LLM-summary-snippet fallback for everything else
  satisfies: [ISC-258, ISC-259, ISC-260, ISC-261, ISC-262, ISC-263, ISC-264, ISC-265, ISC-266]
  depends_on: [AutoJoinRecording, TeamsPresenceAdhocRecording, RecordingTriggerProvenance, StorageLayer]
  parallelizable: false

- name: QuitCrashRace
  description: Fixes a SIGABRT on normal app quit — the app now waits for the background model-loading thread to finish before letting the process's exit teardown race its in-flight Metal init
  satisfies: [ISC-267, ISC-268, ISC-269, ISC-270]
  depends_on: []
  parallelizable: false

- name: MicMuteToggle
  description: Phase 1 of the call-audio-scope idea — a manual mute gate on kai-notetaker's own mic capture (writes silence, not real audio, for muted spans), toggleable from the recording badge overlay or a global hotkey regardless of OS focus
  satisfies: [ISC-271, ISC-272, ISC-273, ISC-274, ISC-275, ISC-276, ISC-277, ISC-278]
  depends_on: [AudioCapture, RecordingIndicatorOverlay]
  parallelizable: false

- name: TeamsCallMuteSync
  description: Phase 2 — the same mute toggle also posts Teams' own mute keystroke directly into Teams' process via CGEventPostToPid, so one hotkey press actually mutes the real call, not just kai-notetaker's own recording
  satisfies: [ISC-279, ISC-280, ISC-281, ISC-282, ISC-283, ISC-284]
  depends_on: [MicMuteToggle]
  parallelizable: false
```

## Decisions

- 2026-08-05 08:36: Project created at `~/Projects/kai-notetaker` (not inside `~/.claude`) — this is a standalone product, not a PAI system component, matching the existing `~/Projects/interceptor` precedent for external tools.
- 2026-08-05 08:36: Rust toolchain was not installed on the dev Mac at task start. Installed via `brew install rustup-init` → `rustup toolchain install stable`, then symlinked `cargo`/`rustc`/etc. from the Homebrew keg-only `rustup` formula into `/opt/homebrew/bin` because non-interactive Bash tool calls do not source `~/.zshrc`, so the standard PATH-export approach silently fails on every subsequent tool call. This is the load-bearing fact this session's THINK phase surfaced and verified before proceeding.
- 2026-08-05 08:36: `create-tauri-app`'s official CLI supports `-y`/non-interactive flags (`--manager bun --template react-ts --tauri-version 2 -y`), avoiding the need to hand-write the Tauri scaffold or use a blocked `curl | sh` pattern for any part of setup.
- 2026-08-05 08:36: Scope for this session's BUILD/EXECUTE explicitly limited to ProjectScaffold + the three Hard Gate features (AuditLogGate, RetentionGate, CloudSyncGate), per the user's own build plan which states these three gates must exist and be tested before any UI code. ASR/diarization/local-LLM model integration require large binary model downloads and are deliberately deferred to follow-up sessions rather than faked or stubbed with placeholder "always succeeds" logic.
- 2026-08-05 08:36: show-your-math on ISC count — 93 ISCs drafted at OBSERVE against the E4 soft floor of 128. The full app's eventual ideal state (ASR/diarization/LLM edge cases, full UI state matrix, per-platform performance tuning) will grow this file well past 128 as those Features are actually worked in future sessions; drafting speculative ISCs now for pipelines that don't exist yet would violate the "no nameable probe" granularity rule for anything model-download-dependent. Frontmatter `progress: 0/93` anticipates near-term growth as Windows CI and model-integration ISCs are added; will be corrected to the true count at LEARN.
- 2026-08-05 08:36: Forge (GPT-5.4 via codex) is the standing E3+ auto-include for coding tasks, but per prior session's explicit deferral (`feedback_skip_forge_until_reinstalled` memory, codex CLI not installed), Forge is skipped this run. Delegation floor for E4 (soft, ≥2) is met instead via Agent-tool research delegation for unfamiliar crate APIs (whisper-rs, ort, blake3) during BUILD, and ContextSearch as a thinking capability — documented here as the required show-your-math for the Forge skip specifically, not for delegation generally.
- 2026-08-05 15:20: Rust/Tauri is an explicit, Council-approved override of the standing "bun/TypeScript for all new code" PAI operational rule — logged here per the advisor's flag so a future session reading that rule doesn't mistake the Rust tree for a mistake. The React/TS frontend still honors the rule; only the core engine is Rust, per the 3-round Council debate's converged recommendation (2026-08-05, see `~/Downloads/KCG_AI_Notetaker_Council_Debate.md`).
- 2026-08-05 15:20: Advisor call (Rule 2, commitment-boundary) surfaced four real gaps, addressed as follows: (1) **audit-before-delete ordering bug** — `retention_sweep` originally deleted rows THEN wrote the audit entry; fixed to write the audit entry FIRST so a mid-sweep crash produces a visible "claimed deletion" rather than an invisible unlogged one. Tests re-run, still 17/17 passing. (2) **hash-chain overclaim risk** — added an explicit doc-comment limitation to `audit_log.rs`: the chain proves "not accidentally corrupted," not "cryptographically unforgeable by someone with disk access" (no external-keyed HMAC yet); a v2 hardening pass should anchor the chain to an OS-keychain-held key or head-hash. (3) **rusqlite feature-conflict risk** — checked via `cargo tree -i libsqlite3-sys -e features`; confirmed only one feature set (`bundled-sqlcipher-vendored-openssl` cascading to its own prerequisites) resolves in the graph, no conflict. (4) **BAA gate fail-closed** — already true and already tested (`blocked_by_default_with_no_baa_record`); no change needed, but noted that once a real egress path exists it should be re-verified the gate is actually called on that path, not just unit-tested in isolation.
- 2026-08-05 15:20: Advisor also surfaced a gap OUTSIDE this ISA's scope that could NOT be resolved unilaterally: the OLD Bun/TS notetaker pipeline (`PAI/MEMORY/WORK/meeting-notetaker-agent/`, cron-scheduled every 5 minutes in `PULSE.toml`, auto-uploads screenshots/audio to OneDrive) has none of these gates and continues running in parallel with zero code changes this session. This is Jeremiah's decision to make (keep both running during a transition period vs. decommission the old pipeline now vs. some hybrid) — surfaced to him directly rather than acted on, since disabling a pipeline he currently relies on without his sign-off would be exactly the kind of unilateral action the "confirm before risky/hard-to-reverse actions" standing rule exists to prevent.
- 2026-08-05 15:20: Retention's "hard-delete" (ISC-23) currently covers only SQLite rows, not the underlying audio/screenshot files on disk or their OneDrive copies (advisor's point: "the dominant retention surface is the files, not the DB rows"). This is a known gap for the AudioCapture feature (not yet built) to close — file-deletion-on-retention needs to be part of that feature's own ISC set, not retrofitted as an afterthought once files exist.
- 2026-08-05 17:15: Jeremiah asked how end users (Nesta, Paula, contractors) will actually get the models — today's downloads were all manual `curl` commands, not part of any installer. Answered directly: this is a real gap, not yet built. Recommended a first-run download flow (small installer, app checks for models on first launch, downloads with a progress screen) over bundling ~5.5GB into the installer itself, because installer-bundling would force every future app update to re-ship all models. Added as a future `InstallerFirstRun` concern — not yet a Feature in this ISA, needs to be added before UI work assumes models already exist on disk.
- 2026-08-05 17:15: refined: embedding model changed from "bge-large" (as named in the Council plan) to bge-small-en-v1.5 (~64MB vs ~639MB) — same BGE family, same local/on-device/no-new-dependency approach (runs through the already-loaded `llama-cpp-2` crate's native embedding-mode support, zero new dependencies), just a smaller size tier appropriate for a 16GB machine already running an 8B LLM plus diarization models concurrently. Plan's intent (local BGE embeddings for search/Q&A) fully honored.
- 2026-08-05 17:15: Frontier-model vendor defaulted to Anthropic Claude (`claude-haiku-4-5` — cheap/fast, appropriate for light editorial polish, not frontier-tier reasoning) rather than GPT, matching Jeremiah's existing Anthropic-centric setup (Claude Code, PAI's own Inference.ts). Flagged to Jeremiah as an easily-reversible choice, not blocked on his confirmation before building, since swapping vendors later only touches `frontier.rs`'s request/response shape.
- 2026-08-05 17:15: Real bug caught: `llm.rs` and `embeddings.rs` each keep an independent `OnceLock<LlamaBackend>` (justified — see architecture comment in both files), but `llm.rs`'s first version used `.expect()` on `LlamaBackend::init()` instead of the same graceful `unwrap_or` fallback `embeddings.rs` used for the expected `BackendAlreadyInitialized` case. First cross-module test run (llm:: + embeddings:: + summarization:: tests in one process) panicked immediately. Fixed by making both modules use the identical fallback pattern — inconsistency between two structurally-identical pieces of code, not a novel bug.
- 2026-08-05 20:15: Built the previously-missing middle piece: stop_recording used to just save a WAV and stop. Now it creates a `processing` meeting row, and a background OS thread runs the full pipeline (resample → ASR → diarization → summarize → extract action items → embed → persist) and flips the row to `ready` or `failed`. Real storage schema (`storage.rs`) replaces the old placeholder — still unencrypted SQLite (SQLCipher wiring remains its own deferred item, ISC-38/39), but now has real content columns (transcript segments, summaries, action items, embedding vectors as BLOBs) instead of empty stub tables. Engines (ASR/diarization/LLM/embedding) load once at startup in a background thread rather than per-recording — confirmed via a real log line, not assumed. Meeting Library and Meeting Detail screens built against this real data, both polling for async processing completion rather than requiring a manual refresh.
- 2026-08-05 19:30: Jeremiah hands-on tested the recording button himself (mic permission granted, red state + timer confirmed working) and caught two real, valuable issues: (1) the device selector looked like static text — `appearance: none` had stripped native select styling with nothing added back to signal it's interactive; fixed with a chevron icon + hover state. (2) He asked for live device switching mid-recording (e.g., switching input source without stopping/restarting), citing music recording as a use case where stopping isn't acceptable. Built for real: `CANONICAL_SAMPLE_RATE` (48kHz, not 16kHz, specifically because music was named as a use case and Whisper's 16kHz would be lossy for that) normalizes every device's audio via a real-time rubato sinc resampler before it reaches the shared buffer, so `RecordingSession::switch_device` can swap the live stream without corrupting the WAV's single declared sample rate. Told Jeremiah honestly up front that a small (tens-of-ms) gap during the actual hardware handoff is real and unavoidable — promised "no glitch/corruption," not "zero gap." Real testing limitation stated plainly (ISC-98): only one physical input device exists on this dev machine, so true cross-hardware resampling correctness needs a second device or CI with virtual audio devices — not yet verified beyond same-device switch-continuity and synthetic resampler math.
- 2026-08-05 18:05: RecordingControl screen built (React + real design-token system: KCG Navy/Teal/Amber palette + Cormorant Garamond, no KCG logo/wordmark per explicit instruction; both light/dark via system `prefers-color-scheme`). Real Tauri commands wired (`list_audio_devices`, `start_recording`, `stop_recording`) backed by the already-tested `audio_capture` module; `cpal::Stream`'s `Send`/`Sync` status was verified against the crate's own compile-time assertion before trusting it in Tauri managed state, not assumed. Visually confirmed via one safe, exact-window-bounds screenshot: renders correctly, dark mode applied, real device ("MacBook Air Microphone (default)") correctly detected live. NOT confirmed: the actual click-to-record flow end-to-end. Two attempts to screenshot the live "recording" state accidentally captured unrelated sensitive content on Jeremiah's real desktop (Signal DMs, then a military LES with partial SSN) — both deleted immediately, neither described further. Root cause: clicking Record almost certainly triggers macOS's first-time microphone permission dialog, a system-modal prompt that only Jeremiah can approve and that stole window focus during automated screenshot attempts. Stopped automating further interaction with his live desktop rather than risk a third accidental capture. ISC-77/ISC-48 left unchecked pending Jeremiah's own hands-on first click (he'll see a real macOS "kai-notetaker wants to access the microphone" prompt the first time — that's expected, not a bug).
- 2026-08-05 17:15: Real finding, not a logic bug: the full test suite (42 tests, 4 GPU-accelerated model types loading concurrently — Whisper, pyannote diarization, 8B LLM, BGE embeddings) showed one transient failure under cargo test's default parallel execution on this 16GB machine, but passed 42/42 reliably both single-threaded and after forcing `RUST_TEST_THREADS=1` via `.cargo/config.toml`. Documented as a real hardware-driven test-execution requirement, not worked around silently — a future CI runner with more RAM may not need this, but shouldn't assume it doesn't either without re-verifying.
- 2026-08-05 16:10: refined: Constraints originally named "pyannote models exported to ONNX, run via `ort`" verbatim from the Council plan. Research (real, verified) found the official pyannote org gates all models behind a HF auth token and publishes zero ONNX exports — only community conversions exist, and hand-rolling raw `ort` tensor I/O against them means reimplementing sliding-window aggregation, powerset-class decoding, and speaker-embedding clustering from scratch. `k2-fsa/sherpa-onnx` already solves exactly this, ships an official Rust crate (not the archived third-party `sherpa-rs`), and internally uses ONNX Runtime against the same pyannote-segmentation-3.0 model family — verified downloadable with no auth from `github.com/k2-fsa/sherpa-onnx/releases`. Constraint refined to "on-device diarization via sherpa-onnx (ONNX Runtime-based internally)" — same goal (local, cross-platform, no vendor lock-in, no cloud), better-fitted mechanism. The plan's INTENT (on-device, ONNX-based, no cloud) is fully honored; only the specific crate-level plumbing changed.
- 2026-08-05 15:35: Rule 2a (Cato cross-vendor audit, mandatory at E4/E5) did not fire this session — two independent reasons, not one excuse standing in for the other: (1) `phase` was deliberately NOT set to `complete` this run (this is an ongoing project ISA with ~53 ISCs still pending across unbuilt Features; Rule 2a's literal trigger is "before setting phase: complete," which doesn't apply to a mid-stream `phase: execute` checkpoint), and (2) even if it had fired, Cato runs via `codex exec` (GPT-5.4), the same codex CLI already confirmed not installed in this environment (`reference_forge_codex_unavailable.md`) — identical blocker to the Forge skip logged earlier. Both reasons recorded so a future session doesn't assume Cato was silently forgotten.
- 2026-08-05 21:00 (commit c46469a): Real bug fixed: `CREATE TABLE IF NOT EXISTS` doesn't migrate an existing table's shape. Jeremiah's own real database still had the 2-column placeholder `meetings` table from earlier in the same dev cycle, so every query against `title`/`duration_secs`/etc. failed with "no such column: title" at runtime. Fixed via `reset_if_schema_outdated()` — detects the mismatch and drops+recreates all app tables. Explicitly documented in the module's own header comment as dev-only, unacceptable once real user data exists (tracked as the still-open ISC-42 gap below).
- 2026-08-05 21:15 (commits a70daa0, 8cc42f4): Two real bugs from Jeremiah's own hands-on testing of a 5-second test recording (some of it in Spanish). (1) Spanish speech came back garbled/forced into English — root-caused by inspecting the vendored whisper.cpp C++ source directly: `whisper_full_default_params` hardcodes `language: "en"`, not `"auto"`, when left unset. Asked Jeremiah which behavior he wanted (`AskUserQuestion`) — chose auto-detect + transcribe-in-original-language over always-translate-to-English. Fixed via `params.set_language(Some("auto"))`; ASR model swapped from `ggml-base.en.bin` to multilingual `ggml-base.bin`. (2) The meeting had no summary at all — queried Jeremiah's real `.sqlite3` file directly and found the actual LLM output was a refusal ("It appears that there is no meeting transcript excerpt provided..."), not empty output. First fix attempt (skip the redundant reduce-step for a single chunk) didn't resolve it — the refusal happened at the chunk-summary step itself. Root-caused to `chunk_summary_prompt`'s wording; strengthened with explicit anti-refusal instructions, verified against a new regression test using Jeremiah's exact real (garbled, pre-language-fix) transcript text.
- 2026-08-05 21:30 (commit d81ca5e): Added audio playback to Meeting Detail, per Jeremiah's request ("Is the actual audio not going to be part of that summarized page?"). Uses Tauri's asset protocol (`assetProtocol.enable` + scope `$APPDATA/recordings/*`) + `convertFileSrc`. Known, disclosed limitation: no real HTTP range-request support for scrubbing very large files (upstream Tauri limitation, not something this app's code can fix).
- 2026-08-05 21:45 (commit 1079be1): Built bidirectional transcript-audio sync per Jeremiah's request — auto-scroll follows the active transcript segment during playback (fires only on active-index change, not every `timeupdate` tick, so it doesn't fight a user who scrolled ahead to read); clicking any transcript line seeks the audio to that segment's start time. Confirmed working by Jeremiah ("Yes it works. Great.").
- 2026-08-05 22:00 (commits 3f3eac7, 506c150): Two layout requests, same session. First: split Meeting Detail so only the transcript region scrolls, not the whole page (`.detail-screen__top` fixed height, `.detail-screen__transcript` independently `overflow-y: auto`). Immediately followed by a second, related request: make that split boundary itself freely draggable. Built via pointer-events drag (not mouse events — better multi-input support), with the top section's height stored in React state and clamped between `MIN_TOP_HEIGHT`/`MIN_TRANSCRIPT_HEIGHT` so neither pane can be dragged to zero. Reads real layout measurements (`getBoundingClientRect()`) at drag-start rather than assuming a fixed window size, so clamping stays correct across window resizes.
- 2026-08-05 22:15 (this session, SQLCipher): Load-bearing fact verified BEFORE writing any real code (not assumed): built a standalone `cargo run --example` scratch test proving the `bundled-sqlcipher-vendored-openssl` feature actually compiles working SQLCipher (not a no-op) — correct key round-trips real data, wrong key fails to read, and a raw byte-scan of the file confirms no plaintext leakage. Key is generated (32 random bytes via `getrandom::fill`, NOT derived from a passphrase — avoids weak-password risk entirely) and stored in the macOS Keychain via `security-framework`'s `passwords::{get_generic_password, set_generic_password}`, cached process-wide in a `static OnceLock` so the Keychain isn't queried on every single Tauri command (each command opens its own connection). `security-framework`'s real resolved version was 3.7.0, not the 2.x assumed from memory — verified its actual `passwords.rs`/`base.rs` source directly before use, per this project's established discipline of never trusting assumed crate APIs (same lesson as cpal/whisper-rs/llama-cpp-2 earlier). A pre-existing dev-era plaintext DB (Jeremiah's own test data from this same session) is detected before SQLCipher ever touches it and archived (renamed with a `.pre-encryption-backup-<timestamp>` suffix, including `-wal`/`-shm` sidecars) rather than corrupted — same dev-only-safe discipline already established by `reset_if_schema_outdated`. **Real-world confirmation, not just tests:** Tauri's dev-mode file watcher auto-rebuilt and relaunched the actual running app after this commit; Jeremiah's real dev-era database was correctly detected as plaintext and archived, and the real on-disk file is now confirmed unreadable by a plain `sqlite3` CLI (`file is not a database`) — encryption is live in the actual app, not just proven in isolated tests.
- 2026-08-05 22:30 (this session, installer + import prep): Building the installer surfaced a real, separate pre-existing gap: engine loading at app startup was hardcoded to `env!("CARGO_MANIFEST_DIR")`, a path baked in at BUILD time on the dev machine — this would resolve to a nonexistent directory on Nesta's, Paula's, or any contractor's installed machine. Fixed via `resolve_models_dir()`: production path is always `$APPDATA/models` (what the installer populates); dev builds fall back to the source-tree `models/` directory only when `$APPDATA/models` is empty and the dev copy is complete, so this machine's already-downloaded ~5.3GB of models never needs re-fetching. `cfg!(debug_assertions)` gates the fallback off entirely in release builds.
- 2026-08-05 22:30 (this session, installer): Several internal modules (`asr`, `audit_log`, `diarization`, `embeddings`, `llm`, `pipeline`, `storage`) changed from `mod` to `pub mod` so `examples/import_legacy_recordings.rs` can use them — examples compile as an external consumer of the lib crate, not as part of it, so private items aren't visible to them regardless of same-repo location.
- 2026-08-05 22:45 (this session, MeetingImport): Before running the real import against Jeremiah's actual recordings (including two genuine KCG client-related meetings — a PCI-DSS assessment call and a strategy call), inspected `audit_log.rs::append()` and found it does an unsynchronized read-then-write of the hash chain's tip with no file locking. Two processes appending concurrently (the live running dev app + a separate import binary) could race and produce a forked chain. Rather than add file-locking machinery for a one-off migration, stopped the running dev app first (`kill` on the `target/debug/kai-notetaker` PID, dev frontend/vite process left running), ran the import standalone, and will restart the dev app only once the import fully completes. Documented as ISC-115 (anti-criterion).
- 2026-08-05 22:50 (this session, real crash + fix): The first import attempt aborted the whole process partway through the 3rd real recording (`FwPicDssAssessment`, a genuine ~43-minute PCI-DSS assessment call) via a real llama.cpp assertion (`GGML_ASSERT(n_tokens_all <= cparams.n_batch)`), not a hypothetical edge case. Root cause and fix logged separately in `llm.rs` (see Changelog below). Made `import_legacy_recordings` idempotent before retrying: skips any recording whose destination file already has a `ready` meeting row, and cleans up any `processing`/`failed` row left behind by the aborted attempt (using the correct current table names — `transcript_segments`/`meeting_summaries`/`action_items`/`meeting_embeddings` — not the stale names still hardcoded in `retention.rs`'s own deletion SQL, a separate latent bug noticed in passing but out of this session's scope to fix).
- 2026-08-05 22:55 (this session, real operational finding — Keychain prompts on rebuild): Discovered via a real hung process (a throwaway `inspect_db` example) and confirmed via `sample`'s call-stack evidence (blocked inside `SecItemCopyMatching` → `mach_msg`, the exact signature of a pending `SecurityAgent` authorization dialog): macOS's Keychain ACL for the generated SQLCipher key does NOT persist cleanly across every rebuild of this unsigned dev binary — a brand-new binary path (like a new `examples/*.rs` target) triggers a fresh, real, un-clickable-by-me system dialog. Re-running the SAME already-approved binary path did NOT re-prompt. This is the same class of "shouldn't ask permission every single time" concern Jeremiah raised earlier about the mic prompt — surfacing here for Keychain access on every full `bun run tauri dev` relaunch during dev. Real fix (proper code-signing with a stable identity, so macOS's ACL binds to a durable identity instead of a rebuild-fragile one) is out of scope for a dev build and needs an Apple Developer certificate — logged as a real, not-yet-solved gap rather than worked around silently. Avoided making it worse: deleted the throwaway `inspect_db` example rather than leaving a second binary identity around to prompt again later.
- 2026-08-05 23:00 (this session, MeetingImport complete): Final result — `=== import complete: 6 succeeded, 0 failed ===`. All 6 real recordings from the old cron (2 short test recordings, 2 real KCG business calls — `FwPicDssAssessment` 43min, `StrategyCallWithNesta` 22min — and 2 more short test recordings) now have `ready` meetings in the new encrypted database. `meeting-watcher` disabled in PULSE.toml only after this confirmation, per Jeremiah's explicit ordering. Flagged to Jeremiah in the same turn: `meeting-watcher` did more than store recordings — it also auto-detected calendar meetings and auto-joined them via `MeetingJoiner.ts`; kai-notetaker has no calendar-auto-join equivalent yet, so this is a real capability gap until that's built, not just a storage-location change.
- 2026-08-05 23:10 (this session, commitment-boundary advisor call, Rule 2): Called before declaring this batch done. Real findings acted on immediately: (1) **PULSE.toml is only read at Pulse daemon startup, not hot-reloaded** — `enabled = false` on disk alone would NOT have stopped `meeting-watcher` from firing; confirmed via `~/.claude/PAI/Pulse/lib.ts`'s single `loadConfig` call site and the running daemon's actual PID predating my edit (`com.pai.pulse`, alive since Tuesday). Restarted it (`launchctl kickstart -k`); confirmed via the daemon's own startup log that its enabled-job count dropped by exactly one (11 → 10) and zero `meeting-watcher` runs logged since. (2) **Original old recordings were never deleted** — confirmed all 6 `.wav`+`.txt` pairs still present at their original path, untouched; the import script only ever copies. This is the actual current DR position for the 2 real KCG client meetings, not the SQLCipher-encrypted copy alone. **Real gaps surfaced but NOT closed this session** (deliberately not expanding scope unilaterally on a user's explicit 4-item ask): no cryptographic checksum on installer downloads (byte-size match only — weaker, though this session's specific crash was confirmed to be the n_batch bug, not a corrupted download, since the same model file completed two earlier real transcriptions successfully first); no escrow/backup of the SQLCipher Keychain key (if that Keychain item is ever lost, the encrypted DB is permanently unreadable — mitigated only by the untouched original files above); OneDrive-copy existence and old-pipeline audio-redaction-compliance for the migrated files were not verified this session; and the deeper architectural risk that llama.cpp inference runs in-process with live audio capture, so ANY future llama.cpp abort (not just this specific n_batch class, now fixed) would kill a concurrently-recording live meeting with no graceful save — this session fixed the one crash actually hit, not that whole class of risk.
- 2026-08-06 00:00 (Jeremiah's real hands-on review of the migrated meetings): Reported four things: (1) wants rename + delete/undo — built (ISC-119/120), a shared toast lifted to `App.tsx` so either the library list or the detail screen's delete gets the same 8s undo window. (2) "the summary is subpar" — traced to real transcript garbage, not a prompt/model-quality issue (see next entry). (3) speaker labels wrong, saw "Speaker 31" for a known 3-person call — real data showed it was actually worse (up to Speaker 82). Root-caused to `FastClusteringConfig`'s bare default (`threshold: 0.5`) being far more permissive than sherpa-onnx's own official example for the unknown-speaker-count case (`--clustering.cluster-threshold=0.90`, verified via the actual `sherpa-onnx-offline-speaker-diarization.cc` example usage comment). Fixed; real reprocessing dropped distinct speaker count 82 → 11-12. Still not exactly 3 — an honest partial result, not perfect, logged as such rather than overclaimed. (4) asked where files are stored — answered directly (`$APPDATA/recordings/`, `kai-notetaker.sqlite3`, `audit-log.jsonl`).
- 2026-08-06 00:15 (real root-cause chase on the summary-quality bug — three real attempts, two negative results before the actual fix): Queried the real `FwPicDssAssessment` transcript directly rather than trust the summary's own text. Found 1481 of 1579 segments (94%) were the exact same hallucinated sentence ("I don't know much about it, honestly") — the summarizer had nothing real to summarize. Attempt 1: `no_context: true` (stop feeding prior-window text forward as decode context) — real, defensible hygiene, but a REAL reprocessing run with only this change produced byte-for-byte IDENTICAL output (still 1579 segments, still 1481 repeats) — a genuine negative result, logged rather than hidden (ISC-123, anti-criterion). Investigated why by reading whisper.cpp's actual decode loop directly (`whisper.cpp` v1.7.6, vendored in `whisper-rs-sys-0.15.0`): its temperature-fallback ladder exists and does add real stochastic sampling at temperatures > 0 (`whisper_sample_token(..., best=false)` uses `std::discrete_distribution`) — contrary to an initial (wrong, corrected before acting on it) assumption that greedy decoding is temperature-invariant throughout. The ladder likely never triggered at all here because its only quality gate (`entropy_thold`) checks WITHIN-window token diversity (catches "the the the the"), not a single fluent, non-degenerate sentence recurring ACROSS dozens of separate ~30s windows — a different failure class entirely, driven by a long stretch of acoustically-similar background noise (a real `(wind howling)` segment elsewhere in the same file confirms genuine non-speech content is present). The mechanism actually built for THIS pattern is the `is_no_speech` gate (`no_speech_prob > no_speech_thold AND avg_logprobs < logprob_thold` → suppress the segment's text entirely) — but the crate's default `logprob_thold` (-1.0) is so permissive that a fluent-sounding hallucination on non-speech audio never trips it. Fixed via `params.set_logprob_thold(-0.5)`. Real reprocessing (attempt 2, same file): segment count 1579 → 338, zero repeated-phrase pattern (checked via `sort | uniq -c` on every segment's text, not eyeballed), summary went from a dismissive "appears to be a test recording" to genuinely coherent PCI-DSS/Smithville/CMMC/QSA content matching what Jeremiah described the call being about.
- 2026-08-06 00:30 (Jeremiah's real report after confirming the fixes worked): "I just want the entire window to be adaptive when resizing, because I didn't know the action items were there until I made the window full screen." Root cause: `topHeight === null` meant "let flexbox decide naturally" (`flex-shrink: 0`, no cap) — at a small window size the top section's content (summary + action items) could grow taller than the actual window, and since the element was never given a height smaller than its content, its own `overflow-y: auto` never had anything to act on; content just got pushed off-screen instead of becoming scrollable. Separately, once manually dragged, `topHeight` became a frozen pixel value that wouldn't adapt if the window was resized afterward either. Fixed by always maintaining a real, clamped pixel height (default 55% of container height when not manually set) recalculated via `ResizeObserver` on every container resize, not just at drag-time.
- 2026-08-06 00:40 (Jeremiah's request): summaries (new and already-generated) shouldn't open with "Here is a combined meeting summary of N sentences:" — the model echoing its own instructions back as a preamble. Fixed both `chunk_summary_prompt` and `reduce_prompt` with an explicit "output ONLY the summary, no preamble" instruction, matching the pattern `action_items_prompt` already used for the same reason. For the 4 real already-generated summaries with this preamble (meetings 1, 2, 3, 5 — 2 and 4 didn't have it), stripped it in place via a one-off `clean-summaries` example mode (find the leading "here is"/"here's", strip through the first colon, keep everything after) rather than re-running the full pipeline, since only the preamble text was wrong — the underlying content was already correct and expensive to regenerate for the two long real meetings.
- 2026-08-06 01:00 (Jeremiah asked directly: "you're sure everything we need to run this app is on that private repo?"): Honest answer was no — checked rather than assumed. Model binaries (correctly gitignored, ~5.3GB, downloaded on first run) and a real README were both genuine gaps; the README was still the generic create-tauri-app boilerplate. Wrote a real README (verified prerequisites — `cmake` confirmed via `grep 'name = "cmake"' Cargo.lock` + `which cmake`, not guessed) and closed ISC-111 (previously DEFERRED-VERIFY) for real: `cargo run --example verify_model_download` downloaded all 5 models fresh into the actual `$APPDATA/models` production directory (not a throwaway temp dir), which also means the app now uses that real production path going forward instead of the dev-tree fallback it had been running on all session. One real download's byte-count check failed on the first pass (archive-extracted diarization segmentation model) — root-caused as a bug in the NEW test script itself (compared the extracted file's size against the compressed archive's download size, not against the extracted file's actual expected size) rather than a real defect, confirmed via SHA256 checksum match against the known-good dev-tree copy before writing off the mismatch. Fixed the test's comparison logic and re-confirmed clean.
- 2026-08-06 09:45 (new session — Jeremiah's plan from the prior night: real speaker ID, fix auto-join, calendar sign-in): Before building anything, investigated the real feasibility of "native join automation" for Zoom/Meet/Teams, since that was this session's first framing. Found via real research: Zoom has a genuine Meeting SDK with raw audio access, but as of March 2026 requires extra authorization (ZAK/OBF tokens) specifically for joining meetings you didn't host; Google Meet's new official Meet Media API requires the meeting HOST to grant per-meeting permission via a Calendar setting; Microsoft Teams application-hosted media bots require enterprise Graph tenant provisioning (Calls.AccessMedia.All + admin consent) not available by default on arbitrary tenants. Presented this to Jeremiah as a real constraint before committing engineering time to any of it.
- 2026-08-06 10:00 (Jeremiah's correction, important): the app must NEVER join as a separate bot participant — it should join as the user's own authenticated session, so the meeting just sees the real person, not an added "Kai Notetaker" attendee. Researched why cloud SaaS notetakers (Otter, Fireflies, Recall.ai) use bots at all: confirmed it's purely a structural workaround for those services running on someone else's servers with zero access to the user's own device — a constraint that doesn't apply to a local-first app that's already running ON the user's machine. This reframes the whole join design: open the meeting link using the user's own already-logged-in session (browser/native client), no SDK-bot, no enterprise tenant registration needed anywhere, uniformly across all three platforms.
- 2026-08-06 10:05 (Jeremiah's real technical concern, confirmed and solved): worried that "system audio capture" for hearing other call participants would mean recording via physical speakers into a mic, picking up room noise. Confirmed this is a real risk if done naively, and found the real fix: `cpal` 0.18.1 (the exact version already in `Cargo.toml`, no bump needed) added native loopback-capture support in a real, verified PR (`RustAudio/cpal#894`, "Support ScreenCapture loopback") — macOS via ScreenCaptureKit-backed CoreAudio loopback (confirmed present in the installed source: `src/host/coreaudio/macos/loopback.rs`), Windows via WASAPI loopback. This is a clean digital tap of the pre-speaker audio stream, not an acoustic re-recording — genuinely solves the noise concern, and is a real improvement over the old pipeline's BlackHole virtual-audio-device approach (no third-party driver install needed). `device_description.rs` already classifies loopback as just another `DeviceType`, meaning this extends `audio_capture.rs`'s existing device-enumeration pattern rather than requiring a new one.
- 2026-08-06 10:15 (Jeremiah's request, cross-platform secure storage + migrate the encryption key too): confirmed and did the real migration. Verified `keyring` 4.1.6's `v1` module (`Entry::new/get_secret/set_secret/delete_credential`, `Error::NoEntry`) is enabled by DEFAULT (not an opt-in feature) and covers macOS Keychain + Windows Credential Manager + Linux Secret Service via one API. Real, load-bearing finding before trusting this with production data: `apple-native-keyring-store`'s macOS backend is built directly on `security_framework::passwords::{generic_password, set_generic_password_options}` — the EXACT same crate, same `kSecClassGenericPassword` item class, same service/account keying `keychain.rs` was already using directly. Migrated `keychain.rs` to two reusable primitives (`get_secret`/`set_secret` by account name) on top of `keyring`, removed `security-framework`/`security-framework-sys` entirely. Verified against the REAL production database (not just tests) before considering this safe: `cargo run --example import_legacy_recordings -- inspect 3` after the migration returned the identical title, summary, and 338-segment transcript as before — proving `keyring` found the exact same Keychain item rather than silently generating a new key that would have made Jeremiah's real migrated meetings permanently unreadable. `cargo test --lib`: 71/71 passing.
- 2026-08-06 (this session, SpeakerIdentification): Built real voice-fingerprint enrollment/auto-match layered on top of the already-fixed diarization clustering. New storage tables (`known_speakers`, `known_speaker_embeddings`, `meeting_speaker_labels`) plus a new `speaker_id.rs` module wrapping sherpa-onnx's `SpeakerEmbeddingExtractor`/`SpeakerEmbeddingManager` (match threshold 0.6, verified against the crate's own official example rather than guessed). `process_meeting_inner` now auto-matches each distinct diarized speaker's audio against enrolled voices and uses the real name in `labeled_transcript` when found, falling back to "Speaker N" otherwise. Deliberate design decision (ISC-135, anti-criterion): auto-match never writes a new embedding sample itself — only an explicit human label does — so a wrong auto-match can't silently reinforce itself into future meetings. New Tauri commands `list_known_speakers`/`label_meeting_speaker` (the latter takes a `remember` flag controlling whether labeling also captures a new fingerprint sample) back a new inline-editable speaker-label UI in `MeetingDetail.tsx` (click a speaker's name → text input with autocomplete against known speakers + a "Remember" checkbox → Save). Real regression test `a_voice_enrolled_after_one_meeting_is_auto_recognized_in_the_next` proves the full loop closes: unenrolled voice in meeting 1 → manually enroll (simulating a real label) → same voice auto-recognized by name in meeting 2. Also fixed a real correctness gap found while reviewing `import_legacy_recordings.rs`'s `reprocess`/crash-recovery cleanup paths: neither cleared `meeting_speaker_labels`, so reprocessing a labeled meeting (which can assign different speaker indices than the prior run) could leave a label silently attached to the wrong voice — added the missing `DELETE FROM meeting_speaker_labels` to both paths (ISC-134). Per Jeremiah's own explicit ordering ("get the real speaker identification fix completed first... then we can start on the calendar OAuth build"), calendar OAuth work has not started yet.
- 2026-08-06 (Jeremiah's real frustration, redirected to the next planned feature — CalendarOAuth): Jeremiah reported the summary quality is "super lame" and compared unfavorably to cheap AI notetakers, and said to move on to OAuth. Answered honestly rather than silently absorbing the complaint: the likely root cause is architectural (this app's summarization runs a local 8B model for privacy/cost reasons; cheap cloud notetakers use small *cloud* models like GPT-4o-mini/Gemini Flash, which are meaningfully stronger than an 8B model on a laptop) — not something a prompt tweak fixes. Flagged a real, already-scaffolded-but-dormant lever for later: `frontier.rs::request_polish` (a capped, one-call-per-meeting frontier-model polish pass from the original Council plan) was never wired up. Also corrected a real expectation before Jeremiah built hope around it: calendar/attendee data tells the app WHO was invited, not WHICH voice is which — it's a complementary signal to fold into speaker matching later (per his own earlier question, still unbuilt), not a replacement for the diarization/voice-matching work already shipped. Neither of these was acted on this session (explicitly deferred, not silently dropped) since Jeremiah asked to move to OAuth specifically.
- 2026-08-06 (provider choice — AskUserQuestion, Jeremiah chose Microsoft 365/Outlook): Asked directly rather than guessing, since Jeremiah's stated goal ("Gmail, Outlook, Zoom, or any other calendars") named three real candidates and building all three without any way to test against a real account would be unverifiable ceremony. He chose Microsoft. Built the entire generic OAuth2+PKCE engine (`oauth.rs`) first regardless of provider choice, since 100% of it is shared mechanics (verified: Google/Microsoft both implement RFC 6749 + RFC 7636 identically for public/native clients; only Zoom's flow needs a client secret even for PKCE-only apps, a real documented quirk to handle if/when Zoom is built).
- 2026-08-06 (real API verification before writing any Microsoft-specific code, per this project's established discipline of never assuming external API shapes): Fetched Microsoft's own current documentation rather than working from memory, and found three load-bearing facts that would have been wrong or unverified otherwise: (1) `{tenant}` value `common` (not `organizations`) is required for the "any account, any org, personal too" portability Jeremiah asked for — confirmed via `v2-oauth2-auth-code-flow`'s own parameter table. (2) The port component of a registered `http://localhost` redirect URI is IGNORED by Microsoft for matching purposes — direct quote from `reply-url#localhost-exceptions`: "Due to ephemeral port ranges often required by native applications, the port component... is ignored for the purposes of matching a localhost redirect URI... This is *only* true for localhost redirect URIs." This meant the app never needs to coordinate a specific port with the Azure registration at runtime — a real simplification, not assumed. (3) `Calendars.Read` requires no admin consent for either work/school or personal accounts (Microsoft Graph permissions reference, `AdminConsentRequired = No`) — meaning Jeremiah's KCG tenant's own admin policies won't block this delegated permission by default. Also verified the real Graph `event` resource's JSON field names (`subject`, `start`/`end.dateTime`, `attendees[].emailAddress.{name,address}`, `onlineMeeting.joinUrl`) against Microsoft's live Event resource page before writing the parser struct — same discipline as this session's sherpa-onnx/whisper.cpp verifications, applied to a new external API family.
- 2026-08-06 (Jeremiah's question, answered directly — attendee-list as a complementary signal): Jeremiah asked whether knowing who's actually on the call could help determine/track speakers alongside voice matching. Answered: yes, a valid complementary signal (narrows the candidate set, helps disambiguate genuinely similar voices, gives a sensible default label before any audio-based match exists) but not a replacement for voice fingerprinting (voice ID still needed to know WHICH attendee is talking at any given moment, and it works even when calendar data is wrong/absent). Not built this session — real attendee data doesn't exist yet since calendar OAuth (the next planned feature) hasn't been started. Logged as a validated future enhancement to fold into `SpeakerIdentification` or a follow-on feature once calendar integration exists, not a redesign of the current architecture.
- 2026-08-06 (Jeremiah's real pushback, correctly identifying two separate gaps — KnownSpeakerCountOverride): Jeremiah pointed out that segment-scoped correction alone doesn't address the root cause: the real Smithville call has exactly 3 people, but diarization's threshold-based clustering produced up to 12 distinct raw indices — "there should not be 'Speaker 8' or 'Speaker 11'." He also asked whether a real name could be attributed to a voice directly (already-built capability — `SpeakerIdEngine` enrollment/auto-match — but not yet proven against his own real, current call). Verified via the real sherpa-onnx C++ header before writing any code (`fast-clustering-config.h`, fetched from the official repo) that `FastClusteringConfig.num_clusters`, when set to a positive integer, forces exactly that many clusters and ignores `threshold` — a distinct, officially-supported clustering mode, not a workaround. Threaded a `num_speakers: Option<i32>` override through `DiarizationEngine::load` → `process_meeting`'s new `diarization_override` param → a new `reprocess_meeting_with_speaker_count` Tauri command, kept deliberately separate from the shared, long-lived `PipelineEngines.diarization` (loaded once at startup, reused by every normal recording) so a rare, explicit, per-meeting "I know there were exactly N people" action never disturbs the default unknown-count path. Added a small UI control in Meeting Detail ("Fix speaker count") so this is available in the app itself, not just a terminal reprocess — directly addressing "make sure what is possible is being done instead of leaving it alone." Once diarization gives an honest ~3-way split, labeling each with "Remember" checked (the already-built `SpeakerIdentification` feature) is literally "attributing a name to a voice" — the missing piece was a diarization result clean enough to label against, not a missing enrollment capability.
- 2026-08-06 (Jeremiah's real hands-on bug report, root-caused before fixing — SegmentScopedSpeakerCorrection): Reported that renaming "Speaker 0" to correct his own early solo talking (first ~7 minutes) also silently renamed Dave, since Dave's later turn got clustered into the same raw index. Per the Root-Cause-at-Ingestion checkpoint, investigated where the bad state actually enters before touching any label-scoping code: ran `import_legacy_recordings -- inspect 3` against the real Smithville call and confirmed directly (not assumed) that raw speaker index 0 is one unbroken run of segments from 28.5s through past the 10-minute mark — diarization's clustering step never detected a turn boundary at all between Jeremiah stopping and Dave starting, so the individual ASR transcript segments remain distinct rows with real, different timestamps/text, but the raw speaker_index column never changes across that whole span. This ruled out a "just scope edits to a contiguous same-index run" fix (the merge itself is one long contiguous run — there's no index-change boundary to stop a run at) and confirmed the real fix needed genuine per-segment (or shift-click multi-segment range) label overrides, independent of where diarization happened to place its cluster boundaries. Built: new `transcript_segment_speaker_overrides` table taking precedence over the index-wide `meeting_speaker_labels` in `get_meeting_detail`'s resolution; new `label_transcript_segments` Tauri command (replacing the single-meeting-turn-old `label_meeting_speaker` command) taking explicit `segment_ids` plus an `apply_to_whole_speaker` flag (default `false` in the UI) for the old bulk-by-index behavior when the index genuinely is correct throughout. Frontend: single click still labels one line by default; shift+click extends to a range, so a long real mislabeled stretch can be fixed in one action without dragging in unrelated lines. Two new regression tests reproduce the reported failure shape exactly (two people sharing one raw index, no boundary between them) and prove each can now be corrected independently.
- 2026-08-06 (refined — Out of Scope updated for AutoJoinRecording): "calendar-based auto-join is a future feature" (originally written 2026-08-05) is now stale — Jeremiah picked it as the explicit next feature after CalendarOAuth. Updated the Out of Scope prose to clarify what stays permanently out of scope (Zoom/Teams bot APIs, bypassing lobby prompts, regex-parsing free-text meeting links) versus what's now in scope (Graph-native `onlineMeeting.joinUrl` meetings only, opening the link + auto-starting local capture).
- 2026-08-06 (FirstPrinciples THINK pass, AutoJoinRecording): deconstructed "auto-join" and found the term itself was overclaiming — neither the old disabled cron nor this replacement ever makes a bot join a call unattended (ruled out by Out of Scope; no webhook endpoint exists for push-based detection either). The real, honest mechanism is auto-*record*, with join-link-opening as a convenience side effect. This confirmed rather than changed the design already reflected in ISC-170/171.
- 2026-08-06 (ApertureOscillation THINK pass, AutoJoinRecording): oscillating the poller (tactical) against the whole app's provider-agnostic strategic vision and its established "shared state stays untouched, per-instance decisions stay local" pattern (from KnownSpeakerCountOverride) surfaced one real correction: the poll cycle's meeting-fetch step should call a small `Vec` of provider-fetcher closures (today: one, wrapping the Microsoft call) rather than hardcoding a direct call to `calendar::list_upcoming_meetings` inline — split off as ISC-172.1 so a future Google/Zoom provider is "register a second closure," not "rewrite the poll cycle."
- 2026-08-06 (delegation-floor show-your-math, AutoJoinRecording, E3): selected only Forge as the delegation capability rather than meeting the E3 soft floor of 2. Reasoning: the feature is one cohesive Rust module (`auto_join.rs`) directly mirroring an existing in-file pattern already proven in this codebase (the retention-sweep `tauri::async_runtime::spawn` interval loop in `lib.rs::run()`'s `setup()`), plus a small, bounded UI addition. A second delegate (Anvil in parallel, or a research agent) would add coordination/merge overhead without a correctness benefit on work this bounded and pattern-matched — the soft floor is deliberately relaxable for exactly this case per Algorithm doctrine.
- 2026-08-06 (refuted — Forge dispatch failed, redispatched to Anvil): Forge reported `codex` CLI absent on this machine (verified via `which codex`, direct path checks, and a recursive `find` — all negative) and refused to silently substitute Claude-family code under the Forge label, correctly citing that this would corrupt the cross-vendor assumption a later Cato audit would rely on. This matches standing memory (`reference_forge_codex_unavailable.md`, `feedback_skip_forge_until_reinstalled.md`) that should have been checked as this task's load-bearing fact for capability selection before dispatch, not discovered after — a real miss in this run's PLAN phase. Redispatched the identical spec to Anvil (Kimi K2.6 via Moonshot direct API) instead of Forge or Engineer, preserving the non-Anthropic cross-vendor property Forge would have provided.
- 2026-08-06 (refuted — Anvil dispatch also failed, redispatched to Engineer): Anvil halted at startup — `MOONSHOT_API_KEY` unset in both `process.env` and `~/.claude/.env` — a missing-credential failure, not a Moonshot outage. Both available non-Anthropic delegates (Forge, Anvil) are now blocked on separate real infra gaps (missing CLI; missing API key) neither of which is this feature's problem to fix, and both would require Jeremiah to go configure credentials mid-task for a benefit (cross-vendor code-review diversity) that matters less here because the dispatch spec itself is unusually complete — every table schema, function signature, and pattern-to-mirror was already fully specified by Kai before either dispatch attempt, per Anvil's own routing recommendation (option 2 in its halt report). Redispatched to Engineer (Marcus Webb, Claude-family) rather than pausing to provision a credential unrelated to what Jeremiah actually asked for. Delegation floor for this run is now effectively 0 external-vendor delegates; documented here rather than silently absorbed.
- 2026-08-06 (Engineer delivered — 113/113 tests, `tsc` clean, independently re-verified by Kai, not just trusted from the subagent's self-report): read the actual diff (`auto_join.rs`, `lib.rs`, `calendar.rs`, `storage.rs`, `CalendarSettings.tsx`/`.css`) and re-ran both `cargo test --lib` and `bunx tsc --noEmit` directly rather than accepting Engineer's reported numbers at face value. Two real bugs surfaced and fixed in Engineer's own test-writing (not production code): an invalid chrono format spec (`%.7f`) in a fixture helper, and a wall-clock-`now()` fractional-second issue that made boundary assertions unreliable — both are documented in `## Verification` below.
- 2026-08-06 (Advisor commitment-boundary review, real findings, all addressed or verified rather than accepted at face value): Advisor raised several points; each checked against actual system state before acting: (1) **Real, actioned** — the fixed 5-minute post-start grace was narrower than the manual Start Recording path's own (effectively unbounded) window, which would produce false "why didn't it record?" reports; widened `eligibility()` to close the window at the meeting's real `end` time instead (ISC-168 refined, code + tests updated, re-verified 113/113 passing after the change). (2) **Real question, verified fresh rather than trusted from memory** — whether the old, now-disabled `meeting-watcher` Pulse cron could still be running and collide with this feature; checked `PULSE.toml` directly (`enabled = false`, confirmed), `launchctl list`, and crontab — genuinely disabled, no conflict (ISC-179). (3) **Raised but not applicable** — Advisor referenced a `MeetingWatcher.ts`-specific "kill-switch" and a periodic-screenshot capability; investigated both: the kill-switch is a self-contained state file internal to that now-disabled TS job (not a shared mechanism), and dropping screenshots in favor of real audio capture is this entire project's founding Problem statement, not a gap. (4) **Correct, deferred honestly rather than claimed** — Advisor's evidence-standard point that 113 green unit tests is weaker proof here than usual, since Kai cannot write to Jeremiah's calendar to force a real end-to-end fire; logged as `[DEFERRED-VERIFY]` ISC-180 with a concrete follow-up (watch for the poller's own log lines against Jeremiah's next real connected-calendar meeting), matching the same honest-deferral pattern already used successfully for ISC-156 in CalendarOAuth.
- 2026-08-06 (real incident, root-caused and owned — lost the "First Meeting" recording): AutoJoinRecording's poller fired for real against a live calendar meeting while this session's own subsequent code edits were in flight. Tauri dev's file-watcher rebuilds and restarts the app process on every source edit; recordings only write their `.wav` to disk at *stop* (audio sits in an in-memory buffer until then, per `audio_capture.rs`'s `RecordingSession`). At least 5 restarts happened after the trigger (confirmed via `grep -n "all pipeline engines loaded and ready"` line-count in the dev log) before Jeremiah's own screen-check found no active recording and no new file existed on disk — the audio was lost with whichever process instance was holding it in memory, unrecoverable. Root cause: Kai read Jeremiah's own message ("it does not show any active recording right now, but you can stop it") as a passive status remark instead of the direct instruction it was, and kept editing code instead of stopping to verify recording state first. Logged as [[feedback_recording_state_instructions_are_commands]] — a real behavior-change commitment, not just an apology. ISC-181.1 documents the underlying dev-mode gap (restarts can destroy live recordings) as a real, separate, deliberately-deferred hardening item.
- 2026-08-06 (real requirement, Jeremiah's own words, driving GoogleCalendarOAuth + ZoomOAuth + SilenceBasedStopPrompt): directly caused by the incident above — Jeremiah asked for a real auto-stop (built, ISC-181), then clarified Zoom/Google Meet auto-stop should work "akin to the Microsoft outlook one" (real calendar OAuth per provider, not process/window detection — validated against and now exercising the provider-agnostic fetcher architecture ISC-172/172.1 was built for), explicitly ruled out building anything for manually-started recordings ("No manual recording ideas for now on this functionality request"), and required the stop-or-continue popup to trigger off "60 seconds of actual silence and no talking" rather than a fixed timer. Interpretation adopted (stated here so it can be corrected if wrong, per this project's own error-correction discipline): the calendar-end-time auto-stop (ISC-181) is a confident, direct signal and stays silent/automatic for all three providers now; the 60-second-silence signal is a second, lower-confidence layer that prompts rather than silently stopping, since a real meeting can run past or end before its scheduled calendar time, and momentary silence mid-conversation isn't proof the call ended. Real external API research done before writing any provider-specific code (this project's standing discipline, applied to two new API families): Google's native-app OAuth docs (`developers.google.com/identity/protocols/oauth2/native-app`, `.../calendar/api/v3/reference/events{,/list}`) and Zoom's OAuth + granular-scopes + meetings-API docs (`developers.zoom.us/docs/integrations/oauth/`, `.../oauth-scopes-granular/`) — see ISC-182 through ISC-199 for the specific verified facts, including a correction to an earlier (2026-08-06, CalendarOAuth) assumption that Zoom requires a client secret even for PKCE (wrong for Zoom's dedicated public-client app type, ISC-192). `tauri-plugin-dialog`'s real Rust API (`v2.tauri.app/plugin/dialog/`) verified for the native Stop/Continue prompt before assuming it existed or guessing its shape.
- 2026-08-06 (Engineer delivered GoogleCalendarOAuth + ZoomOAuth + SilenceBasedStopPrompt — 140/140 tests, independently re-verified by Kai): read the actual diff (`google.rs`, `zoom.rs`, `silence_monitor.rs`, `lib.rs`, `audio_capture.rs`, `auto_join.rs`, `Cargo.toml`, `CalendarSettings.tsx`/`.css`) and re-ran `cargo test --lib` + `bunx tsc --noEmit` directly rather than trusting Engineer's self-report alone. Three real findings, each disclosed by Engineer rather than hidden and each spot-checked by Kai as genuinely correct: (1) Google's `singleEvents=true&orderBy=startTime` and `access_type=offline&prompt=consent` authorize params — not in the original spec, but without them recurring events would fire against the wrong instance and the refresh token needed for long-term access would never be issued; (2) a real deadlock risk (the silence tracker's `Mutex` is non-reentrant, and the dialog's own "Continue" callback re-locks it) — fixed by scoping the lock to drop before the dialog call, confirmed via direct code read; (3) effective silence-prompt latency is ~120s in practice, not a literal 60s, because the RMS reading itself is a trailing-60s window compounding with the 60s consecutive-readings requirement — logged in ISC-202 as a refined criterion rather than silently shipped as "60 seconds" when it isn't. Also fixed a genuine bug in Engineer's own new test (re-prompt timing measured from the wrong reference point) before reporting done — the implementation was correct, only the test's arithmetic was wrong.
- 2026-08-06 (Advisor commitment-boundary review, Rule 3 conflict-surfacing applied — two of the Advisor's stated "blockers" described a DIFFERENT, unrelated system and were rejected after direct verification, not silently accepted or silently dismissed): the Advisor's `--auto-state` context pulled in facts from the OLD, disabled, TypeScript screenshot-based `meeting-watcher` system (`ffmpeg`, `BlackHole` dual-stream capture, `caffeinate`, a `kill -INT` stop hang, `MarkPrivate.ts` redaction) — the exact system this entire kai-notetaker project exists to replace. Verified directly against this repo before accepting or rejecting either claim: `grep -rn "ffmpeg\|BlackHole\|caffeinate" src-tauri/src/` returns zero matches; `RecordingSession` (`audio_capture.rs`) holds exactly one `cpal::Stream` (single mic capture), not a dual-stream setup; `MarkPrivate.ts` doesn't exist anywhere in this repo; the ISA's own `ISC-16` is the audit log's append-only-API guarantee, not a recording kill switch. Both "blockers" (silence detector misreading a dead BlackHole stream; the dialog's Stop button reusing a broken ffmpeg/caffeinate kill path) do not apply to this codebase and were dropped rather than acted on. Three of the Advisor's other points were real and acted on: (1) Google's OAuth scope narrowed from `calendar.readonly` to `calendar.events.readonly` — least privilege, verified both scopes' real semantics against Google's own docs before switching (ISC-184 refined). (2) The "personal-account scope creep" concern doesn't apply architecturally as raised (there's no automatic multi-account aggregation — each provider is one explicit, user-initiated OAuth connection where Jeremiah personally chooses the account at that provider's own login screen, same as the already-verified live Microsoft connection), but is worth Jeremiah's awareness going forward: whichever Google/Zoom account he signs into during Connect is the one that gets polled — his call which account that is per provider. (3) Google Cloud Console's "Testing" publishing status expires refresh tokens after 7 days (a real, well-known Google OAuth platform constraint, not a code bug) — flagged as operational setup guidance for whenever Jeremiah registers the real Google OAuth client, parallel to the Azure App Registration and Zoom public-client-toggle steps already given for the other two providers; not something fixable in this session's code.
- 2026-08-06 (refuted — a RootCauseAnalysis fork's claim about `hound::flush()` contradicted Kai's own direct source read, Rule 3 conflict-surfacing applied): the fork's FiveWhys report on the "First Meeting" incident claimed `hound::WavWriter::flush()` "flushes sample bytes but does not patch the RIFF/data chunk size header (that only happens at finalize())" and recommended the startup-orphan-recovery scan actively repair the WAV header from real file byte-length. Re-verified directly against the installed `hound` 3.5.1 source a second time (`write.rs:488-533`) rather than trusting either the fork's claim or Kai's own earlier read blindly: `flush()` calls `update_header()`, which explicitly seeks to byte offset 4 and writes the RIFF file-size field, then seeks to `data_len_offset` and writes the `data` chunk's size field — both header fields, not sample bytes. `flush()`'s own doc comment states directly: "Flush writes the WAVE header to the underlying writer to make the written bytes a valid wav file." The fork's claim is factually wrong; Kai's original read (THINK phase, this session) was correct. No header-repair logic is needed in the recovery scan — a `flush()`-checkpointed file already has a valid, correctly-sized header and opens cleanly via plain `hound::WavReader`, exactly as ISC-220 assumes.
- 2026-08-06 (Engineer delivered RecordingDurability — 151/151 tests, independently re-verified by Kai): read the actual diff (`audio_capture.rs`, `lib.rs`, `storage.rs`) rather than trusting the self-report alone. Confirmed the load-bearing ISC-210 test genuinely proves the fix — it reads the WAV file *while the `RecordingSession` is still alive*, before any `Drop` could run, which is the honest proof that `flush()` alone (not a destructor) makes the file durable. Confirmed ISC-213/214's "three call sites" assumption from the original spec was pleasantly wrong in a good way: the calendar-end auto-stop and silence-prompt paths both already route through the `stop_recording` Tauri command rather than calling `stop_and_write` directly, so there is exactly one real call site, unchanged. Two real deviations disclosed by Engineer and adopted after review: zero-duration orphans (crashed inside their very first checkpoint window) are skipped rather than turned into a permanently-broken `meetings` row (ISC-220 refined); the orphan-detection query deliberately includes soft-deleted `meetings` rows, so a recording Jeremiah already deleted is never resurrected by a later restart.
- 2026-08-06 (Advisor call failed twice — genuine tool timeout, not skipped, resolved via direct code verification instead): both `bun Inference.ts --mode advisor` invocations for this feature's VERIFY commitment-boundary check returned `Timeout after 120000ms`. Rather than block indefinitely or silently skip Rule 2, answered both of the intended questions myself against the actual code: (1) **Real gap found and fixed** — a write/flush/finalize failure inside the flusher thread (e.g. disk full) previously exited the thread silently via `?` with no log line, surfacing only whenever `stop_and_write` eventually joins the thread — which could be much later in a long meeting, meaning durability checkpointing could silently stop working with zero visible signal. Fixed directly (added `eprintln!` on all three failure paths before returning `Err`, ISC-223), re-verified 151/151 passing after the change. Panic handling was already correct and tested (`stop_and_write`'s `flusher.join.join()` match arm converts any panic payload into a proper `AudioCaptureError::Flusher`, never a raw propagated panic). (2) **Verified safe** — the startup orphan-recovery scan is safe to run every launch indefinitely: it's one directory listing + one batched DB query per launch, and the app already deletes a recording's WAV file after successful processing (pre-existing behavior, `RecordingSession`'s own module doc comment), so `recordings/` stays bounded to recently-orphaned or intentionally-retained files rather than growing unboundedly — no realistic scale risk for a single-user tool.
- 2026-08-06 (real gap in the ISA vs actual code — corrected, not built over): ISC-77/78/79/81/82 were still marked `[ ]`/DEFERRED from the original 2026-08-05 scaffold, but real screens (`RecordingControl.tsx`, `MeetingLibrary.tsx`, `MeetingDetail.tsx`, `FirstRunSetup.tsx`) and a motion-tokens system already existed, built at some point earlier in this multi-day project without the ISA ever being updated to match. Kai's own "real UI" recommendation to Jeremiah that morning was based on the stale markers, not the actual code — caught and corrected only because Jeremiah asked a direct clarifying question ("what does 'real UI' mean compared to what we currently have?"). All five ISCs corrected with real probes; ISC-80 (window resize) was the one genuinely-unverified item, and Jeremiah confirmed hands-on the same day that it works fine on macOS. Lesson: check actual code before recommending "what's next," not just an ISA's checkbox state, which can silently drift from reality.
- 2026-08-07 (Jeremiah's real question, driving TeamsPresenceAdhocRecording + RecordingIndicatorOverlay): asked directly whether an unscheduled/ad-hoc meeting (no calendar entry) would trigger AutoJoinRecording — answered honestly: no, the whole feature is calendar-data-driven, an ad-hoc call has no calendar signal to key off of. Presented four real options (mic-detection-prompt, mic-detection-silent, meeting-app-process-detection, stay manual) with a recommendation toward the prompt-based option, matching this project's established "low-confidence signal → ask, don't silently act" pattern (SilenceBasedStopPrompt). Jeremiah asked a sharper, more specific question instead — whether Teams' own "Meet Now" button specifically could be detected — which led to real research rather than picking from the offered menu: Microsoft Graph's `/me/presence` endpoint (`Presence.Read` scope, least-privilege, delegated) exposes a real, calendar-independent `activity` value (`inACall`/`inAMeeting`) that Microsoft's own Teams admin docs confirm is driven by "the state of the Teams app" (not the calendar) for "In a call," specifically distinct from "In a meeting" (calendar-driven). This is a genuinely better answer than any of the four originally-presented options — a first-party, narrow-scope, already-authenticated signal, not process/window scraping or broad mic detection. Jeremiah then chose silent auto-start (not the prompt pattern this session had leaned toward for lower-confidence signals) — a real, informed tradeoff he made himself, conditioned explicitly on a persistent visual indicator existing so he's never unaware recording is running, which became RecordingIndicatorOverlay. He also explicitly scoped this to Teams/Microsoft only for now ("one at a time"), deferring Zoom/Google Meet equivalents to his own separate research.
- 2026-08-07 (real design gap found via FirstPrinciples, resolved before it became a silent bug): a presence-triggered ad-hoc recording has no calendar end-time to auto-stop against, unlike ISC-181's calendar-triggered case — deconstructed the assumption that auto-stop requires a pre-known timestamp and found it false: presence is bidirectional, so the same poll that detects entering a call state also detects leaving one, requiring zero new mechanism, just checking both directions of the same signal. This required extending `AutoRecordingMarker`'s shape (previously a single `DateTime<Utc>` field, ISC-181) into an enum (`CalendarEnd(DateTime<Utc>)` vs `PresenceBased`) so the two independent stop mechanisms (calendar-end-time vs. presence-leaving) never cross-trigger against the wrong recording's marker — a real structural change flagged explicitly (ISC-233) rather than silently reusing the old shape in a way that would have broken ISC-181's existing tests or logic.
- 2026-08-07 (real field-mapping correction found via direct source comparison, before it could mislead implementation): Microsoft's own `/graph/api/resources/presence` reference page's property table has `activity` and `availability`'s possible-value lists SWAPPED relative to the actual example JSON responses shown on the sibling `/graph/api/presence-get` page (e.g. a real response pairs `"availability": "DoNotDisturb"` with `"activity": "Presenting"` — proving the richer states like `inACall`/`inAMeeting`/`Presenting` live in `activity`, not `availability`, contradicting that one table). Resolved by trusting the real example payloads (ground truth) over the prose table (apparently a documentation-rendering artifact) before writing ISC-224's field-name claim — the kind of doc-fetch inconsistency this project has hit before (Advisor `--auto-state` pulling wrong-system context; a RootCauseAnalysis fork's incorrect `hound::flush()` claim) and resolved the same way each time: verify against the most concrete evidence available, don't average two conflicting sources.
- 2026-08-07 (Engineer delivered TeamsPresenceAdhocRecording + RecordingIndicatorOverlay — 171/171 tests, independently re-verified by Kai): read the actual diff (`presence.rs`, `RecordingBadge.tsx`/`.css`, `lib.rs`, `auto_join.rs`, `calendar.rs`, `capabilities/default.json`, `main.tsx`, `RecordingControl.tsx`, `CalendarSettings.tsx`) rather than trusting the self-report alone. `AutoStopTrigger` was placed in `auto_join.rs` rather than `lib.rs` as originally spec'd — a real, disclosed, correct deviation: both `auto_join` and `presence` reason about it in pure/tested code, and neither should reach into the Tauri-bound crate root to do so. Two real bugs found and fixed by Engineer during its own build, neither catchable by the test suite: (1) `capabilities/default.json` scoped `invoke()` to `"windows": ["main"]` only, which would have made the overlay render and then freeze at `00:00` forever — every command call from the badge window would have been silently denied by Tauri's own runtime permission system, a class of bug no Rust or TypeScript test exercises. (2) A hardcoded overlay position would have misplaced the badge off-screen on any display narrower than the one it was tuned for — fixed with a pure, unit-tested function computed from the real primary-monitor size instead. Caught Kai's own bulk-edit mistake immediately after: a Python script marking ISC-224 through ISC-244 done incorrectly included ISC-227, which explicitly requires Jeremiah's own live Meet Now test and had not happened — corrected back to `[DEFERRED-VERIFY]` before this entry was written, not after.
- 2026-08-07 (real open design question, disclosed by Engineer, not silently resolved either way): the presence poller gates only on Microsoft being connected — it does NOT check the `auto_join_enabled` toggle (labeled "Auto-join & record calendar meetings" in the UI, explicitly calendar-scoped in its own wording). This means unticking that toggle stops calendar-triggered auto-recording but leaves ad-hoc Teams-call detection running. Defensible as shipped (the toggle's own label is calendar-specific; Jeremiah asked for presence detection as an explicitly separate capability the same session, already accepting silent operation for it specifically, conditioned on the now-built overlay indicator) — but plausibly not what a user would assume "auto-join" means if read as "all auto-recording." Flagged to Jeremiah directly rather than picked silently; a future separate toggle for presence-based detection is the clean fix if he wants independent control, deferred pending his real-world read of the current behavior during first live test.
- 2026-08-07 (real regression, root-caused and fixed same-session — a CSS global-scope leak broke the main window's theme): Jeremiah reported the main app's background had gone all-white with illegible tabs immediately after this round's build. Root cause: `RecordingBadge.css` styled the overlay window with a bare `html, body, #root { background: transparent; ... }` selector — since both Tauri windows load the identical compiled CSS bundle (confirmed in `main.tsx`'s own doc comment), this unscoped rule matched inside the MAIN window's document too, silently overriding `tokens.css`'s real theme background (`body { background: var(--color-bg); ... }`) with `transparent`, which rendered as a blank/white fallback behind light-on-dark theme text. Fixed by scoping the override to `html.recording-badge-window` and adding/removing that class on `<html>` from within `RecordingBadge.tsx`'s own mount effect — since each Tauri window is a genuinely separate `document` (not a shared DOM), a class set only inside the badge window's own React tree can never leak into the main window's document, closing the leak at its actual source rather than just patching the symptom. Verified via Vite HMR log (`hmr update /src/components/RecordingBadge.tsx` then `.css`) and a fresh `cargo test --lib` (171/171 unaffected — this was a pure frontend CSS-scoping bug, no backend logic involved).
- 2026-08-07 (Jeremiah's direct response to the auto-join-toggle scope question, driving both a quick text fix and RecordingTriggerProvenance): rather than change the toggle's actual behavior, Jeremiah asked to keep it as-is but make its real boundary explicit in its own hint text (fixed directly, `CalendarSettings.tsx`'s auto-join hint now states plainly that it only governs scheduled meetings and that ad-hoc Teams detection runs regardless). He also asked for something more durable than UI copy: a permanent, per-meeting record of HOW each recording was triggered, so a past meeting's capture method is never ambiguous "for analysis moving forward." Interpretation adopted (stated here so it can be corrected if wrong): the trigger source is displayed as UI metadata next to the title/summary in `MeetingDetail.tsx` (and a compact indicator in `MeetingLibrary.tsx`'s list), NOT injected into the LLM-generated summary text itself — baking it into model-generated prose would make the signal fragile (dependent on the summarization prompt/model rather than a real, structured DB field) and would blur operational metadata into content meant for a different purpose. A distinct `Recovered` value (not a guess at which of Manual/Calendar/Presence a crash-orphaned recording originally was) is used for orphan-recovered meetings, since that information genuinely did not survive the crash — honest uncertainty, not a fabricated best-guess.
- 2026-08-07 (real gap found live, fixed directly rather than delegated — CalendarDisconnectButton): Jeremiah needed to re-consent Microsoft for the new `Presence.Read` scope and discovered there was no way to disconnect/reconnect from the UI at all — Kai had to manually delete a Keychain entry to unblock him. Root cause was one layer deeper than "the UI is missing a button": neither `oauth.rs` nor its underlying `keychain::delete_secret` had a real, non-test production delete path — both were `#[cfg(test)]`-gated, existing only so tests could reset Keychain state between runs. Fixed by promoting `keychain::delete_secret` to real production code and adding a real `oauth::delete_tokens` on top of it (the previous `delete_tokens_for_test` now just calls the real function, removing duplication rather than leaving two copies), a new `disconnect_provider` Tauri command, and a Disconnect button per provider in `CalendarSettings.tsx` — deliberately forgetting only the stored tokens, not the client ID, so reconnecting is just the browser consent flow again, not re-pasting an Azure/Google/Zoom client ID that hasn't changed. Built directly by Kai (small, well-scoped, no Engineer round-trip needed) — 175/175 tests passing, independently re-verified, before Jeremiah's live Meet Now test.
- 2026-08-07 (real bug found and fixed, unrelated to recording logic — leftover system audio devices from the OLD notetaker): Jeremiah's live Meet Now test succeeded (presence detection, silent auto-start, overlay badge all confirmed working), but during a hands-on audio test he found his own voice was captured whether Teams reported him muted or not — the actual finding that opened the entire mute/audio-scope investigation below. Separately, while investigating unrelated AirPods instability and call echo he'd been experiencing, `system_profiler`/the real `com.apple.audio.SystemSettings.plist` confirmed two leftover virtual audio devices — a `Multi-Output Device` literally named `com.pai.meetingnotetaker.multioutput` (bridging AirPods with BlackHole) and a generic `Aggregate Device` (bridging the built-in mic + built-in speaker + BlackHole) — both dead infrastructure from the OLD, retired TypeScript notetaker's BlackHole-based capture architecture, never cleaned up. kai-notetaker's current Rust capture (`cpal::default_input_device()`) has zero use for either. Jeremiah removed both via Audio MIDI Setup — this real, separate problem is resolved and confirmed independent of anything in kai-notetaker's own code.
- 2026-08-07 (PAUSED — extensive research into correct call-audio capture, no code shipped, Jeremiah's explicit call to stop here): Jeremiah's real requirement — "the recording should be whatever is on the actual meeting and nothing outside of it," specifically his own voice matching his real Teams mute state — was investigated exhaustively across five real technical avenues, each with a definitive, evidence-backed outcome (not abandoned from lack of trying):
  1. **Raw mic capture (current, shipped behavior)**: mute-blind by construction — Teams' own mute is an application-internal gate the OS never exposes, confirmed independently by this session's own research AND a prior 2026-08-03 finding on the old system (two automatic approaches, Teams' own mute and macOS's `kAudioDevicePropertyMute`, already ruled out empirically back then).
  2. **Core Audio Process Tap** (macOS 14.4+, native, zero downloads, via the `corti-coreaudio` Rust crate): CONFIRMED via research to solve the "everyone else on the call" half completely and correctly (Teams never locally echoes the user's own voice, so tapping Teams' own output naturally excludes it) — genuinely buildable, never built (paused before implementation).
  3. **Per-process mic-in-use attribution** (`kAudioDevicePropertyDeviceIsRunningSomewhere` + `kAudioHardwarePropertyProcessObjectList`, also via `corti-coreaudio`): investigated as a possible mute-signal proxy, CONCLUSIVELY ruled out — the `corti` project's own real-world source code explicitly documents "a muted Zoom still reads as the owner," confirming conferencing apps keep the OS-level mic stream open continuously regardless of their own mute state.
  4. **Teams' own native cloud recording**: would solve both halves correctly (server-side, reflects real transmitted media) but cannot be triggered programmatically — Graph's `onlineMeeting.recordAutomatically` only applies at meeting creation with no live start/stop action, confirmed against the real API resource schema.
  5. **Compliance Recording Bot**: the one path that would have worked completely and correctly. Real findings, each verified rather than assumed: no Microsoft ISV certification required for KCG-internal-only use (only for reselling to other tenants); licensing requires the M365 license only on the policy-holder (Jeremiah), not on external call participants — resolving his stated licensing concern in his favor; but the underlying real-time media SDK is confirmed **Windows Server-only for runtime execution, no Linux/macOS support** — meaning Jeremiah's existing Ubuntu-based Hostinger VPS (`ubuntu.vps.ciso.assistant`, running CISO Assistant) cannot host it, a new Windows machine and a separate .NET/C# service would be required, with real recurring cloud-hosting cost (~$25-45/month estimated for light internal use, not a quoted figure). Jeremiah's own words at the pause point: "I don't like this at all anymore" — the new-infrastructure/cost/new-tech-stack requirement, once concretely quantified, changed his own calculus. This is a real, informed no, not a stalled investigation.
  
  **Nothing was implemented for any of these five** — the shipped app's actual behavior remains raw mic capture, mute-blind, exactly as it was before this investigation began. Follow-up, whenever Jeremiah returns to this: the Process Tap (#2) remains a real, ready-to-build, zero-cost, zero-new-infrastructure option that solves half the problem cleanly on its own; the only remaining option for his own voice, given every automatic path is now confirmed closed, is a kai-notetaker-controlled pause (hotkey or a click on the existing overlay badge) that he'd operate himself — not yet decided, not yet built.
- 2026-08-07 (MeetingTitleStandardization delegated to Engineer, independently verified by Kai — not trusted from self-report alone): Jeremiah's request — real calendar/Teams titles carry through immediately, a deterministic naming convention replaces the old LLM-summary-snippet fallback for everything else — landed clean against spec. The Engineer's own build surfaced one real latent bug the spec itself would have shipped: `mark_meeting_processing` used to clear `title` to `NULL` on every reprocess, which was harmless only because `mark_meeting_ready` used to re-derive a title from the LLM summary afterward. Removing that re-derivation (the whole point of ISC-259) without also un-coupling this reset would have permanently stranded any reprocessed meeting — including one Jeremiah had renamed by hand — on `"Processing…"` forever, a direct ISC-265 violation. Fixed in the same round, guarded by a new regression test (`reprocessing_a_meeting_does_not_wipe_its_title`). `cargo test --lib` → 187 passed (baseline 175, +12 new), `bunx tsc --noEmit` → exit 0, both independently re-run by Kai, not just accepted from the report.
- 2026-08-07 (Kai's own follow-on fix, decided directly rather than presented as an open question — per Jeremiah's standing "stop presenting options, just execute" instruction after the audio-scope pause above): the Engineer flagged, correctly, that `fallback_title` formatted the UTC `created_at` verbatim with no local-time conversion — meaning a 2:34 PM Central ad-hoc call would title itself `"...7:34 PM"` while `MeetingLibrary.tsx`'s own date column (which does convert to local) showed 2:34 PM on the very same row. That is exactly the "titles don't match what actually happened on the call" annoyance this whole feature exists to fix, so it wasn't treated as a stylistic preference — it's a correctness bug, decided and fixed directly: `fallback_title` now converts the stored UTC instant to the system's local time before formatting (`chrono::DateTime::<Utc>::from_naive_utc_and_offset(...).with_timezone(&Local)`), while `meetings.created_at` itself stays UTC (unchanged storage format, so nothing else downstream is affected). This makes the four exact-string unit tests machine-dependent by nature (a local-time stamp can't be a hardcoded literal across arbitrary test machines), so they were rewritten to independently re-derive the expected local stamp via a structurally distinct conversion call rather than calling `fallback_title` itself — still catches a wrong label or a dropped em-dash, just not a hardcoded clock string. Also fixed in the same pass: `examples/import_legacy_recordings.rs`, flagged by the Engineer as already broken pre-existing (missing the `trigger_source`/`known_title` args `create_meeting` has required since ISC-246/ISC-258) — given `TriggerSource::Manual` (a deliberately-run import script, not a crash recovery) and the loop's own `stem` as `known_title`, since a real filename-derived name is a better title than any generic fallback for legacy-imported audio. `cargo build --example import_legacy_recordings` now finishes clean.
- 2026-08-07 (`bun tauri build` — the first real release build, and Jeremiah's first normal Cmd+Q of the actual installed app): the release build itself hit an unrelated native-toolchain break first — `llama-cpp-sys-2` and `whisper-rs-sys` both vendor ggml's own CMake build, which leaves `CMAKE_OSX_DEPLOYMENT_TARGET` unset; on this machine's very new Command Line Tools SDK that made `std::filesystem::path` (used by `ggml-backend-reg.cpp`'s `fs::u8path` call) unavailable, aborting `cargo build --release` — never hit under `tauri dev`'s debug profile, only forced by a from-scratch release cmake configure. Root-caused two layers deep: `MACOSX_DEPLOYMENT_TARGET` alone never reaches either build.rs (both only forward `CMAKE_`-prefixed env vars to cmake), and a stale `CMakeCache.txt` from the first failed attempt kept re-pinning `10.13` even after the correctly-named var was set, since both build.rs scripts call `.always_configure(false)`. Fixed permanently via `CMAKE_OSX_DEPLOYMENT_TARGET = "13.0"` in `src-tauri/.cargo/config.toml`'s `[env]` block, after clearing the stale cache dirs once. `bun tauri build` then succeeded end-to-end: `kai-notetaker.app` + `.dmg` bundled, copied to `/Applications`.
- Within a minute of that first real install, Jeremiah reported a genuine crash on normal quit — a full macOS crash report, `EXC_CRASH (SIGABRT)`, `Terminating Process: kai-notetaker`, main thread inside `-[NSApplication terminate:]` → `exit()` → `ggml_metal_device_free` → `ggml_metal_rsets_free` → `ggml_abort`, while a second thread was mid-`__ggml_metal_rsets_init_block_invoke` (a `usleep`-based retry loop). Root cause, confirmed by reading `spawn_engine_loading`: the four heavy models (Whisper/diarization/LLM/embeddings, all Metal-backed) load on a fully fire-and-forget background `std::thread::spawn` at startup with no `JoinHandle` kept anywhere — nothing in the app ever knew whether that thread was still running. Under `tauri dev`, the process was always killed by the file-watcher/Ctrl+C, never through a real `NSApplication terminate:` — so this exact atexit path had never actually run before tonight's first genuine Cmd+Q of a real installed app, 44 seconds after a cold launch, plausibly right in the middle of the 8B LLM's first Metal load. Not a bug introduced by anything built this session — a pre-existing race in the app's startup lifecycle, newly exposed by exercising a code path (`tauri dev` never exercised) for the first time. Fixed via a new `EngineLoadHandle` state that captures the loading thread's real `JoinHandle`, plus a `RunEvent::ExitRequested` handler that calls `api.prevent_exit()` and joins that handle on a second thread before calling `app_handle.exit(0)` for real, whenever `engine_load_still_in_progress` (a small, independently unit-tested pure function) reports the load is still running. The common case (already finished loading) costs one non-blocking `is_finished()` check and nothing else. `cargo test --lib`: 190 passed (baseline 187, +3 new), one transient failure (`oauth::tests::await_authorization_code_times_out_if_nothing_connects`) seen once under heavy concurrent system load from an unrelated stale background process being killed mid-run, confirmed a pre-existing flake — not caused by this fix — by passing in isolation and then passing again in a fully clean re-run (190/190). `bunx tsc --noEmit`: exit 0.
- 2026-08-07 (MicMuteToggle — Phase 1 of the paused call-audio-scope investigation, reframed by Jeremiah into something genuinely tractable): rather than re-litigating the five already-exhausted avenues, Jeremiah proposed a new idea — a manual mute button for kai-notetaker's OWN capture (so a sidebar conversation with his wife/kids isn't recorded), built first standalone, with an explicit Phase 2 in mind (auto-tying it to Teams/Zoom/Meet's own mute). Kai's key reframe, given directly rather than as a menu of options (per the standing fatigue lesson): don't try to READ the call app's internal mute state — already conclusively proven unreadable from the OS. Instead make kai-notetaker the thing that SENDS the mute command, via `CGEventPostToPid` targeting the call app's process directly (not "whatever's focused") — sidesteps the entire dead end. Jeremiah confirmed Phase 1 and asked specifically whether the hotkey would be truly global regardless of focus; clarified this cleanly separates into two independent claims — the hotkey's INPUT side is trivially global (any registered system-wide shortcut fires regardless of focus, never in question), while the earlier caveat only applied to the Phase 2 OUTPUT side (sending a keystroke INTO the call app), which `CGEventPostToPid` also resolves. Phase 1 itself needs neither mechanism — it only toggles kai-notetaker's own capture, no synthetic keystrokes into any other app at all.
- Delegated to Engineer with a precise spec (ISC-271–278), independently re-verified by Kai line-by-line, not accepted from self-report: `git diff` read in full across `audio_capture.rs`/`lib.rs`/`Cargo.toml`/`RecordingBadge.tsx`/`RecordingControl.tsx`/new `MicMuteIcon.tsx`+`useMicMute.ts`; the `Modifiers::META` → `SUPER` (Command) normalization claim independently re-checked against the real installed `global-hotkey` 0.8.0 source (`hotkey.rs:89-91`), not taken on faith. Two real refinements beyond the original spec, both correct and both disclosed by the Engineer rather than hidden: (1) the resampled branch gates the resampler's OUTPUT, not its input — gating the input would still leak a few ms of real audio through the sinc filter's ~128-tap history; (2) the `RecordingBadge` overlay window is created once at startup and only shown/hidden, never unmounted, so without an explicit fix it would display the PREVIOUS recording's stale muted state the instant a new one starts — fixed by emitting `mic-mute-changed: false` from `start_recording` itself, a gap the original spec's ISC-277 named for the backend but not the UI. `cargo test --lib`: 195 passed (baseline 190, +5 new), independently re-run by Kai. `bunx tsc --noEmit`: exit 0.
- The Engineer's own mutation-testing catch, worth recording as a real methodology lesson for this project: its first ISC-272 test asserted "all samples captured after a device switch are zero" as proof mute survived the switch — then, testing its own test, deliberately broke `switch_device` to hand the new stream an unrelated flag and found the test STILL PASSED, because a real microphone in a quiet room emits sample values of exactly `0.0`, making "all zero" indistinguishable from "correctly muted." Fixed by adding `Arc::strong_count(&session.muted) == 2` as the actual discriminator (2 = session field + one live stream; a broken implementation drops to 1) — a check that needs no ambient sound at all. Re-verified by Kai as a real, sound test design, not just accepted on description.
- 2026-08-07 (real bug found from Jeremiah's own hands-on test of MicMuteToggle, root-caused by Kai directly rather than guessed at): Jeremiah tested mute/unmute mid-recording and reported "no audio playback at all... for when I muted and when I didn't mute" — read literally and verified against the actual WAV file rather than assumed: `python3`'s `wave` module confirmed all 2,896,896 samples across the full 60.35s recording were exactly `0` (`nonzero_fraction: 0.0`), including the UNMUTED spans. Since `gate_muted` only zeros samples when `muted` is true, an all-zero recording across BOTH states meant the microphone contributed nothing at all — a total capture failure, not a mute-logic bug. Root cause, confirmed by directly querying `~/Library/Application Support/com.apple.TCC/TCC.db`: `com.kairoscompliance.kainotetaker` had **no row at all** under `kTCCServiceMicrophone` — not merely denied, never even decided — while `com.apple.Terminal` (which launches `bun tauri dev`) already had a real grant. Every prior "real audio" test this entire project has ever passed was riding on Terminal's OWN microphone permission, because `tauri dev` runs the raw unbundled executable directly, not inside a proper `.app`. Tonight's `tauri build` was the very first time kai-notetaker ever ran as its own bundled, independently-permissioned process — and its `Info.plist` was missing `NSMicrophoneUsageDescription` entirely (confirmed via `PlistBuddy -c "Print :NSMicrophoneUsageDescription"` → "Does Not Exist"), which is why macOS never even showed a permission prompt: without that key, TCC silently denies rather than asking. Not a regression from MicMuteToggle, the crash fix, or the deployment-target fix — a pre-existing gap in the project's Tauri bundling config, invisible for the entire multi-week dev-mode testing history and only exposed by tonight's first real bundled launches. Fixed by adding `src-tauri/Info.plist` (a project-root fragment file Tauri's bundler merges into the generated bundle's `Info.plist` — verified empirically, not just from documentation memory: added the key, ran a real `bun tauri build`, then re-ran `PlistBuddy` against the actual output bundle and confirmed the string landed exactly as written). Rebuilt and reinstalled to `/Applications`; the next real recording should trigger a genuine, real macOS microphone permission dialog for kai-notetaker for the first time ever — Jeremiah needs to click Allow.
- 2026-08-07 (TeamsCallMuteSync — Phase 2, triggered by Jeremiah's real live call with Paula): Jeremiah tested MicMuteToggle on an actual Teams call and reported the hotkey didn't mute him on the call itself — only Teams' own button did — and one further observation that read as inconsistent (hotkey-muted-with-Teams-unmuted sometimes seemed to still be heard, sometimes not). Investigated the apparent inconsistency directly rather than assumed: read `global-hotkey` 0.8.0's actual macOS backend and confirmed Cmd+Option+M is registered SOLELY via Carbon's `RegisterEventHotKey`, which macOS itself guarantees intercepts the keystroke before it reaches any focused app's normal dispatch (the crate's own `CGEventTapCreate` usage is a completely separate code path, `start_watching_media_keys`, gated to `CGEventType::SystemDefined` media keys and never invoked for our registration) — so the hotkey provably could not have reached Teams directly; the apparent inconsistency was almost certainly Teams' own mute state getting crossed with kai-notetaker's during a real live conversation, not a code leak. Jeremiah's actual ask — "the hotkey should be able to control the mute key from the teams call as well" — is exactly Phase 2, already scoped in principle during MicMuteToggle's planning (make kai-notetaker SEND Teams' own mute command via `CGEventPostToPid`, rather than trying to READ Teams' already-proven-unreadable internal state).
- Delegated to Engineer twice: the first run did real, substantial work (confirmed Teams' real bundle id/executable via the actual installed `/Applications/Microsoft Teams.app/Contents/Info.plist`, confirmed `core-graphics` 0.25.0's real API surface against its installed source, wrote `call_mute.rs` and the `lib.rs` integration) but hit an API session-usage limit mid-task before delivering a final report — the agent's own worktree wasn't preserved (isolation cleans up if it judges no changes were made, or the run ended before that check), but its actual file edits had already landed in the MAIN working tree, recovered and reviewed there rather than re-built from scratch. Independently re-verified by Kai line-by-line exactly as every other delegated feature this session: read `call_mute.rs` and the `lib.rs` diff in full, confirmed both real API-level discoveries the Engineer made beyond the original spec (documented in ISC-279/ISC-280 above) against the actual crate sources itself before accepting either claim. `cargo test --lib`: 199 passed (baseline 195, +4 new), independently re-run by Kai. `bunx tsc --noEmit`: exit 0 (backend-only feature, zero frontend changes).
- 2026-08-07 (real gap found from Jeremiah's own forward-looking question, fixed directly by Kai — no ISC numbers added, treated as a bug fix to existing calendar parsing rather than a new feature block, matching the earlier CMAKE_OSX_DEPLOYMENT_TARGET precedent): after TeamsCallMuteSync shipped, Jeremiah asked whether Google Meet invites sent to jeremiah@kairoscompliance.com could trigger auto-recording the same way Teams meetings do. Verified rather than assumed which mailbox that domain actually is: `dig +short MX kairoscompliance.com` resolved to `kairoscompliance-com.mail.protection.outlook.com` — a real, live DNS lookup confirming it's Microsoft 365, not Google Workspace, so `jeremiah@kairoscompliance.com` cannot itself be a Google OAuth account, and any Google Meet invite sent to it lands on his Outlook calendar as plain text, not a native Teams meeting. Read `calendar.rs`'s existing Graph event parsing directly and confirmed the real gap: `join_url` was only ever read from `onlineMeeting.joinUrl` (populated exclusively by Outlook's own "Teams Meeting" toggle) with zero fallback — `auto_join.rs` already had an explicit pre-existing test comment acknowledging the identical gap for Zoom links in event bodies ("invisible to v1 by design"), confirming this was a known, deliberately-deferred limitation rather than an oversight. `auto_join::decide()` requires a non-empty `join_url` to trigger at all (`Decision::SkipNoJoinUrl` otherwise), so without this fix, a Google Meet invite on his Outlook calendar would silently never auto-record or auto-open, no matter how it was configured.
- Fixed by adding a fallback text scan of the event's `body.content`/`location.displayName` (both already returned by Graph today — no request changes needed) for a `meet.google.com` or `zoom.us` link, via a new pure `extract_fallback_join_url` function, only consulted when the native Teams field is absent — the native field always wins when present, since it's structured data about a real Teams meeting and strictly more trustworthy than a regex match against free text (verified by a dedicated test: a native `joinUrl` present alongside an unrelated Meet link in the body still resolves to the Teams link). Two real correctness issues found and fixed during Kai's own build, both verified empirically with a throwaway compiled probe rather than assumed from reading the regex alone: (1) the zoom-matching pattern's first draft used a permissive character class (`[a-zA-Z0-9.-]*zoom\.us`) that incorrectly matched `https://notzoom.us/...` as a Zoom link — confirmed via a real compiled test, then fixed by requiring each subdomain label to end in an explicit dot (`(?:[a-zA-Z0-9-]+\.)*zoom\.us`), re-verified to still match a real regional subdomain like `us02web.zoom.us` while correctly rejecting `notzoom.us`; (2) an initial assumption that a tracking `?hs=...` query parameter should be stripped from the matched URL was wrong and deliberately reversed — Zoom links often carry a load-bearing `?pwd=...` passcode that a bare meeting ID cannot join without prompting for a password, so the extractor keeps query strings intact rather than trimming at `?`, verified against the actual match output of a compiled probe before writing the final test assertions (an earlier draft test asserted the wrong, stripped value and was corrected before it ever ran). Once a fallback join link is found, it flows through the exact same pipeline as a native Teams meeting with zero additional changes — the same auto-start-recording trigger and the same `open::that(join_url)` auto-open-in-browser behavior at the scheduled time. `cargo test --lib`: 208 passed (baseline 199, +9 new — 8 in `calendar.rs` plus one adjusted assertion in the pre-existing response-count test), independently re-run by Kai. `bunx tsc --noEmit`: exit 0 (backend-only change).
- 2026-08-07 (SECOND real QuitCrashRace, same day — the original fix was incomplete, not wrong): Jeremiah sent a full macOS crash report identical in signature to the first — `EXC_CRASH (SIGABRT)`, main thread inside `-[NSApplication terminate:]` → `exit()` → `ggml_metal_device_free` → `ggml_metal_rsets_free` → `ggml_abort`, a second thread mid-`__ggml_metal_rsets_init_block_invoke` — but this time over an hour into the session (launch 19:29:26, crash 20:43:23), well outside the "still loading models at startup" window `EngineLoadHandle` was built to guard. Root-caused directly by grepping every `std::thread::spawn` call site in `lib.rs`: THREE separate fire-and-forget threads touch `pipeline::process_meeting` (the same Metal-backed inference call path as engine loading) with no handle tracked anywhere — `stop_recording`'s pipeline kickoff, `reprocess_meeting_with_speaker_count`, and orphan recovery's per-meeting processing thread. `EngineLoadHandle` only ever had visibility into the ONE thread that runs once at startup; it never had any way to know about these three, which run every single time a meeting is recorded, reprocessed, or recovered from a crash. The real trigger mechanism, reasoned through rather than assumed: ggml's Metal backend appears to lazily build resource sets sized for the actual workload on a context's FIRST real inference call, not at context-construction time — meaning this race can recur any time real transcription/summarization work is happening in the background, not only during the initial model preload.
- Fixed by generalizing rather than patching the same spot again: a new `ActiveGpuWork(Arc<AtomicUsize>)` counter plus an RAII `GpuWorkGuard` (increment on creation, decrement on `Drop` — including during a panic or an early `return`, so a thread that hits a real error mid-pipeline can never leave the counter stuck above zero) that all three previously-untracked threads now acquire as the very first line inside their spawned closure. The exit handler was extended (not replaced) to check both `EngineLoadHandle` AND this new counter, `prevent_exit()`-ing and waiting for both to clear before calling `app_handle.exit(0)` for real — `EngineLoadHandle` itself is untouched, since it already correctly covers its one case. Deliberately did NOT unify everything into one mechanism: `EngineLoadHandle`'s join-based tracking was already correct and independently tested, and rewriting working, verified code to chase symmetry would have been risk for its own sake. Five new tests prove the guard's actual failure-isolation properties directly rather than only the happy path: multiple concurrent guards are counted independently (not just "something is running" as a boolean), the counter reaches zero on a normal drop, on an early `return` past a simulated DB-open failure (mirroring the real early-return branches in all three fixed call sites), and — run inside a real spawned thread so the panic doesn't fail the test process itself — even when the guard's own thread panics mid-pipeline, proving `Drop` still runs during unwinding. `cargo test --lib`: 213 passed (baseline 208, +5 new), independently re-run by Kai from a clean shell (one run hit a genuine tooling stall — near-zero CPU for 11+ minutes, unrelated to this change — killed and re-run clean rather than accepted or debugged further; a separate run hit the same pre-existing `oauth` flake already characterized earlier this session, confirmed unrelated by passing in isolation, then a fully clean 213/0 run). `bunx tsc --noEmit`: exit 0 (backend-only change).

## Changelog

- 2026-08-05 | conjectured: the model-download URLs for the installer could be reconstructed from memory/assumption (well-known filenames like `ggml-base.bin`, `Meta-Llama-3.1-8B-Instruct-Q4_K_M.gguf`)
  refuted by: multiple different HuggingFace repos publish files with the exact same filename but different byte content (e.g. `lmstudio-community` vs `bartowski`'s `Meta-Llama-3.1-8B-Instruct-Q4_K_M.gguf` differ by 64 bytes) — a filename match is not a content match
  learned: for any model file already on disk, get the exact local byte size (`stat -f%z`) and confirm it against a `curl -sIL` HEAD request's `content-length` on the candidate URL before trusting that URL for an installer — filename-only matching is not sufficient verification for binary provenance
  criterion now: all 5 installer download URLs (Whisper, sherpa-onnx segmentation + speaker-embedding, Llama-3.1-8B-Instruct GGUF, bge-small-en GGUF) were confirmed via exact byte-size match against the real files already on this dev machine, not assumed from the filename alone

- 2026-08-05 | conjectured: writing the DB deletion first and the audit-log entry second is a safe, natural order for `retention_sweep` (mirrors "do the work, then log it")
  refuted by: commitment-boundary advisor call pointed out that since the audit log and SQLCipher DB are separate storage primitives with no shared transaction, a crash between the two calls in that order produces a silently unlogged deletion — the exact failure mode the audit log exists to prevent
  learned: when two independent storage writes can't be made atomic, order them so the failure mode is "over-logged" (a claimed action that didn't fully complete, visible and reconcilable) rather than "under-logged" (a real action with zero record, invisible) — this generalizes beyond retention to any future dual-write path (e.g. a future export/delete feature)
  criterion now: ISC-24 (retention deletion triggers an audit append) is unchanged in wording but its implementation now writes audit-before-delete; a future ISC should be added once StorageLayer exists to test actual crash-recovery/reconciliation behavior, not just that both writes eventually happen

- 2026-08-05 | conjectured: setting `n_ctx` on `LlamaContextParams` is sufficient to let `complete()` handle any prompt up to that many tokens, since the code already rejects longer prompts via `PromptTooLong`
  refuted by: a real ~43-minute meeting transcript's chunk-summary prompt exceeded 2048 tokens (still well under the 4096 `n_ctx` used) and aborted the entire process with `GGML_ASSERT(n_tokens_all <= cparams.n_batch)` — `n_batch` (max tokens per single decode() call) is an independent parameter that `LlamaContextParams::default()` sets to 2048 regardless of `n_ctx`, and `complete()` submits the whole prompt as one batch
  learned: `n_ctx` (total context window) and `n_batch` (max tokens processed per decode call) are two different llama.cpp parameters that must be reasoned about separately — a prompt-length guard against `n_ctx` alone does not protect against an `n_batch` mismatch, and this class of crash is a hard process abort (SIGABRT via `GGML_ASSERT`), not a catchable Rust `Result` or panic, so no amount of `Result`-based error handling downstream would have caught it
  criterion now: `complete()` now calls `.with_n_batch(n_ctx)` so a single call's batch is always sized to match the context window it was configured with; a new regression test proves a ~2500-token prompt (over the old 2048 default) completes successfully

- 2026-08-06 | conjectured: `no_context: true` (stop carrying the previous window's transcribed text forward as decoding context) would fix a real 43-minute meeting's transcript degenerating into one hallucinated sentence repeated across 94% of its segments, since the mechanism looked like early hallucination compounding via context bias
  refuted by: a real reprocessing run with ONLY that change applied produced byte-for-byte identical output — same 1579 segments, same 1481 repeats of the same sentence. Reading whisper.cpp's actual decode loop (not assumed) showed its one built-in anti-repetition gate (`entropy_thold`) only measures token diversity WITHIN a single ~30s window's own decode — a fluent, grammatically normal sentence recurring ACROSS many separate windows (driven by a long stretch of acoustically-similar background noise) never trips it, since each individual window's decode isn't internally degenerate
  learned: whisper.cpp has two structurally different anti-hallucination mechanisms that address different failure classes — `entropy_thold`/temperature-fallback for within-window repetition, and the separate `is_no_speech` gate (`no_speech_prob` + `logprob_thold`) for suppressing text on likely-non-speech audio. Fixing the wrong one produces a real, measurable negative result (identical output), not a partial improvement — worth verifying which mechanism actually applies before assuming a fix landed
  criterion now: ISC-123 (anti-criterion) records the `no_context`-only negative result explicitly; ISC-122 is satisfied by the actual fix (`logprob_thold: -0.5`), verified via a second real reprocessing run — segment count 1579 → 338, zero repeated-phrase pattern

## Verification

- **QuitCrashRace generalization (second real crash, same signature, root-caused to three additional untracked threads):** `grep -n "std::thread::spawn" src/lib.rs` before the fix enumerated every spawn site and confirmed exactly three (`stop_recording`, `reprocess_meeting_with_speaker_count`, orphan recovery) called `pipeline::process_meeting` with no `JoinHandle` or any tracking mechanism at all — the same category of gap `EngineLoadHandle` closed for the one startup thread, just never extended to these. Read `lib.rs`'s diff in full after the fix: confirmed `ActiveGpuWork`/`GpuWorkGuard`/`begin_gpu_work` are used identically at all three sites (guard acquired as the literal first line inside each spawned closure), the exit handler checks both `EngineLoadHandle` and the new counter before `prevent_exit()`, and `EngineLoadHandle` itself was left untouched rather than rewritten for symmetry. `cargo build --lib` → clean, zero new warnings. `cargo test --lib active_gpu_work_tests::` → all 5 new tests `ok`, including the panic-during-unwind case. Full suite: `test result: ok. 213 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 140.90s` (baseline 208, +5 new), independently re-run by Kai from a clean shell after killing one earlier run that stalled for an unrelated reason (near-zero CPU for 11+ minutes — a real tooling hang, not a test failure) and confirming a second run's one failure (`oauth::tests::await_authorization_code_times_out_if_nothing_connects`) was the same pre-existing flake already characterized earlier this session, by passing in isolation. `bunx tsc --noEmit` → exit 0 (backend-only change).
- **Google Meet/Zoom fallback join-link detection, built directly by Kai from Jeremiah's own forward-looking question:** `dig +short MX kairoscompliance.com` → `kairoscompliance-com.mail.protection.outlook.com`, a real live DNS query confirming Microsoft 365 hosting before any code was written. `grep -n "fn join_url\|onlineMeeting"` on `calendar.rs` (before the fix) confirmed the real gap: only `onlineMeeting.joinUrl` was ever read. Two regex correctness bugs caught and fixed via a real compiled probe (`rustc` against the actual linked `regex` crate, not reasoned about on paper alone): a permissive character class matching `notzoom.us` as a false positive (fixed with an explicit subdomain-label-dot requirement, re-verified against both the negative case and a real regional Zoom subdomain), and an incorrect assumption that `?query=` strings should be stripped (reversed after recognizing Zoom's `?pwd=...` passcode parameter is often load-bearing — verified the actual match output before finalizing test assertions, catching and fixing two test expectations that were wrong under the original, incorrect assumption). `cargo test --lib calendar::` → 14/14 passed in isolation. Full suite: `test result: ok. 208 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 294.22s` (baseline 199, +9 new). `bunx tsc --noEmit` → exit 0.
- **TeamsCallMuteSync, recovered from a session-limited Engineer run and independently re-verified by Kai line-by-line (not accepted from an incomplete self-report):** `git diff --stat` confirmed the exact file list (new `call_mute.rs`, `lib.rs`, `Cargo.toml`, `Cargo.lock`). Read `call_mute.rs` in full — `find_running_pid`/`post_key_combo_to_pid`/`accessibility_permission_granted` all match ISC-279/280/282 exactly, including the corrected `launchd`-doesn't-work test fixture rationale. Independently re-verified the `elcapitan` feature-gate claim against the real installed `core-graphics` 0.25.0 source (`event.rs:777-778`, `Cargo.toml:57`) — confirmed, not taken on faith. Read the `lib.rs` diff — confirmed `sync_teams_call_mute()` returns `()` (not `Result`), called from `toggle_mic_mute_inner` after the recording-state lock is dropped, matching ISC-281's failure-isolation requirement structurally rather than by convention alone. `grep -rni "zoom\|google meet\|\bmeet\b" src-tauri/src/call_mute.rs` → zero matches, confirming ISC-283. `cargo build --lib` → clean. `cargo test --lib` → `test result: ok. 199 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 166.32s` (baseline 195, +4 new), independently re-run by Kai from a clean shell. `bunx tsc --noEmit` → exit 0, independently re-run.
- **Missing NSMicrophoneUsageDescription (real bug from Jeremiah's own test, root-caused and fixed by Kai):** `python3 -c "import wave..."` against the actual recorded WAV (`20260807T214924.wav`) confirmed `nonzero_samples: 0` of `2896896` total — literal, complete silence across the whole 60.35s file, both muted and unmuted spans. `sqlite3 ~/Library/Application Support/com.apple.TCC/TCC.db "SELECT service, client FROM access WHERE service='kTCCServiceMicrophone'"` listed `com.apple.Terminal`, `com.microsoft.teams2`, and five other apps, but no row at all for `com.kairoscompliance.kainotetaker`. `PlistBuddy -c "Print :NSMicrophoneUsageDescription" /Applications/kai-notetaker.app/Contents/Info.plist` → "Does Not Exist" before the fix. Added `src-tauri/Info.plist` with the key; re-ran `bun tauri build` (exit 0) and re-checked the SAME `PlistBuddy` command against the freshly built bundle → returned the exact string written, confirming Tauri's bundler actually merges this file rather than assuming the documented mechanism works. Reinstalled to `/Applications`.
- **MicMuteToggle, delegated to Engineer with a precise spec, independently re-verified by Kai line-by-line (not accepted from self-report):** `git diff --stat` confirmed the exact file list claimed (`audio_capture.rs`, `lib.rs`, `Cargo.toml`, `RecordingBadge.tsx`+`.css`, `RecordingControl.tsx`+`.css`, new `MicMuteIcon.tsx`+`useMicMute.ts`). Read `gate_muted` in full — matches ISC-271 exactly, plus the output-not-input gating refinement in the resampled branch. Read `toggle_mic_mute_inner`/`toggle_mic_mute`/the global-shortcut handler in `lib.rs` — confirmed a single shared function backs both entry points, non-fatal hotkey registration, and the `#[cfg(desktop)]`/target-scoped `Cargo.toml` dependency correctly matches the plugin crate's own mobile-excluding `#![cfg(...)]` gate rather than a blanket top-level add. Independently re-verified the `Modifiers::META` → `SUPER`/Command claim against the real installed `global-hotkey` 0.8.0 source at `hotkey.rs:89-91`, not taken on faith. `cargo test --lib` → `test result: ok. 195 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 133.69s` (baseline 190, +5 new), independently re-run by Kai from a clean shell, not copy-pasted from the Engineer's report. `bunx tsc --noEmit` → exit 0, independently re-run.
- **QuitCrashRace, root-caused and fixed directly by Kai from a real user-supplied crash report (not a hypothetical):** `Read` of `lib.rs` confirms `EngineLoadHandle`, `engine_load_still_in_progress`, both `spawn_engine_loading` call sites passing a real handle, and the `RunEvent::ExitRequested` closure's `prevent_exit()` + join-then-`exit(0)` sequence, matching the crash trace's exact call stack (`ggml_metal_device_free` → `ggml_metal_rsets_free` → `ggml_abort` on the main thread racing `__ggml_metal_rsets_init_block_invoke` on the loading thread). `cargo build --lib` → clean, zero new warnings beyond pre-existing dead-code ones. `cargo test --lib engine_load_handle_tests` → 3/3 `ok`. Full suite: `test result: ok. 190 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 136.51s` (baseline 187, +3 new) — a clean re-run after the one transient `oauth` flake (see Decisions) was confirmed unrelated by passing in isolation. `bunx tsc --noEmit` → exit 0 (this fix never touched frontend code).
- **MeetingTitleStandardization, independently re-verified by Kai (not trusted from Engineer's self-report alone):** `git diff` on `storage.rs`/`lib.rs`/`pipeline.rs` read line-by-line and confirmed to match the delegated spec exactly, including both deliberate deviations (blank-title-as-absent, `mark_meeting_processing` no longer clearing `title`). `git diff --stat -- src/components/RecordingControl.tsx` → empty, confirming ISC-262. `cargo test --lib` → `test result: ok. 187 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 212.43s` (baseline 175, +12 new tests). `bunx tsc --noEmit` → exit 0.
- ISC-258/259/263 (local-time fix, Kai's own follow-on): `Read` of `storage.rs` confirms `fallback_title` converts via `chrono::DateTime::<Utc>::from_naive_utc_and_offset(dt, Utc).with_timezone(&Local)` before formatting; the four exact-string tests (`fallback_title_names_a_manual_recording` etc.) rewritten to independently re-derive the expected stamp via `chrono::Local.from_utc_datetime(&naive)` rather than calling `fallback_title` itself, still pass in the same 187-count run above.
- `examples/import_legacy_recordings.rs` (pre-existing break, fixed): `cargo build --example import_legacy_recordings` → `Finished \`dev\` profile [unoptimized + debuginfo] target(s) in 3.62s`, zero errors (only pre-existing, unrelated dead-code warnings elsewhere in the crate).
- ISC-1/2: `git log --oneline` — `3d9b1ca Initial Tauri + React/TS scaffold (create-tauri-app, bun)`; `package.json` + `src-tauri/Cargo.toml` both present post-scaffold.
- ISC-3: Fresh non-interactive Bash call — `which rustc cargo` → `/opt/homebrew/bin/rustc` / `/opt/homebrew/bin/cargo` (symlinked from Homebrew keg-only rustup into an already-on-PATH directory, since `~/.zshrc` isn't sourced by non-interactive tool calls).
- ISC-4: `cargo check` — `Finished \`dev\` profile [unoptimized + debuginfo] target(s) in 53.70s` on the unmodified scaffold.
- ISC-5: `bun run tauri dev` launched (pid alive), log showed `Running \`target/debug/kai-notetaker\`` and stayed alive 30s with zero panic lines in stderr; process cleanly killed after.
- ISC-8 through ISC-20 (AuditLog): `cargo test audit_log::` — 7/7 tests passed: `append_writes_all_required_fields`, `first_entry_uses_genesis_prev_hash`, `verify_chain_passes_untampered`, `verify_chain_detects_mutation` (asserts tampered index 2 identified), `verify_chain_detects_deletion`, `verify_chain_detects_reordering`, `audit_log_file_path_is_distinct_from_a_hypothetical_db_path`. Public API surface confirmed via `grep "pub fn" src/audit_log.rs` → exactly `new, path, append, read_all, verify_chain` (no update/delete).
- ISC-21 through ISC-28 (Retention): `cargo test retention::` — 5/5 tests passed: `rejects_disabling_retention_via_huge_max_age`, `accepts_reasonable_max_age`, `find_expired_identifies_correct_subset`, `sweep_deletes_expired_and_writes_audit_entries` (asserts 2 audit entries created + `verify_chain()` still `Ok` after the sweep), `sweep_leaves_no_orphaned_child_rows`. ISC-23 caveat logged in Decisions: deletion-logic test runs against a real (unencrypted, in-memory) rusqlite `Connection` — full SQLCipher-encrypted-file integration is deferred to the not-yet-built StorageLayer feature; the SQL deletion behavior itself is proven now, the encryption-at-rest wrapper is a separate, later concern.
- ISC-29 through ISC-36 (CloudSyncGate): `cargo test cloud_sync_gate::` — 5/5 tests passed: `blocked_by_default_with_no_baa_record`, `allowed_with_current_baa_record`, `blocked_when_baa_record_expired`, `blocked_exactly_at_expiration_boundary`, `no_default_allow_for_unmatched_vendor`. `grep -r "reqwest::\|hyper::\|TcpStream::connect" src/` (non-test) → zero matches, confirming ISC-34's intended v1 state (gate exists, nothing to gate around yet).
- ISC-37: `cargo check` succeeded after adding `rusqlite = { version = "0.32", features = ["bundled-sqlcipher-vendored-openssl"] }` to Cargo.toml.
- ISC-76: `grep -n "cfg(target_os" src/audit_log.rs src/retention.rs src/cloud_sync_gate.rs` → zero matches across all three hard-gate modules.
- ISC-89: `awk` scan for `.unwrap()` outside `#[cfg(test)]` blocks in all three hard-gate modules → zero matches.
- ISC-90/91: `grep -n "println!\|eprintln!"` in the three hard-gate modules → zero matches (all logging lives in `lib.rs`'s app-setup wiring, not inside the protected modules).
- ISC-93: `git status --short` after this session's work → changes confined to `src-tauri/Cargo.toml`, `src-tauri/src/lib.rs`, and three new files under `src-tauri/src/`; zero changes under `src/` (the React frontend).
- **Full suite:** `cargo test` — `test result: ok. 17 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`.
- ISC-38: `grep -n "PRAGMA key" src-tauri/src/storage.rs` → `conn.execute_batch(&format!("PRAGMA key = \"x'{}'\";", keychain::to_hex(key)))?;` where `key` comes from `db_key()` → `keychain::get_or_create_db_key()` → `security_framework::passwords::{get_generic_password, set_generic_password}`. No literal key string anywhere in the codebase.
- ISC-39: `cargo test --lib storage::` → `wrong_key_cannot_read_data_written_with_the_real_key ... ok` — real file, real key writes `meetings` row, a second connection with a deliberately wrong 32-byte key fails `SELECT count(*) FROM meetings`.
- ISC-38 (encryption is real, not theater): `cargo test --lib storage::` → `open_connection_actually_encrypts_the_database_file ... ok` — writes a real audio path string, reads the raw file bytes back, asserts the plaintext string is NOT present anywhere in the file.
- Legacy-plaintext safety: `cargo test --lib storage::` → `legacy_plaintext_database_is_archived_not_corrupted ... ok` — a pre-existing plain (un-keyed) database with real data is archived to a `.pre-encryption-backup-<timestamp>` file, and a fresh encrypted database (confirmed empty via `list_meetings`) takes over the original path.
- **Full suite after SQLCipher wiring:** `cargo test --lib` → `test result: ok. 61 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 366.04s` (includes real GPU-accelerated model-loading integration tests, confirming the encryption change didn't regress anything else). `cargo build` → real Mach-O arm64 binary produced at `target/debug/kai-notetaker`.
- ISC-105/107: `cargo test --lib model_provisioning::` → `missing_models_reports_all_five_for_an_empty_directory ... ok`, `missing_models_is_empty_once_every_file_exists ... ok`.
- ISC-106: real `curl -sIL` HEAD requests against all 5 candidate URLs, `content-length` compared byte-for-byte against `stat -f%z` on the real local files — all 5 matched exactly (Whisper 147951465, diarization segmentation archive 6958444, speaker-embedding 29596978, LLM 4920739232, embeddings 67308128).
- ISC-108: `cargo test --lib model_provisioning::resolve_models_dir_prefers_dev_source_tree_when_appdata_is_empty ... ok`; `grep -n "CARGO_MANIFEST_DIR" src-tauri/src/lib.rs` shows zero remaining matches (moved entirely into `model_provisioning::resolve_models_dir`'s dev-only fallback branch).
- ISC-109: `grep -n "downloaded.*total" src/components/FirstRunSetup.tsx` shows the progress bar's width is computed from the real `model-download-progress` event payload's `downloaded`/`total` fields, not a fake/simulated percentage.
- **Full suite after installer wiring:** `cargo test --lib` → `test result: ok. 65 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 72.98s`. `cargo build` → clean. `bunx tsc --noEmit` → exit 0.
- ISC-112: `cargo test --lib pipeline::read_wav_mono_resampled_accepts_a_real_16k_fixture ... ok` — real 16kHz fixture read and confirmed pass-through unchanged (no resampling artifact) since it's already at `PIPELINE_SAMPLE_RATE`.
- ISC-115: real process list (`ps aux`) confirmed only one `target/debug/kai-notetaker` process before the import started; that process was killed (verified absent via a second `ps aux` check) before `cargo run --example import_legacy_recordings` began.
- ISC-113: real crash on attempt 1 (see Changelog — `n_batch`/`n_ctx` mismatch, real 43-minute transcript), fixed in `llm.rs`, full suite re-verified 66/66 passing before retry. Attempt 2's own log, in full: `=== import complete: 6 succeeded, 0 failed ===`, with per-meeting evidence — `OK: 2026-08-01T04-12-40-450Z-teams-call -> meeting_id=1, duration=149s` (attempt 1), `OK: 2026-08-01T04-20-38-748Z-call-at-11-19-pm -> meeting_id=2, duration=278s` (attempt 1), `OK: FwPicDssAssessment_2026-08-03_13-59-36 -> meeting_id=3, duration=2603s` (attempt 2, post-fix), `OK: RealTitleLookupTestMeeting_2026-08-01_00-30-50 -> meeting_id=4, duration=10s`, `OK: StrategyCallWithNesta_2026-08-03_18-48-20 -> meeting_id=5, duration=1302s`, `OK: UntitledMeeting_2026-08-01_00-24-55 -> meeting_id=6, duration=10s`. Attempt 2 also confirmed the idempotency logic worked correctly: it printed `--- skipping ... already imported and ready ---` for meeting_id 1 and 2, and `cleaning up stale non-ready meeting row from a previous attempt: id=3` for the crashed attempt, rather than duplicating or silently reprocessing either.
- ISC-114: `PULSE.toml`'s `meeting-watcher` job now reads `enabled = false`, edited only after the ISC-113 evidence above was in hand — real file read-back confirms the edit landed.
- ISC-119/120: `cargo test --lib storage::` → `rename_meeting_updates_title ... ok`, `rename_meeting_errors_for_unknown_id ... ok`, `deleted_meeting_disappears_from_list_but_undelete_restores_it ... ok`, `delete_meeting_errors_for_unknown_id ... ok`, `deleted_at_column_migration_is_idempotent_on_a_pre_existing_database ... ok`. `bunx tsc --noEmit` → exit 0 after wiring `rename_meeting`/`delete_meeting`/`undelete_meeting` commands and the shared `App.tsx` undo-toast state.
- ISC-121: real reprocessing of meeting_id=3 (`FwPicDssAssessment`) via `cargo run --example import_legacy_recordings -- reprocess 3`, then `-- inspect 3`. Before: `distinct speaker labels: {..., Some(82), ...}` (39 distinct values, up to 82). After the `threshold: 0.9` fix: `distinct speaker labels: {None, Some(0)..Some(11)}` (12 distinct values). Real, large improvement; not exactly the true 3 speakers — logged honestly as partial, not perfect.
- ISC-122/123: same real reprocessing target. Before any fix: 1579 transcript segments, `sort | uniq -c` showed 1481 (94%) identical to `"I don't know much about it, honestly."`. After `no_context: true` ALONE (attempt 1): byte-identical — still 1579 segments, still 1481 repeats (this is the ISC-123 anti-criterion evidence, a real negative result). After ALSO adding `logprob_thold: -0.5` (attempt 2): 338 segments, `sort | uniq -c` shows no repeated-phrase pattern (max repeat count 12, for a natural "Mmm."). Real summary text before: "...appears to be a test recording or sound check..."; after: genuinely coherent content about Smithville's PCI-DSS scope, network/data-flow diagrams, CMMC, and QSA engagement — matching what Jeremiah described the real call being about.
- **Full suite after diarization/ASR fixes:** `cargo test --lib` → `test result: ok. 71 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` (rename/delete/migrate/diarization/asr changes together, no regressions).
- ISC-111: `cargo run --example verify_model_download` — real network download into the actual `$APPDATA/models` production directory. Whisper: 147951465 bytes exact match. Diarization speaker-embedding: 29596978 bytes exact match. LLM: 4920739232 bytes exact match. Embeddings: 67308128 bytes exact match. Diarization segmentation (archive-extracted): initial run flagged a "MISMATCH" comparing extracted-file-size (5992913) against the archive's compressed download size (6958444) — a test-script bug; `shasum -a 256` on both the newly-extracted file and the known-good dev-tree copy returned the identical hash (`220ad67c...e938e1079`), proving the extraction was correct all along. Fixed the test's comparison logic (skip byte-count comparison for archive-extracted specs, just confirm non-empty), re-ran: `=== verification complete: 0 failed, 0 of 5 still missing ===`.
- ISC-126: `git show HEAD~1:README.md` (before this fix) → 3 lines, generic create-tauri-app template. After: real prerequisites (Rust, bun, Xcode CLT, CMake — `cmake` confirmed as a real transitive dependency via `grep 'name = "cmake"' src-tauri/Cargo.lock` and `which cmake`, not assumed), first-time setup steps, the 5-model download table, and an encryption/Keychain note.
- **Full suite after model_provisioning made pub + README/verify_model_download added:** `cargo test --lib` → `test result: ok. 71 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2172.32s`.
- ISC-128: `cargo test --lib storage::` → `get_or_create_known_speaker_is_idempotent_by_name ... ok`, `speaker_embedding_samples_round_trip_through_storage ... ok`, `label_meeting_speaker_upserts_and_resolves_in_meeting_detail ... ok`, `unlabeled_speaker_falls_back_to_none ... ok`, `list_known_speakers_returns_all_enrolled_names_sorted ... ok`.
- ISC-129: `cargo test --lib speaker_id::` → `loads_real_model_and_reports_a_real_dimension ... ok`, `enrolled_speaker_is_found_by_their_own_embedding ... ok`, `unenrolled_voice_does_not_match_a_different_enrolled_speaker ... ok` — real sherpa-onnx model loaded, real embeddings extracted and matched, not mocked.
- ISC-130/131: `cargo test --lib pipeline::` → `a_voice_enrolled_after_one_meeting_is_auto_recognized_in_the_next ... ok` — meeting 1 processed with an unenrolled voice (all `speaker_label` confirmed `None`), that speaker manually enrolled as "Test Speaker" (simulating a real label action), meeting 2 processed with the same voice: `speaker_label == Some("Test Speaker")` present on the resulting segments and the generated summary is non-empty.
- ISC-132/133: `cargo build` (full app) → clean; `grep -n "list_known_speakers\|label_meeting_speaker" src-tauri/src/lib.rs` shows both commands defined and present in the `generate_handler!` list. `bunx tsc --noEmit -p tsconfig.json` → exit 0 after wiring the click-to-rename speaker UI (autocomplete `<datalist>`, "Remember" checkbox, Save/Cancel) into `MeetingDetail.tsx`.
- ISC-134: `grep -n "meeting_speaker_labels" src-tauri/examples/import_legacy_recordings.rs` shows the `DELETE FROM meeting_speaker_labels WHERE meeting_id = ?1` added to both the `reprocess` mode's cleanup block and the crash-recovery stale-row cleanup block, alongside the pre-existing transcript/summary/action-item/embedding deletes.
- ISC-135: `grep -n "add_speaker_embedding_sample" src-tauri/src/pipeline.rs src-tauri/src/lib.rs` — zero matches inside `pipeline.rs`'s auto-match block (only `label_meeting_speaker` is called there); the only call site is `lib.rs`'s `label_meeting_speaker` Tauri command, inside its `remember` branch — confirming auto-match cannot silently add embedding samples.
- **Full suite after SpeakerIdentification:** `cargo test --lib` → `test result: ok. 80 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 131.87s` (real GPU-accelerated model tests included — Whisper, diarization, 8B LLM, embeddings, and the new speaker-ID engine, all loaded concurrently with no regression). `cargo build` → clean, only pre-existing dead-code warnings. `bunx tsc --noEmit -p tsconfig.json` → exit 0.
- ISC-137: real reproduction, `cargo run --example import_legacy_recordings -- inspect 3` against Jeremiah's actual Smithville call, quoted verbatim — segments at `[28560ms-30360ms] speaker=Some(0)` through `[599320ms-600320ms] speaker=Some(0)` (nearly 10 minutes) with zero index change in between, confirming diarization detected no turn boundary across that whole span, not merely a labeling-scope choice.
- ISC-136/140: `cargo test --lib storage::` → `segment_override_takes_precedence_over_index_wide_label ... ok` — an index-wide label resolves normally for an unoverridden segment, but a segment-level override on a second segment sharing the same raw index wins over it.
- ISC-138: `cargo test --lib storage::` → `segment_override_corrects_a_diarization_merge_without_touching_the_other_speaker ... ok` — reproduces the exact reported shape (two segments early, two segments ~7 minutes later, all four sharing raw index 0, no boundary between them): labeling only the later two as "Dave" leaves the earlier two unlabeled (`None`), then labeling the earlier two as "Jeremiah" resolves all four correctly and independently.
- ISC-139: `grep -n "applyToWholeSpeaker\|editingRange" src/components/MeetingDetail.tsx` confirms `applyToWholeSpeaker` initializes to `false` and is only set `true` via an explicit checkbox; `startEditingSpeaker`'s shift-click branch extends `editingRange` rather than replacing it. `bunx tsc --noEmit -p tsconfig.json` → exit 0.
- **Full suite (storage:: subset) after SegmentScopedSpeakerCorrection:** `cargo test --lib storage::` → `test result: ok. 21 passed; 0 failed; 0 ignored; 0 measured; 61 filtered out; finished in 5.42s`. `cargo build` and `cargo build --examples` → both clean.
- **Full suite (all modules, real GPU model tests included) after SegmentScopedSpeakerCorrection:** `cargo test --lib` → `test result: ok. 82 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 193.87s`. Zero regressions from the `label_meeting_speaker` → `label_transcript_segments` command rename or the new schema.
- ISC-141: real model test — `cargo test --lib diarization::tests::known_speaker_count_forces_exactly_that_many_clusters` → `ok`, forcing `Some(1)` against a real single-voice fixture still reports exactly 1 speaker (plumbing exercised for real, not just compiled).
- ISC-144: real reprocess of Jeremiah's actual Smithville call — `cargo run --example import_legacy_recordings -- reprocess 3 3` → `OK: reprocessed meeting_id=3`. `inspect 3` afterward: `distinct speaker labels: {None, Some(0), Some(1), Some(2)}` (down from 12 distinct indices before this fix) with a real, plausible per-speaker segment distribution (`None: 51, Some(0): 207, Some(1): 58, Some(2): 22`, 338 total). Directly re-checked the exact stretch Jeremiah reported (~7-minute mark, `433320ms`-`501320ms`): now alternates turn-by-turn between `Some(0)` and `Some(1)` rather than one unbroken `Some(0)` run — the real turn boundary that was previously missed entirely is now detected.
- ISC-142/145: `grep -n "engines_arc.diarization =" src-tauri/src/lib.rs` → zero matches; the known-count engine is only ever passed as `Some(&fresh_diarization)` into `process_meeting`, never assigned into the shared `PipelineEngines`.
- **Full suite after KnownSpeakerCountOverride:** `cargo test --lib` → `test result: ok. 84 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 125.16s`. Zero regressions from the `DiarizationEngine::load`/`process_meeting` signature changes across ASR/LLM/embeddings/storage.
- ISC-159: `calendar::tests::parses_a_real_graph_shaped_response_into_upcoming_meetings` (extended) — real Graph `id` field parsed into `UpcomingMeeting.id`, distinct per occurrence. Independently re-ran `cargo test --lib` (Kai, not just Engineer's self-report): `test result: ok. 113 passed; 0 failed`.
- ISC-160/161: `storage::auto_join_log_persists_across_reconnect` (real file-backed connection closed and reopened, row survives) and `storage::log_auto_join_is_idempotent_and_preserves_the_first_trigger_time`; `auto_join::does_not_double_trigger_same_event` at both the `decide()` and full `poll_cycle` level.
- ISC-162/175: `auto_join::never_triggers_a_meeting_without_a_join_url` — covers `None`, whitespace-only, and a meeting whose only link is Zoom text sitting in the event body (confirmed NOT parsed — `grep -in "\.body\|\.location"` in `auto_join.rs` shows the string only in a test-name/comment, no parsing code).
- ISC-163: `auto_join::skips_auto_start_when_already_recording` — confirmed via direct `Read` of `run_auto_join_cycle` (`lib.rs:347-353`): locks `RecordingState`, checks `.is_some()` before ever calling `start_recording`; still opens the join link and logs per the ISC's own wording.
- ISC-164/173: `storage::auto_join_is_disabled_by_default_on_a_fresh_database`; `run_auto_join_cycle` (`lib.rs:302-313`) checks `oauth::load_client_id` first and returns before opening any DB connection when absent — confirmed via `Read`, matches "zero poller activity, not a silent DB open."
- ISC-165/174: `Read` of `CalendarSettings.tsx` shows the checkbox bound to `get_auto_join_enabled`/`set_auto_join_enabled` and an `autoJoined` log list rendered from `list_auto_joined_meetings`, refreshed on mount + every 30s. `bunx tsc --noEmit` → exit 0 (independently re-run by Kai).
- ISC-166: `auto_join::toggle_takes_effect_next_cycle`; confirmed in `run_auto_join_cycle` the setting is read fresh from storage every call, never cached.
- ISC-167: `Read` of `lib.rs` setup() shows the poller spawned unconditionally via `tauri::async_runtime::spawn` on a `POLL_INTERVAL_SECS` interval, independent of any UI tab being active.
- ISC-168/178: `auto_join::eligibility_window_boundaries` (60/61s and 300/301s exact edges) and `auto_join::no_meeting_can_slip_between_two_consecutive_polls` (61s case + a 0-120s sweep proving no gap between polls).
- ISC-169: `auto_join::survives_graph_api_error` — a failing fetcher is skipped (others still processed), a failing idempotency-lookup ends the cycle early with zero actions rather than risking a duplicate trigger; no panic in any case.
- ISC-170: `Read` confirms `open::that(join_url)` is the real call (same one `connect_microsoft` already used); `git diff Cargo.toml` for this feature is empty — no new bot-API dependency.
- ISC-171: `Read` of `run_auto_join_cycle` shows exactly one call site — `start_recording(app.clone(), app.state::<RecordingState>())` — the identical command function the UI's Start Recording button invokes.
- ISC-172/172.1: `Read` of `auto_join.rs` confirms `pub type MeetingFetcher = Box<dyn Fn() -> Result<Vec<UpcomingMeeting>, String> + Send + Sync>` and `poll_cycle(..., fetchers: &[MeetingFetcher], ...)` — the poll cycle never names "Microsoft" directly; `auto_join::merges_meetings_from_multiple_registered_providers` proves multi-fetcher concatenation works today, before any second provider exists.
- ISC-176/177: Independently re-run by Kai (not just Engineer's self-report) — `cargo test --lib`: `test result: ok. 113 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 150.49s`. `bunx tsc --noEmit`: exit 0.
- **Two real bugs found and fixed during this feature's own test-writing** (Engineer's report, spot-checked by Kai): an invalid chrono format spec (`%.7f`, not a real spec — chrono only offers `%.f`/`%.3f`/`%.6f`/`%.9f`) in a test fixture helper, not production code; and a wall-clock-`now()` fractional-second bug making boundary-test assertions unreliable, fixed with a deterministic `fixed_now()` helper. Both are test-infrastructure fixes, not behavior changes to `auto_join.rs`'s actual logic.
- ISC-181: `Read` of `lib.rs::run_auto_join_cycle` and `stop_recording` confirms `AutoRecordingState` is set only at auto-trigger time and cleared on every stop path; `cargo test --lib auto_join::tests::should_auto_stop_fires_strictly_after_the_meetings_end` — `ok`.
- ISC-182-190 (GoogleCalendarOAuth): `Read` of `google.rs` confirms real endpoints (`accounts.google.com/o/oauth2/v2/auth`, `oauth2.googleapis.com/token`, `www.googleapis.com/calendar/v3/calendars/primary/events`), scope `calendar.readonly`, no `client_secret`. `cargo test --lib google::` — 6/6 passing, including `resolves_join_url_from_conference_data_then_falls_back_to_hangout_link` and `an_all_day_event_never_auto_joins_and_never_panics`. Independently re-verified by Kai: `grep -n "singleEvents\|access_type=offline" google.rs` confirms both real, load-bearing query params Engineer added beyond the original spec (without them, recurring-event start times and refresh-token issuance would both be silently wrong) — logged as a real, disclosed deviation, not hidden.
- ISC-191-199 (ZoomOAuth): `Read` of `zoom.rs` confirms real endpoints (`zoom.us/oauth/authorize`, `zoom.us/oauth/token`, `api.zoom.us/v2/users/me/meetings`), scope `meeting:read:list_meetings` (non-admin), no `client_secret` (Zoom's public-client app-registration toggle). `cargo test --lib zoom::` — 6/6 passing, including `end_is_computed_as_start_plus_duration_minutes` and `a_zoom_meeting_flows_through_the_shared_eligibility_logic` (proves Zoom's computed `end` correctly drives the SAME `auto_join::eligibility()` used by Microsoft/Google — no Zoom-specific branch). `cargo test --lib auto_join::tests::active_providers_match_exactly_the_connected_set_for_every_combination` — all 8 on/off combinations pass, including cross-wiring protection (each provider's own client id, never another's).
- ISC-200-207 (SilenceBasedStopPrompt): `Read` of `audio_capture.rs` confirms `trailing_rms`/`trailing_rms_of` reads only the buffer's tail (not the whole recording) without disturbing the live `cpal::Stream`; `Read` of `lib.rs::silence_monitor_tick`/`prompt_stop_or_continue` confirms the tracker lock is explicitly scoped and dropped BEFORE the dialog call (avoiding a real deadlock risk against the non-reentrant `std::sync::Mutex`, since the dialog's own callback re-locks the same tracker on "Continue") and confirms it only ever fires when `AutoRecordingState` holds a marker (manual recordings excluded, ISC-205). `cargo test --lib audio_capture::tests::trailing_rms` (4 tests) and `cargo test --lib silence_monitor::` (7 tests) all passing.
- **Full suite, independently re-run by Kai after this round (not trusted from Engineer's self-report alone):** `cargo test --lib` → `test result: ok. 140 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 117.46s`. `bunx tsc --noEmit` → exit 0.
- ISC-209/210 (RecordingDurability, the load-bearing proof): `Read` of `recording_killed_mid_flight_is_still_a_valid_readable_wav_up_to_the_last_flush` confirms the test reads the WAV file *while the `RecordingSession` is still alive* — before any `Drop` could run — which is the genuine proof that `flush()` alone (not a destructor) makes a never-finalized file valid and readable. `cargo test --lib audio_capture::` — both new tests `ok`.
- ISC-211: `grep -n "WavWriter\|File::create\|\.flush()\|\.finalize()" audio_capture.rs` confirms zero references inside `build_capture_stream`'s callback closures — every hit is inside `spawn_flusher` or `start()`, never the real-time audio path.
- ISC-213/214: `grep -rn "stop_and_write(" src-tauri/src/lib.rs` shows exactly one call site (`lib.rs:176`, the `stop_recording` command) — the calendar-end auto-stop and silence-prompt paths both route through the `stop_recording` command rather than calling `stop_and_write` directly, so the "three call sites" originally anticipated collapses to one real call site plus its two internal callers, with zero code changes at any of them. `cargo test --lib` confirms the pre-existing `stopped_recording_always_declares_canonical_sample_rate` and `recording_path_is_inside_provided_data_dir_never_system_temp` tests pass unmodified.
- ISC-217-221 (orphan recovery): `Read` of `find_orphaned_recordings`/`recover_orphan_into_db`/`recover_orphaned_recordings` confirms the exact spec — regex-equivalent filename matching, a single batched `all_audio_paths` query diffed in memory (not one query per file), real duration read via `hound::WavReader`, and the identical `pipeline::process_meeting` kickoff pattern `stop_recording` already uses (own thread, own DB connection, same audit log). `cargo test --lib` — all 7 new orphan-recovery tests `ok`, including the disclosed `imported-*`-exclusion and already-known-row-exclusion cases.
- **Full suite after RecordingDurability, independently re-run by Kai (not trusted from Engineer's self-report alone):** `cargo test --lib` → `test result: ok. 151 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 136.41s`. `bunx tsc --noEmit` → exit 0.
- ISC-224/225: `Read` of `calendar.rs::microsoft_config` confirms the scope string is `"openid profile email offline_access Calendars.Read Presence.Read"`, and a test explicitly asserts it does NOT contain `Presence.Read.All`. `cargo test --lib calendar::` — passing.
- ISC-228: `Read` of `presence.rs::classify_activity` confirms case-matching on `inACall`/`inAMeeting` → `InCall`, all else → `NotInCall` (fail-closed on unrecognized values). `cargo test --lib presence::` — passing.
- ISC-233/234/235: `Read` of `auto_join.rs::AutoStopTrigger::calendar_auto_stop_due` confirms `CalendarEnd(end)` still calls the original `should_auto_stop` unmodified, while `PresenceBased` always returns `false` from THIS check — isolating the two stop mechanisms exactly as designed (a presence-triggered recording is never touched by the calendar loop's time check, and vice versa). Silence monitor's `guard.is_some()` check confirmed unchanged — composes with both variants.
- ISC-238-240: `Read` of `lib.rs` confirms exactly one `WebviewWindowBuilder::new` call site (verified against the actually-installed `tauri` 2.11.5, not just docs — `.title/.inner_size/.position/.always_on_top/.decorations/.resizable/.visible/.skip_taskbar/.shadow`, all real methods on that version), one `.show()`, one `.hide()`.
- **Real bug found and fixed during this feature's own build, before any test could have caught it:** `capabilities/default.json` scoped `invoke()` permissions to `"windows": ["main"]` only — the overlay window would have rendered and then frozen at `00:00` forever, since every command call from `"recording-badge"` would have been silently denied by Tauri's own permission system. No Rust or TypeScript test exercises this (it's a runtime IPC-permission check, not a compile-time one) — caught by Engineer reasoning about the actual deployed config, not by the test suite. Fixed: `"windows": ["main", "recording-badge"]`.
- **Real off-screen hazard found and fixed:** a hardcoded overlay position would misplace the badge (partially or fully off-screen) on any display narrower than the one it was tuned for — a real risk given an indicator whose entire job is being visible. Fixed with a pure `recording_badge_position` function computed from the primary monitor's real logical size, with a conservative fallback; unit-tested across 5 real display-size fixtures plus degenerate inputs.
- **Full suite after TeamsPresenceAdhocRecording + RecordingIndicatorOverlay, independently re-run by Kai (not trusted from Engineer's self-report alone):** `cargo test --lib` → `test result: ok. 171 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 140.94s`. `bunx tsc --noEmit` → exit 0.
- ISC-245: `Read` of `storage.rs` confirms `ALTER TABLE meetings ADD COLUMN trigger_source TEXT;` — nullable, no `NOT NULL`, so pre-existing rows are never retroactively assigned a fabricated trigger. `cargo test --lib storage::tests::trigger_source_column_migration_is_idempotent_and_preserves_pre_existing_rows` — `ok`.
- ISC-246/247: `Read` confirms `TriggerSource` is a 4-variant enum (`Manual`/`Calendar`/`Presence`/`Recovered`) and `create_meeting`'s new parameter is required (not `Option`) — a future caller that forgets it is a compile error, not a silently-defaulted row.
- ISC-249: `git diff`/`git status` on `RecordingControl.tsx` confirm zero changes — the manual Start Recording button's existing no-argument `invoke("start_recording")` call needed no modification at all.
- ISC-250/251: `Read` of `lib.rs` confirms the calendar-triggered call site passes `Some(TriggerSource::Calendar)` explicitly (line 656), the presence-triggered call site passes `Some(TriggerSource::Presence)` explicitly (line 758), and the orphan-recovery call site passes a hardcoded `TriggerSource::Recovered` (line 1245) — none fall through to the `Manual` default.
- ISC-252/253: `Read` of `MeetingDetail.tsx`/`MeetingLibrary.tsx`/`lib/triggerSource.ts` confirms both surfaces render the field via shared label/icon helpers, gracefully rendering nothing for `null` (pre-existing meetings recorded before this feature) rather than a misleading label.
- ISC-254 (anti-criterion): `grep -rn "trigger_source\|TriggerSource" src-tauri/src/summarization.rs src-tauri/src/llm.rs` → zero matches, independently re-confirmed by Kai. Provenance never reaches the LLM summarization prompt.
- **Full suite after RecordingTriggerProvenance, independently re-run by Kai (not trusted from Engineer's self-report alone):** `cargo test --lib` → `test result: ok. 175 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 149.91s`. `bunx tsc --noEmit` → exit 0.
