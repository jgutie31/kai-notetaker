{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the recording indicator overlay",
  "windows": ["main", "recording-badge"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
